openapi: 3.1.0
info:
  title: Owlette API
  version: 2.3.1
  description: |
    The Owlette API lets you programmatically manage your fleet of Windows machines,
    deploy software, monitor processes, execute tools, and integrate with external systems
    via webhooks.

    ## Authentication

    Protected API requests use scoped credentials. New public integrations
    should send `Authorization: Bearer owk_live_...` or
    `Authorization: Bearer owk_test_...`.
    Create API keys from the Owlette dashboard under Settings > API Keys.
    Scripted `POST /api/keys` creation requires a user session or Firebase ID
    token; API keys cannot bootstrap broader replacement keys.

    Include your API key using one of these methods:

    - **Header** (recommended): `Authorization: Bearer owk_your_key_here`
    - **Legacy header**: `x-api-key: owk_your_key_here`
    - **Compatibility query parameter**: `?api_key=owk_your_key_here`

    Every protected operation includes an Authentication and scopes note. API-key
    callers must hold at least one listed scope and must also satisfy resource
    ownership, site membership, role, and capability checks.

    ## Versioning

    Owlette does not require a new global API-version header for the
    developer-preview MVP. Roost/project-distribution routes may accept the
    existing advisory `Roost-Version: YYYY-MM-DD` header; missing headers can
    emit `X-Roost-Version-Missing: true`.

    ## Rate Limiting

    Responses may include `RateLimit-Limit`, `RateLimit-Remaining`,
    `RateLimit-Reset`, and `Retry-After`. API-key traffic is bucketed by key id
    where available. See `/docs/api/rate-limits`.

    ## Pagination

    Collection endpoints use `page_size` and `page_token`, and return
    `next_page_token` when paginated. Some compatibility routes also accept
    `limit` / `cursor` or return `nextPageToken`. See `/docs/api/pagination`.

    ## Idempotency

    Mutating endpoints that can trigger side effects require or accept
    `Idempotency-Key`. Replays are scoped to the same user, environment,
    method, path, query, and body hash. See `/docs/api/idempotency`.

    ## Errors

    Public errors use `application/problem+json` with stable `code`,
    `requestId`, and `docsUrl` fields. Some dashboard compatibility routes may
    retain a temporary `error` alias during the preview window:

    ```json
    { "error": "Description of what went wrong" }
    ```

    Common HTTP status codes:
    | Code | Meaning |
    |------|---------|
    | 400  | Bad request - missing or invalid parameters |
    | 401  | Unauthorized - missing or invalid API key |
    | 403  | Forbidden - no access to this site or resource |
    | 404  | Not found |
    | 409  | Conflict - resource is in an incompatible state |
    | 429  | Rate limit exceeded |
    | 500  | Internal server error |

    ## Base URLs

    | Environment | URL |
    |-------------|-----|
    | Production  | `https://owlette.app` |
    | Development | `https://dev.owlette.app` |
  license:
    name: FSL-1.1-Apache-2.0
    url: https://fsl.software/FSL-1.1-Apache-2.0.template.md
  contact:
    name: Owlette Support

servers:
  - url: https://owlette.app
    description: Production
  - url: https://dev.owlette.app
    description: Development

security:
  - apiKey: []
  - bearerApiKey: []
  - firebaseIdToken: []

tags:
  # One Title-Case tag per resource. Each operation's `tags:` value must match
  # one of these names exactly (case-sensitive) — an unmatched value spawns a
  # description-less group, and a name with no operations renders as an empty
  # section. Keep this list and the operation tags in sync.
  - name: Sites
    description: List, create, and manage sites, and issue or revoke agent enrollment tokens.
  - name: Machines
    description: Monitor and manage machines in your fleet — inventory, commands, display layout, reboot schedules, and uninstall.
  - name: Processes
    description: Configure and control monitored processes.
  - name: Screenshots
    description: |
      Capture, retrieve, and delete machine screenshots. To capture, send
      `type: capture_screenshot` to the machine commands endpoint
      (`POST .../machines/{machineId}/commands`), then poll the returned
      command for a signed `result.screenshot_url`.
  - name: Deployments
    description: Deploy installers and project distributions to machines; track, retry, and cancel rollouts.
  - name: Presets
    description: Manage site-scoped schedule, reboot, distribution, and deployment-template presets.
  - name: Alerts
    description: Manage alert rules.
  - name: Talons
    description: |
      Automations that watch a site and act on it — a trigger (schedule,
      metric threshold, or event), an optional condition, and one or more
      outputs (email, webhook, hoot directive, process command).
  - name: Logs
    description: Query activity logs.
  - name: Audit Log
    description: |
      Tamper-evident, hash-chained log of every mutating operation
      performed against a site's roost resources.
  - name: Quotas
    description: Storage and bandwidth usage against plan limits.
  - name: Webhooks
    description: |
      HTTP-callback subscriptions for roost events (publish, rollback,
      deploy, quota, etc.). See [webhooks.md](/docs/api/webhooks).
  - name: API Keys
    description: |
      Self-service api key management. Scoped, expiring bearer tokens
      (`owk_live_*` / `owk_test_*`) used to authenticate every public
      roost request. See [authentication.md](/docs/api/authentication).
  - name: Installer
    description: Agent installer versions, platform system presets, and TouchDesigner builds.
  - name: Hoot
    description: AI-powered chat and tool execution.
  - name: Users
    description: Manage platform users — roles, site assignments, and account provisioning.
  - name: Site Members
    description: Manage per-site membership and member roles.
  - name: Roost
    description: |
      Content-addressed project sync (v2). Upload folders once,
      fan out atomic deploys to the fleet, roll back instantly.
      All roost endpoints use RFC 7807 `application/problem+json`
      error envelopes (see the `ProblemDetails` schema).
  - name: Versions
    description: |
      Immutable oci-style roost versions. Every publish creates a new
      version; rollback is a pointer flip. See [versions.md](/docs/api/versions).
  - name: Chunks
    description: |
      Content-addressed blob layer for roost. Dedup lookup, signed
      upload/download urls, cross-roost mount. See [chunks.md](/docs/api/chunks).
  - name: Utility
    description: Identity, version, live event stream, and platform diagnostics.

# Sidebar grouping (Scalar / Redocly `x-tagGroups`). NOTE: every tag declared
# above must appear in exactly one group — Scalar hides any tag that is not in
# a group. Keep this in sync when adding or renaming tags.
x-tagGroups:
  - name: fleet
    tags: [Sites, Machines, Processes, Screenshots]
  - name: deployment
    tags: [Deployments, Presets, Installer]
  - name: roost
    tags: [Roost, Versions, Chunks, Audit Log]
  - name: hoot
    tags: [Hoot]
  - name: monitoring
    tags: [Logs, Alerts, Talons, Quotas]
  - name: access & identity
    tags: [Users, Site Members, API Keys]
  - name: platform
    tags: [Webhooks, Utility]

paths:
  /api/account/api-keys:
    get:
      operationId: listAccountApiKeys
      tags: [API Keys]
      summary: List account API keys
      description: Lists API key metadata for the authenticated superadmin account. Raw keys and hashes are never returned.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: API keys
          content:
            application/json:
              schema:
                type: object
                required: [success, keys]
                properties:
                  success: { type: boolean }
                  keys:
                    type: array
                    items: { $ref: '#/components/schemas/ApiKey' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createAccountApiKey
      tags: [API Keys]
      summary: Create account API key
      description: |
        Creates a superadmin account API key and returns the raw key once.
        New account keys use modern scoped metadata; scoped API-key callers
        carry their existing scopes forward rather than widening them.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: API key created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiKeyCreateResponse' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/account/api-keys/{keyId}:
    parameters:
      - name: keyId
        in: path
        required: true
        schema: { type: string }
    delete:
      operationId: revokeAccountApiKey
      tags: [API Keys]
      summary: Revoke account API key
      description: |
        Revokes an API key owned by the authenticated superadmin account. Same
        semantics as `DELETE /api/keys/{keyId}`: immediate, not a delete — the
        key is stamped `revokedAt` and kept — and idempotent.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: API key revoked
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/platform/email/config:
    get:
      operationId: getPlatformEmailConfig
      tags: [Utility]
      summary: Get platform email configuration
      description: Returns platform email provider configuration for superadmin diagnostics.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Email configuration

  /api/platform/security/kill-switch:
    post:
      operationId: updateSecurityKillSwitch
      tags: [Utility]
      summary: Update security migration kill switch
      description: Flips capability or rate-limit enforcement for a bounded period. Superadmin only.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Kill switch updated
        '400':
          description: Validation error

  /api/platform/system-presets:
    get:
      operationId: listPlatformSystemPresets
      tags: [Installer]
      summary: List platform system presets
      description: Lists global system presets shared across sites.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: System presets
    post:
      operationId: createPlatformSystemPreset
      tags: [Installer]
      summary: Create platform system preset
      description: Creates a global system preset. Superadmin only.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '201':
          description: Preset created

  /api/platform/system-presets/{presetId}:
    parameters:
      - name: presetId
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: getPlatformSystemPreset
      tags: [Installer]
      summary: Get platform system preset
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: System preset
    patch:
      operationId: updatePlatformSystemPreset
      tags: [Installer]
      summary: Update platform system preset
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Preset updated
    delete:
      operationId: deletePlatformSystemPreset
      tags: [Installer]
      summary: Delete platform system preset
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '204':
          description: Preset deleted

  /api/platform/touchdesigner/builds:
    get:
      operationId: listTouchDesignerBuilds
      tags: [Installer]
      summary: List TouchDesigner builds
      description: Fetches recent TouchDesigner installer builds from Derivative's archive page. Superadmin only.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: TouchDesigner builds

  /api/sites/{siteId}/agent-tokens:
    parameters:
      - name: siteId
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: listSiteAgentTokens
      tags: [Sites]
      summary: List site agent refresh tokens
      description: |
        Lists agent refresh token metadata for a site. Superadmin-only via
        `GLOBAL_SETTINGS_WRITE`; API-key callers need site read scope and a
        superadmin owning user.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Agent tokens.
          content:
            application/json:
              schema:
                type: object
                required: [tokens, count]
                properties:
                  tokens:
                    type: array
                    items:
                      type: object
                      required: [id]
                      properties:
                        id: { type: string }
                        machineId: { type: string, nullable: true }
                        version: { type: string, nullable: true }
                        createdBy: { type: string, nullable: true }
                        createdAt: { type: string, format: date-time, nullable: true }
                        lastUsed: { type: string, format: date-time, nullable: true }
                        expiresAt: { type: string, format: date-time, nullable: true }
                        agentUid: { type: string, nullable: true }
                  count: { type: integer, minimum: 0 }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/agent-tokens/revoke:
    parameters:
      - name: siteId
        in: path
        required: true
        schema: { type: string }
    post:
      operationId: revokeSiteAgentTokens
      tags: [Sites]
      summary: Revoke site agent refresh tokens
      description: |
        Revokes one token, all tokens for a machine, or all tokens for a site.
        Superadmin-only via `GLOBAL_SETTINGS_WRITE`; API-key callers need site
        write scope and a superadmin owning user.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tokenId: { type: string }
                machineId: { type: string }
                all: { type: boolean }
              anyOf:
                - required: [tokenId]
                - required: [machineId]
                - required: [all]
      responses:
        '200':
          description: Tokens revoked.
          content:
            application/json:
              schema:
                type: object
                required: [success, revokedCount, message]
                properties:
                  success: { type: boolean }
                  revokedCount: { type: integer, minimum: 0 }
                  message: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/alerts:
    parameters:
      - name: siteId
        in: path
        required: true
        schema: { type: string }
    put:
      operationId: setSiteAlertRules
      tags: [Alerts]
      summary: Set site alert rules
      description: Replaces the alert rules array at `sites/{siteId}/settings/alerts`. Requires `site=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Alert rules saved

  /api/sites/{siteId}/talons:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: listTalons
      tags: [Talons]
      summary: list the site's talons
      description: |
        Returns every talon on the site, ordered by name. Not paginated — a
        site may hold at most 20 talons. Webhook signing secrets live in a
        server-only collection and are never returned by any endpoint.

        Requires `TALON_MANAGE`; API keys need `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: The site's talons.
          content:
            application/json:
              schema:
                type: object
                required: [talons]
                properties:
                  talons:
                    type: array
                    items: { $ref: '#/components/schemas/Talon' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createTalon
      tags: [Talons]
      summary: create a talon
      description: |
        Creates a talon at `sites/{siteId}/talons/{talonId}` and, when the
        talon has a `webhook` output, mints its signing secret in the same
        commit. The secret is never returned.

        Additional preconditions, each reported as a problem `code`:

        - `command` outputs may only be authored by a site admin
          (`403 command_output_forbidden`).
        - `webhook` output urls are re-checked against private, loopback, and
          link-local ranges after DNS resolution (`400 invalid_webhook_url`).
        - a `visual_check` condition or a `cortex` output needs a site-level
          LLM key, since both call the model with no user in the loop
          (`400 site_llm_key_required`).
        - a site already holding 20 talons answers `409 talon_limit_reached`.

        Validation failures (`400 invalid_talon`) carry an RFC 7807 `errors`
        map plus a `fieldErrors` array of `{ field, code, message }` for
        inline rendering.

        Requires `TALON_MANAGE`; API keys need `site=<siteId>:write`.
        Accepts `Idempotency-Key`.
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TalonInput' }
      responses:
        '201':
          description: Talon created.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Talon' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/talons/authored:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - name: uid
        in: query
        required: true
        description: The author whose talons to list.
        schema: { type: string, pattern: '^[A-Za-z0-9_-]{1,128}$' }
    get:
      operationId: listAuthoredTalons
      tags: [Talons]
      summary: talons one person authored on this site
      description: |
        The departure preview. Removing a member touches nothing under
        `sites/{siteId}`, so their talons survive the removal — but a talon
        with a `cortex` output re-resolves its AUTHOR's site access on every
        run, so it starts failing the moment they lose access. Call this before
        removing a member to learn what that removal will break.

        Names only — this is not a second way to read a talon. Bounded by the
        20-talon per-site cap.

        Requires `TALON_MANAGE`; API keys need `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: What this person authored here.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [siteId, uid, count, talons]
                properties:
                  siteId: { type: string }
                  uid: { type: string }
                  count: { type: integer }
                  talons:
                    type: array
                    items:
                      type: object
                      additionalProperties: false
                      required: [id, name, enabled]
                      properties:
                        id: { type: string }
                        name: { type: string }
                        enabled: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/talons/reassign:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    post:
      operationId: reassignTalons
      tags: [Talons]
      summary: hand talon authorship to a successor
      description: |
        Rewrites `createdBy` on one or more talons. Use it before the person
        who wrote them loses access — an unattended run resolves the author's
        site access, not the operator's, so an orphaned author is an
        automation that stops.

        Selection is exactly one of:

        - `fromUid` — every talon on the site currently authored by that uid
          (the departure case);
        - `talonIds` — an explicit list.

        The successor is held to the same bar the store holds every author to:
        an active (non-soft-deleted) account with `TALON_MANAGE` on this site,
        and `MACHINE_EXEC_COMMAND` as well when any selected talon carries a
        `command` output or a `cortex` output with `allowActions`. A refusal is
        `400 successor_invalid`.

        All-or-nothing: nothing is written unless every selected talon exists
        and the successor clears both gates — a partial move would leave some
        automations orphaned with no signal which. Talons already authored by
        the successor come back in `skippedTalonIds` without a write.

        Requires `TALON_MANAGE`; API keys need `site=<siteId>:write`.
        Accepts `Idempotency-Key`.
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [toUid]
              properties:
                toUid:
                  type: string
                  description: The successor who inherits authorship.
                  pattern: '^[A-Za-z0-9_-]{1,128}$'
                fromUid:
                  type: string
                  description: Move every talon this uid authored. Mutually exclusive with `talonIds`.
                  pattern: '^[A-Za-z0-9_-]{1,128}$'
                talonIds:
                  type: array
                  description: Move exactly these talons. Mutually exclusive with `fromUid`.
                  items: { type: string }
                  maxItems: 20
      responses:
        '200':
          description: What moved.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [siteId, toUid, reassignedTalonIds, skippedTalonIds, reassignedCount]
                properties:
                  siteId: { type: string }
                  toUid: { type: string }
                  reassignedTalonIds:
                    type: array
                    items: { type: string }
                  skippedTalonIds:
                    type: array
                    description: Already authored by the successor — untouched.
                    items: { type: string }
                  reassignedCount: { type: integer }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/talons/{talonId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/TalonIdPath'
    get:
      operationId: getTalon
      tags: [Talons]
      summary: fetch a talon
      description: |
        Requires `TALON_MANAGE`; API keys need `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: The talon.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Talon' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateTalon
      tags: [Talons]
      summary: toggle or replace a talon
      description: |
        Two shapes, chosen by the body:

        - `{ "enabled": <bool> }` and nothing else flips the talon on or off.
          Enabling re-arms `nextRunAt` from now, so a talon that sat disabled
          for a month does not fire the instant it is switched back on.
        - anything else replaces the caller-owned fields wholesale and is
          revalidated exactly like a create — including the `command`-output
          privilege gate, the webhook-url SSRF check, and the author-LLM-key
          precondition. Omitted optional fields return to their defaults.

        Run bookkeeping (`lastRunAt`, `lastRunStatus`, `consecutiveFailures`)
        is never touched by either shape. `disabledReason` is cleared by the
        enable/disable shape — a person moving the switch settles the question
        of why it moved.

        Requires `TALON_MANAGE`; API keys need `site=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/TalonEnabledPatch'
                - $ref: '#/components/schemas/TalonInput'
      responses:
        '200':
          description: The updated talon.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Talon' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteTalon
      tags: [Talons]
      summary: delete a talon
      description: |
        Deletes the talon and its webhook signing secret in one commit. Run
        history in `talon_runs` is kept on purpose — it is an audit surface
        and outlives the talon it describes, so
        `GET /api/sites/{siteId}/talons/{talonId}/runs` keeps answering after
        a delete. A repeat delete answers `404 talon_not_found`.

        Requires `TALON_MANAGE`; API keys need `site=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '204':
          description: Talon deleted.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/talons/{talonId}/runs:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/TalonIdPath'
      - $ref: '#/components/parameters/PageSizeQuery'
      - $ref: '#/components/parameters/PageTokenQuery'
    get:
      operationId: listTalonRuns
      tags: [Talons]
      summary: list a talon's run history
      description: |
        Cursor-paginated execution records for one talon, most recent first.
        The talon does not have to still exist: run records outlive the talon
        they describe, so a deleted talon returns its history rather than 404.

        Requires `TALON_MANAGE`; API keys need `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: A page of run records.
          content:
            application/json:
              schema:
                type: object
                required: [runs, next_page_token]
                properties:
                  runs:
                    type: array
                    items: { $ref: '#/components/schemas/TalonRun' }
                  next_page_token:
                    type: string
                    description: Empty string when there are no more pages.
                  nextPageToken:
                    type: string
                    description: Compatibility alias for `next_page_token`.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/talons/{talonId}/test:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/TalonIdPath'
    post:
      operationId: runTalon
      tags: [Talons]
      summary: run a talon on demand
      description: |
        Runs the talon immediately, exactly as a trigger fire would — condition
        gate first, then the outputs — and answers with one summary per machine
        the talon's scope resolved to. The full records are readable from
        `GET /api/sites/{siteId}/talons/{talonId}/runs`.

        Two things differ from a triggered run: the cooldown is bypassed, and
        every recorded run carries `manual: true`. Overlap is still prevented —
        when another execution already holds the talon's slot, the response
        carries a `skipped` summary instead of starting a second live run.

        Not idempotent: calling twice runs the talon twice. Unlike creation it
        is not pro-gated, so a downgraded site can still exercise the talons it
        already has, exactly as the scheduler does.

        Requires `TALON_MANAGE`; API keys need `site=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: The runs this fire recorded.
          content:
            application/json:
              schema:
                type: object
                required: [runs]
                properties:
                  runs:
                    type: array
                    items: { $ref: '#/components/schemas/TalonRunSummary' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/screenshots:
    parameters:
      - name: siteId
        in: path
        required: true
        schema: { type: string }
      - name: machineId
        in: path
        required: true
        schema: { type: string }
      - name: screenshotId
        in: query
        required: false
        schema: { type: string }
    delete:
      operationId: deleteSiteMachineScreenshots
      tags: [Screenshots]
      summary: Delete machine screenshots
      description: |
        Deletes one screenshot by id, or all screenshot history for the machine
        when omitted. Requires `machine=<machineId>:write`. Response shape is
        transitional JSON (`{ success, deleted }`) for dashboard compatibility.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Screenshots deleted.
          content:
            application/json:
              schema:
                type: object
                required: [success, deleted]
                properties:
                  success: { type: boolean }
                  deleted: { type: integer, minimum: 0 }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # Sites
  # ──────────────────────────────────────────────────────────────────────
  # ──────────────────────────────────────────────────────────────────────
  # API Keys
  # ──────────────────────────────────────────────────────────────────────
  /api/sites/{siteId}/logs:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: listSiteLogs
      tags: [Logs]
      summary: List operational site logs
      description: |
        Lists entries from `sites/{siteId}/logs`, newest first. This is
        the operational log surface used for agent/process/deployment
        events, distinct from the tamper-evident audit log.
        Use `page_size` and `page_token`; legacy `limit` and `cursor`
        are accepted for existing callers.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: page_size
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: page_token
          in: query
          schema: { type: string }
          description: Previous response `next_page_token`.
        - name: action
          in: query
          schema: { type: string }
          description: Exact action filter, e.g. `process_crash`.
        - name: machineId
          in: query
          schema: { type: string }
          description: Exact machine id filter.
        - name: level
          in: query
          schema:
            type: string
            enum: [debug, info, warning, error, critical]
        - name: since
          in: query
          schema: { type: string }
          description: ISO 8601 or unix-ms lower timestamp bound.
        - name: until
          in: query
          schema: { type: string }
          description: ISO 8601 or unix-ms upper timestamp bound.
      responses:
        '200':
          description: Paginated operational logs
          content:
            application/json:
              schema:
                type: object
                required: [siteId, logs, next_page_token, nextPageToken]
                properties:
                  siteId: { type: string }
                  logs:
                    type: array
                    items: { $ref: '#/components/schemas/SiteLogEntry' }
                  next_page_token: { type: string }
                  nextPageToken:
                    type: string
                    deprecated: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: clearSiteLogs
      tags: [Logs]
      summary: Clear site logs
      description: |
        Deletes log entries from `sites/{siteId}/logs`. Optional filters
        match the current logs view: action, machineId, level, and a
        `since`/`until` timestamp window. Whole-site clearing (no filters)
        requires explicit `all: true`. Note: full-text search is client-side
        and does NOT scope deletion. API-key callers must hold
        `site=<siteId>:admin`; session callers need the site-log management
        capability.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                all:
                  type: boolean
                  description: Required and true when clearing all logs without filters.
                action:
                  type: string
                machineId:
                  type: string
                level:
                  type: string
                  enum: [debug, info, warning, error, critical]
                since:
                  type: string
                  description: ISO 8601 or unix-ms inclusive lower timestamp bound. Scopes deletion to a time window.
                until:
                  type: string
                  description: ISO 8601 or unix-ms inclusive upper timestamp bound.
      responses:
        '200':
          description: Logs cleared
          content:
            application/json:
              schema:
                type: object
                properties:
                  siteId:
                    type: string
                  deletedCount:
                    type: integer
                  filters:
                    type: object
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/logs/{logId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - name: logId
        in: path
        required: true
        schema:
          type: string
          pattern: '^[A-Za-z0-9_.:-]{1,256}$'
    get:
      operationId: getSiteLog
      tags: [Logs]
      summary: Get one operational site log
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Operational log entry
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SiteLogEntry' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/installer:
    get:
      operationId: listInstallers
      tags: [Installer]
      summary: List installer versions (public, superadmin)
      description: |
        Returns paginated installer versions, newest-first. Superadmin-only.
        API-key callers must hold `installer=*:read`. Use `page_size`
        and `page_token`; legacy `limit` and `cursor` are accepted for
        existing clients.
        Responses include canonical `next_page_token`; `nextPageToken` is
        retained as a deprecated compatibility alias.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: page_token
          in: query
          schema:
            type: string
          description: Cursor for the next page (opaque, returned by a previous response).
        - name: includeDeleted
          in: query
          schema:
            type: boolean
            default: false
          description: When true, soft-deleted versions are included.
      responses:
        '200':
          description: Paginated installer versions
          content:
            application/json:
              schema:
                type: object
                required: [versions, next_page_token]
                properties:
                  versions:
                    type: array
                    items:
                      $ref: '#/components/schemas/InstallerVersion'
                  next_page_token:
                    type: string
                  nextPageToken:
                    type: string
                    deprecated: true
        '403':
          description: Forbidden — caller is not a superadmin or the API key lacks `installer=*:read`.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'

  /api/installer/latest:
    get:
      operationId: getLatestInstaller
      tags: [Installer]
      summary: Get latest installer metadata (public, superadmin)
      description: |
        Returns the active latest installer metadata used by the admin
        dashboard and CLI. Superadmin-only; API-key callers must hold
        `installer=*:read`. The unauthenticated download permalink remains
        `GET /download`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Latest active installer
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InstallerVersion'
        '403':
          description: Forbidden; caller is not a superadmin or the API key lacks `installer=*:read`.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '404':
          description: No active latest installer is available
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'

  /api/installer/upload:
    post:
      operationId: requestInstallerUpload
      tags: [Installer]
      summary: Request signed upload URL (step 1)
      description: |
        Step 1 of the two-step installer upload. Returns a signed PUT URL the
        client uses to upload the binary directly to Storage, plus an
        `uploadId` to pass to step 2 (`PUT /api/installer/upload`).
        Superadmin-only; API-key callers need `installer=*:write`.
        Honors `Idempotency-Key` for safe retry.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [version, fileName]
              properties:
                version:
                  type: string
                  description: Semver string (e.g. "3.0.0").
                fileName:
                  type: string
                  description: Must end in `.exe`.
                contentType:
                  type: string
                  default: application/octet-stream
                releaseNotes:
                  type: string
                  nullable: true
                setAsLatest:
                  type: boolean
                  default: true
      responses:
        '200':
          description: Signed upload URL + opaque uploadId
          content:
            application/json:
              schema:
                type: object
                properties:
                  uploadUrl:
                    type: string
                    format: uri
                  uploadId:
                    type: string
                  storagePath:
                    type: string
                  expiresAt:
                    type: string
                    format: date-time
        '400':
          description: Validation error
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '403':
          description: Forbidden — superadmin required
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '422':
          description: Idempotency-Key reused with a different request body
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
    put:
      operationId: finalizeInstallerUpload
      tags: [Installer]
      summary: Finalize installer upload (step 2)
      description: |
        Step 2 of the two-step installer upload. Verifies the binary is in
        Storage, computes SHA-256 from the stored object, rejects a supplied
        `checksum_sha256` mismatch, and writes version metadata plus the
        `latest` pointer when `setAsLatest` was true at step 1. Superadmin-only;
        API-key callers need `installer=*:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [uploadId]
              properties:
                uploadId:
                  type: string
                checksum_sha256:
                  type: string
                  description: Optional — server computes if omitted.
      responses:
        '200':
          description: Upload finalized
          content:
            application/json:
              schema:
                type: object
                properties:
                  version:
                    type: string
                  download_url:
                    type: string
                    format: uri
                  checksum_sha256:
                    type: string
                  file_size:
                    type: integer
        '400':
          description: Validation error or missing Idempotency-Key
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '404':
          description: Upload record or file not found
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '409':
          description: Upload already finalized
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '410':
          description: Upload window expired — restart the flow
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'

        '412':
          description: Uploaded object checksum does not match `checksum_sha256`
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '422':
          description: Idempotency-Key reused with a different request body
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'

  /api/installer/{version}:
    delete:
      operationId: deleteInstallerVersion
      tags: [Installer]
      summary: Soft-delete an installer version
      description: |
        Soft-deletes the version (sets `deletedAt`); the storage object is
        retained. Refuses to drop the active count below 2 — returns 409
        `min_versions_violated` if it would. Idempotent: deleting an
        already-soft-deleted version returns 200 with `alreadyDeleted: true`.
        Superadmin-only; API-key callers need `installer=*:admin`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: version
          in: path
          required: true
          schema:
            type: string
            pattern: '^\d+\.\d+\.\d+$'
      responses:
        '200':
          description: Version deleted (or already deleted)
          content:
            application/json:
              schema:
                type: object
                properties:
                  version:
                    type: string
                  deletedAt:
                    type: integer
                  alreadyDeleted:
                    type: boolean
        '404':
          description: Version not found
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '409':
          description: Current latest version is protected or min active version count would be violated
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'

  /api/installer/{version}/set-latest:
    post:
      operationId: setInstallerLatest
      tags: [Installer]
      summary: Promote a version to latest
      description: |
        Atomically updates the `installer_metadata/latest` pointer to
        the given version. Refuses if the version is missing or
        soft-deleted. Superadmin-only; API-key callers need
        `installer=*:admin`. Honors `Idempotency-Key`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: version
          in: path
          required: true
          schema:
            type: string
            pattern: '^\d+\.\d+\.\d+$'
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Latest pointer updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  version:
                    type: string
                  latest:
                    $ref: '#/components/schemas/InstallerVersion'
        '400':
          description: Validation error or missing Idempotency-Key
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '404':
          description: Version not found
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '409':
          description: Version is soft-deleted — restore before promoting
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'

  # ──────────────────────────────────────────────────────────────────────
        '422':
          description: Idempotency-Key reused with a different request body
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'

  # ──────────────────────────────────────────────────────────────────────
  # hoot — AI chat conversations (canonical) + the deprecated /api/cortex aliases
  # ──────────────────────────────────────────────────────────────────────
  /api/hoot/conversations:
    get:
      operationId: listHootConversations
      tags: [Hoot]
      summary: List Hoot conversations
      description: |
        List Hoot conversations the caller can see. API-key callers are
        filtered to sites where the key holds `chat=<siteId>:read` or
        `chat=*:read`. Pass `siteId` to narrow the page to one site.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: siteId
          in: query
          required: false
          schema: { type: string, pattern: '^[A-Za-z0-9_-]{1,128}$' }
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
        - name: includeDeleted
          in: query
          required: false
          schema: { type: boolean, default: false }
        - name: owner
          in: query
          required: false
          description: Set to `me` to filter to conversations owned by the caller.
          schema: { type: string, enum: [me] }
      responses:
        '200':
          description: Conversation page
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    properties:
                      conversations:
                        type: array
                        items:
                          $ref: '#/components/schemas/HootConversationSummary'
                      next_page_token: { type: string }
                      nextPageToken:
                        type: string
                        deprecated: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
    post:
      operationId: createHootConversation
      tags: [Hoot]
      summary: Create a Hoot conversation
      description: |
        Create a new Hoot conversation. Provide `machineId` to pin the
        conversation to one machine, or omit it for site-wide mode. Requires
        `chat=<siteId>:write` and `Idempotency-Key`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId]
              properties:
                siteId: { type: string }
                machineId: { type: string }
                title: { type: string, maxLength: 100 }
                initial_message:
                  type: object
                  required: [role, content]
                  properties:
                    role: { type: string, enum: [user] }
                    content: { type: string }
      responses:
        '201':
          description: Conversation created
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  data:
                    $ref: '#/components/schemas/HootConversation'
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }

  /api/cortex/conversations:
    get:
      deprecated: true
      operationId: listCortexConversations
      tags: [Hoot]
      summary: "[deprecated] List Hoot conversations — use /api/hoot/conversations"
      description: |
        List Hoot conversations the caller can see. API-key callers are
        filtered to sites where the key holds `chat=<siteId>:read` or
        `chat=*:read`. Pass `siteId` to narrow the page to one site.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: siteId
          in: query
          required: false
          schema: { type: string, pattern: '^[A-Za-z0-9_-]{1,128}$' }
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
        - name: includeDeleted
          in: query
          required: false
          schema: { type: boolean, default: false }
        - name: owner
          in: query
          required: false
          description: Set to `me` to filter to conversations owned by the caller.
          schema: { type: string, enum: [me] }
      responses:
        '200':
          description: Conversation page
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    properties:
                      conversations:
                        type: array
                        items:
                          $ref: '#/components/schemas/CortexConversationSummary'
                      next_page_token: { type: string }
                      nextPageToken:
                        type: string
                        deprecated: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
    post:
      deprecated: true
      operationId: createCortexConversation
      tags: [Hoot]
      summary: "[deprecated] Create a Hoot conversation — use /api/hoot/conversations"
      description: |
        Create a new Hoot conversation. Provide `machineId` to pin the
        conversation to one machine, or omit it for site-wide mode. Requires
        `chat=<siteId>:write` and `Idempotency-Key`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId]
              properties:
                siteId: { type: string }
                machineId: { type: string }
                title: { type: string, maxLength: 100 }
                initial_message:
                  type: object
                  required: [role, content]
                  properties:
                    role: { type: string, enum: [user] }
                    content: { type: string }
      responses:
        '201':
          description: Conversation created
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  data:
                    $ref: '#/components/schemas/CortexConversation'
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }

  /api/hoot/conversations/{conversationId}:
    parameters:
      - name: conversationId
        in: path
        required: true
        schema: { type: string }
    post:
      operationId: sendHootConversationMessage
      tags: [Hoot]
      summary: Send a Hoot message and stream the reply
      description: |
        Append one user message and stream the assistant reply. The response is
        `text/plain; charset=utf-8` with `X-Vercel-AI-Data-Stream: v1` and
        AI-SDK line-prefixed frames (`0:"delta"`, `d:{...}`, `3:"err"`).

        Public send accepts only `role` and `content`; create a new
        conversation to change the target machine. API-key callers are capped
        to read-only Hoot tools during streamed replies.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role, content]
              additionalProperties: false
              properties:
                role: { type: string, enum: [user] }
                content: { type: string }
      responses:
        '200':
          description: AI-SDK text stream
          content:
            text/plain:
              schema: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '423':
          description: Hoot disabled on target machine
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '429': { $ref: '#/components/responses/Problem429' }
        '503':
          description: Hoot or target machine unavailable
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
    patch:
      operationId: renameHootConversation
      tags: [Hoot]
      summary: Rename a Hoot conversation
      description: |
        Update the conversation title. Other fields are rejected with
        `forbidden_field`. Requires `chat=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              additionalProperties: false
              properties:
                title: { type: string, maxLength: 100 }
      responses:
        '200':
          description: Renamed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    properties:
                      conversationId: { type: string }
                      title: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
    delete:
      operationId: deleteHootConversation
      tags: [Hoot]
      summary: Soft-delete a Hoot conversation
      description: |
        Soft-delete a conversation. A repeated delete returns 200 with
        `alreadyDeleted: true`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Soft-deleted or already deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    properties:
                      conversationId: { type: string }
                      alreadyDeleted: { type: boolean }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }

  /api/cortex/conversations/{conversationId}:
    parameters:
      - name: conversationId
        in: path
        required: true
        schema: { type: string }
    post:
      deprecated: true
      operationId: sendCortexConversationMessage
      tags: [Hoot]
      summary: "[deprecated] Send a Hoot message and stream the reply — use /api/hoot/conversations/{conversationId}"
      description: |
        Append one user message and stream the assistant reply. The response is
        `text/plain; charset=utf-8` with `X-Vercel-AI-Data-Stream: v1` and
        AI-SDK line-prefixed frames (`0:"delta"`, `d:{...}`, `3:"err"`).

        Public send accepts only `role` and `content`; create a new
        conversation to change the target machine. API-key callers are capped
        to read-only Hoot tools during streamed replies.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role, content]
              additionalProperties: false
              properties:
                role: { type: string, enum: [user] }
                content: { type: string }
      responses:
        '200':
          description: AI-SDK text stream
          content:
            text/plain:
              schema: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '423':
          description: Hoot disabled on target machine
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
        '429': { $ref: '#/components/responses/Problem429' }
        '503':
          description: Hoot or target machine unavailable
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetails'
    patch:
      deprecated: true
      operationId: renameCortexConversation
      tags: [Hoot]
      summary: "[deprecated] Rename a Hoot conversation — use /api/hoot/conversations/{conversationId}"
      description: |
        Update the conversation title. Other fields are rejected with
        `forbidden_field`. Requires `chat=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              additionalProperties: false
              properties:
                title: { type: string, maxLength: 100 }
      responses:
        '200':
          description: Renamed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    properties:
                      conversationId: { type: string }
                      title: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
    delete:
      deprecated: true
      operationId: deleteCortexConversation
      tags: [Hoot]
      summary: "[deprecated] Soft-delete a Hoot conversation — use /api/hoot/conversations/{conversationId}"
      description: |
        Soft-delete a conversation. A repeated delete returns 200 with
        `alreadyDeleted: true`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Soft-deleted or already deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    properties:
                      conversationId: { type: string }
                      alreadyDeleted: { type: boolean }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }

  # ──────────────────────────────────────────────────────────────────────
  # Roost (project distribution v2) — content-addressed sync
  # ──────────────────────────────────────────────────────────────────────
  /api/chunks/check:
    post:
      operationId: checkChunks
      tags: [Chunks]
      summary: Check which chunks are already uploaded
      description: |
        Given a list of SHA-256 chunk hashes, returns the subset that are
        NOT yet present in content-addressed storage. Callers skip chunks
        that already exist (CAS dedup), uploading only the missing ones.

        Idempotent. Scoped to the caller's site (per-tenant chunk namespace).
      security:
        - apiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId, hashes]
              properties:
                siteId:
                  type: string
                hashes:
                  type: array
                  description: Lowercase 64-char SHA-256 hex strings.
                  items:
                    type: string
                    pattern: '^[0-9a-f]{64}$'
                  maxItems: 1000
            example:
              siteId: nyc-office
              hashes:
                - 239f59ed55e737c77147cf55ad0c1b030b6d7ee748a7426952f9b852d5a935e5
                - a1b2c3d4e5f6789012345678901234567890abcdef0123456789abcdef012345
      responses:
        '200':
          description: Subset of input hashes not present in storage.
          content:
            application/json:
              schema:
                type: object
                properties:
                  missing:
                    type: array
                    items:
                      type: string
              example:
                missing:
                  - a1b2c3d4e5f6789012345678901234567890abcdef0123456789abcdef012345
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '501':
          $ref: '#/components/responses/Problem501'

  /api/chunks/upload-urls:
    post:
      operationId: getChunkUploadUrls
      tags: [Chunks]
      summary: Get signed upload URLs for chunks
      description: |
        Returns signed PUT URLs (≤60 min TTL) for uploading each chunk
        directly to R2. Honors `Idempotency-Key` header for safe retries.
        Rate-limited per site.
      security:
        - apiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId, hashes]
              properties:
                siteId:
                  type: string
                hashes:
                  type: array
                  items:
                    type: string
                    pattern: '^[0-9a-f]{64}$'
                  maxItems: 1000
      responses:
        '200':
          description: Map of hash → signed upload URL.
          content:
            application/json:
              schema:
                type: object
                properties:
                  urls:
                    type: object
                    additionalProperties:
                      type: string
                      format: uri
                      description: Signed PUT URL; PUT the chunk bytes as the body.
                  expiresAt:
                    type: string
                    format: date-time
                    description: Earliest expiry across the issued URLs.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '402': { $ref: '#/components/responses/Problem402' }
        '429': { $ref: '#/components/responses/Problem429' }
        '501':
          $ref: '#/components/responses/Problem501'

  /api/chunks/download-urls:
    get:
      operationId: getChunkDownloadUrls
      tags: [Chunks]
      summary: Get signed download URLs for chunks
      description: |
        Returns signed GET URLs (≤15 min TTL) for downloading a batch of
        chunks. Scoped to per-tenant prefix; cross-site access returns 403.
      security:
        - apiKey: []
        - firebaseIdToken: []
      parameters:
        - name: siteId
          in: query
          required: true
          schema:
            type: string
        - name: hashes
          in: query
          required: true
          description: Comma-separated list of SHA-256 hashes (lowercase hex).
          schema:
            type: string
      responses:
        '200':
          description: Map of hash → signed download URL.
          content:
            application/json:
              schema:
                type: object
                properties:
                  urls:
                    type: object
                    additionalProperties:
                      type: string
                      format: uri
                  expiresAt:
                    type: string
                    format: date-time
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '501':
          $ref: '#/components/responses/Problem501'

  /api/roosts/{roostId}/versions:
    get:
      operationId: listRoostVersions
      tags: [Versions]
      summary: List versions for rollback UI
      description: |
        Returns the immutable version history for a roost, newest
        first. Used by the rollback UI to let operators pick a previous
        known-good version.
      security:
        - apiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostIdPath'
        - name: siteId
          in: query
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
      responses:
        '200':
          description: Paginated version history.
          content:
            application/json:
              schema:
                type: object
                required: [versions, next_page_token]
                properties:
                  versions:
                    type: array
                    items:
                      $ref: '#/components/schemas/VersionSummary'
                  next_page_token:
                    type: string
                  nextPageToken:
                    type: string
                    deprecated: true
                  nextCursor:
                    type: string
                    nullable: true
                    deprecated: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '501':
          $ref: '#/components/responses/Problem501'
    post:
      operationId: finalizeVersion
      tags: [Versions]
      summary: Finalize a new version (atomic CAS)
      description: |
        Validates the version, confirms every referenced chunk exists in
        R2, and atomically swaps `currentVersionId` on the roost doc
        via a Firestore transaction. On success the roost's previous
        `currentVersionId` becomes `previousVersionId` (one-click rollback).

        If a concurrent finalize has changed `currentVersionId` between
        the client's fetch and this write, responds 412 so the client can
        re-read and retry.

        Honors `Idempotency-Key` for safe retries.
      security:
        - apiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostIdPath'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId, version]
              properties:
                siteId:
                  type: string
                version:
                  $ref: '#/components/schemas/OciVersion'
                description:
                  type: string
                  nullable: true
                  maxLength: 500
                  description: |
                    Optional plaintext "what changed?" note stored on the
                    version doc. ≤ 500 chars; longer payloads are rejected
                    with 400. Empty / whitespace-only strings are stored as
                    null.
                expectedCurrentVersionId:
                  type: string
                  nullable: true
                  description: |
                    For optimistic concurrency. If provided and the roost's
                    current pointer has changed, the server returns 412
                    with code `version_stale`.
      responses:
        '201':
          description: Version finalized; distribution fan-out triggered.
          content:
            application/json:
              schema:
                type: object
                required: [versionId, versionNumber]
                properties:
                  versionId:
                    type: string
                    description: Stable content-addressed id (64-char sha-256 hex).
                  versionNumber:
                    type: integer
                    description: Monotonic 1-indexed version number, per-roost.
                  currentVersionId:
                    type: string
                  previousVersionId:
                    type: string
                    nullable: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '412': { $ref: '#/components/responses/Problem412' }
        '501':
          $ref: '#/components/responses/Problem501'

  /api/roosts/{roostId}/version-url:
    post:
      operationId: mintRoostVersionUrl
      tags: [Versions]
      summary: Mint a fresh signed GET URL for a version body
      description: |
        Issues a short-lived (≤15 min) signed R2 URL for fetching a
        version JSON body. Agents call this at sync time — URLs baked
        into the roost doc at publish time would already be expired by
        the time a canary wave ran, so each attempt mints fresh.
        Mirrors `/api/chunks/download-urls` for chunks.

        Accepts either a superadmin session/key OR an agent bearer token
        whose `site_id` claim matches the requested `siteId`.
      security:
        - apiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId, versionId]
              properties:
                siteId:
                  type: string
                versionId:
                  type: string
                  description: 64-char hex sha-256 of the version's canonical JSON body.
      responses:
        '200':
          description: Fresh signed URL.
          content:
            application/json:
              schema:
                type: object
                required: [url, expiresAt]
                properties:
                  url:
                    type: string
                    format: uri
                  expiresAt:
                    type: string
                    format: date-time
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }

  /api/roosts/{roostId}/resync:
    post:
      operationId: resyncRoost
      tags: [Roost]
      summary: Force all current targets to re-pull the current version
      description: |
        Operator-initiated retry after a failed rollout (or to force
        drift reset on targets where a kiosk tech touched files by
        hand). Queues fresh `sync_pull` commands with a timestamp
        nonce in the cmdId (deduped by the agent's `_seen_commands`
        otherwise), clears `target_state` docs so the UI snaps from
        "failed" back to "queued", and drops the stale
        `rollouts/{versionRef}` doc so the fanout state machine
        doesn't treat new reports as belated arrivals for an aborted
        wave.

        Skips canary→fleet staging — an explicit retry doesn't need
        the safety wave. The canary already ran (or failed).
      security:
        - apiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId]
              properties:
                siteId:
                  type: string
      responses:
        '200':
          description: Re-sync commands queued to all current targets.
          content:
            application/json:
              schema:
                type: object
                required: [resynced, targets]
                properties:
                  resynced:
                    type: integer
                    description: Count of targets the sync_pull was queued to.
                  targets:
                    type: array
                    items:
                      type: string
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409':
          description: |
            Conflict — either the roost has no currentVersionId (nothing
            to resync) or no targets assigned.
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/ProblemDetails' }

  # ══════════════════════════════════════════════════════════════════════
  # Developer-preview public API surface. Paths below carry
  # `x-stub: true` where the route file does not yet exist; the openapi
  # drift validator skips the route-file check for those entries.
  # Contract source: dev/active/public-api/reference/api-surface.md.
  # ══════════════════════════════════════════════════════════════════════

  # ──────────────────────────────────────────────────────────────────────
  # 1. api keys (public surface)
  # ──────────────────────────────────────────────────────────────────────
  /api/keys:
    get:
      operationId: listUserApiKeys
      tags: [API Keys]
      summary: list api keys
      description: |
        Lists the caller's own api keys (raw `key` is never included).
        Requires a user session or Firebase ID token; api keys cannot
        list other api keys. This route is currently unpaginated.
        See [authentication.md](/docs/api/authentication).
      security:
        - firebaseIdToken: []
      responses:
        '200':
          description: List of api key summaries.
          headers:
            RateLimit-Limit:    { $ref: '#/components/headers/RateLimitLimit' }
            RateLimit-Remaining: { $ref: '#/components/headers/RateLimitRemaining' }
            RateLimit-Reset:    { $ref: '#/components/headers/RateLimitReset' }
          content:
            application/json:
              schema:
                type: object
                required: [success, keys]
                properties:
                  success: { type: boolean }
                  keys:
                    type: array
                    items: { $ref: '#/components/schemas/ApiKey' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createUserApiKey
      tags: [API Keys]
      summary: create scoped api key
      description: |
        Creates a scoped api key. The raw `key` value is returned
        **exactly once** — store it in a secrets manager immediately.
        Requires a user session or Firebase ID token. Non-superadmin
        callers cannot create `user` or `installer` scopes; those
        platform resources must use `id: "*"`.
        See [authentication.md](/docs/api/authentication#creating-keys).
      security:
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateApiKeyRequest' }
      responses:
        '200':
          description: Api key created; raw `key` present on this response only.
          headers:
            RateLimit-Limit:    { $ref: '#/components/headers/RateLimitLimit' }
            RateLimit-Remaining: { $ref: '#/components/headers/RateLimitRemaining' }
            RateLimit-Reset:    { $ref: '#/components/headers/RateLimitReset' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiKeyCreateResponse' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/keys/{keyId}:
    parameters:
      - $ref: '#/components/parameters/KeyIdPath'
    patch:
      operationId: updateUserApiKey
      tags: [API Keys]
      summary: edit api key scopes
      description: |
        Edits an existing key's `scopes` and/or `name` in place. The raw
        key value is **not** reissued — holders of the credential keep
        using it and pick up the new scopes on their next request.
        `scopes` is a full replacement, not a merge.

        Scopes are validated with the same rules `POST /api/keys` uses, so
        a key can never be edited into a scope its owner could not have
        been issued. `environment` and `ttlDays` are rejected: rotate to
        extend expiry.

        Returns 409 for a revoked key, and for one inside a rotation grace
        window — edit its successor instead.
        See [authentication.md](/docs/api/authentication#editing-key-scopes).
      security:
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PatchApiKeyRequest' }
      responses:
        '200':
          description: Key updated; the summary reflects the new scopes.
          headers:
            RateLimit-Limit:    { $ref: '#/components/headers/RateLimitLimit' }
            RateLimit-Remaining: { $ref: '#/components/headers/RateLimitRemaining' }
            RateLimit-Reset:    { $ref: '#/components/headers/RateLimitReset' }
          content:
            application/json:
              schema:
                type: object
                required: [success, key]
                properties:
                  success: { type: boolean }
                  key: { $ref: '#/components/schemas/ApiKey' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: revokeUserApiKey
      tags: [API Keys]
      summary: revoke api key
      description: |
        Revokes a key. Takes effect immediately — there is no cache in front of
        the credential lookup, so the very next request with this key is
        rejected. The key is not deleted: the record and its lookup are kept and
        stamped `revokedAt`, so later reads still return the key with
        `revoked: true` and a request replayed with it stays attributable.
        Repeating the call is a no-op that keeps the original revocation time.
      security:
        - firebaseIdToken: []
      responses:
        '200':
          description: Key revoked.
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/keys/{keyId}/rotate:
    parameters:
      - $ref: '#/components/parameters/KeyIdPath'
    post:
      operationId: rotateUserApiKey
      tags: [API Keys]
      summary: rotate api key
      description: |
        Mints a new raw key value; the old key remains valid for a
        fixed 24-hour grace window so running jobs can pick up the new
        value without downtime. The new key keeps the old key's scopes
        and environment.
      security:
        - firebaseIdToken: []
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RotateApiKeyRequest' }
      responses:
        '200':
          description: Rotation complete; new raw `key` present on this response only.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiKeyRotateResponse' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 2. roosts (deploy units) — /api/roosts/{roostId}/{versions,rollback}
  # are already documented above. this block adds the collection endpoints
  # plus the roost-scalar GET/PATCH (the existing DELETE is live and moved
  # here so the spec matches the route file).
  # ──────────────────────────────────────────────────────────────────────
  /api/roosts:
    x-stub: true
    get:
      operationId: listRoosts
      tags: [Roost]
      summary: list roosts in a site
      description: |
        Lists roosts the caller has access to within a site.
        Cursor-paginated; `siteId` query is required. Use `page_size`
        and `page_token`; legacy `limit` and `cursor` are accepted for
        existing clients.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostVersionHeader'
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
        - name: includeDeleted
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: Include soft-deleted roosts.
      responses:
        '200':
          description: Paginated roost list.
          headers:
            RateLimit-Limit:    { $ref: '#/components/headers/RateLimitLimit' }
            RateLimit-Remaining: { $ref: '#/components/headers/RateLimitRemaining' }
            RateLimit-Reset:    { $ref: '#/components/headers/RateLimitReset' }
          content:
            application/json:
              schema:
                type: object
                required: [roosts, next_page_token]
                properties:
                  roosts:
                    type: array
                    items: { $ref: '#/components/schemas/RoostSummary' }
                  next_page_token:
                    type: string
                  nextPageToken:
                    type: string
                    deprecated: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createRoost
      tags: [Roost]
      summary: create roost
      description: |
        Creates a new empty roost. Publish a version to populate it.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostVersionHeader'
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateRoostRequest' }
      responses:
        '201':
          description: Roost created.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Roost' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/roosts/{roostId}:
    parameters:
      - $ref: '#/components/parameters/RoostIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: getRoost
      tags: [Roost]
      summary: fetch roost detail
      description: |
        Returns the roost's current state including `currentVersionId`
        and `previousVersionId`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      x-stub: true
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
      responses:
        '200':
          description: Roost detail.
          headers:
            RateLimit-Limit:    { $ref: '#/components/headers/RateLimitLimit' }
            RateLimit-Remaining: { $ref: '#/components/headers/RateLimitRemaining' }
            RateLimit-Reset:    { $ref: '#/components/headers/RateLimitReset' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Roost' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: patchRoost
      tags: [Roost]
      summary: rename roost or change target machines
      description: |
        Updates the roost's name and/or target machine list. Does not
        affect version history.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      x-stub: true
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PatchRoostRequest' }
      responses:
        '200':
          description: Roost updated.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Roost' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '412': { $ref: '#/components/responses/Problem412' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteRoost
      tags: [Roost]
      summary: soft-delete roost
      description: |
        Deletes the roost doc + versions subcollection. Content chunks
        in R2 are reclaimed by the chunk gc mark-and-sweep (not
        immediate). 30-day tombstone; chunks referenced only by this
        roost become eligible for gc at that point.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
      responses:
        '200':
          description: Roost deleted.
          content:
            application/json:
              schema:
                type: object
                required: [deleted, roostId, versionsDeleted]
                properties:
                  deleted:
                    type: boolean
                    example: true
                  roostId:
                    type: string
                  versionsDeleted:
                    type: integer
                    description: Count of version docs purged.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '412': { $ref: '#/components/responses/Problem412' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/roosts/{roostId}/rollback:
    parameters:
      - $ref: '#/components/parameters/RoostIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    post:
      operationId: rollbackRoost
      tags: [Roost]
      summary: flip currentVersionId to a previously-published version
      description: |
        Rolls a roost back by flipping its `currentVersionId` pointer to
        a previously-published version. Does NOT mint a new version —
        rollback is a pointer change, not a publish, and versions are
        immutable once written.

        `targetVersion` accepts every form supported by the version-ref
        resolver:
          - alias: `current` | `previous` | `first`
          - stable id: 64-char sha-256 hex, or legacy `vrs_<hex>`
          - number: `3` / `"3"` / `"#3"` / `"v3"`
        Defaults to `previous` when omitted, matching one-click
        "undo last publish".

        Auth: scope `rollback`. The scope grammar separates rollback
        from write so an operator key can be issued with rollback-only
        powers (no new pushes).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId]
              properties:
                siteId:
                  type: string
                targetVersion:
                  oneOf:
                    - type: string
                    - type: integer
                  nullable: true
                  description: |
                    Version to roll back to. Accepts an alias
                    (`current` | `previous` | `first`), a stable id
                    (64-char sha-256 hex, or legacy `vrs_<hex>`), or a number (`3` / `"v3"` / `"#3"`).
                    Defaults to `previous` when omitted.
      responses:
        '200':
          description: Pointer flipped. Returns the new current/previous ids.
          content:
            application/json:
              schema:
                type: object
                required:
                  - ok
                  - roostId
                  - siteId
                  - currentVersionId
                  - currentVersionNumber
                properties:
                  ok:
                    type: boolean
                  roostId:
                    type: string
                  siteId:
                    type: string
                  currentVersionId:
                    type: string
                  currentVersionNumber:
                    type: integer
                  previousVersionId:
                    type: string
                    nullable: true
        '400':
          description: |
            Validation failed. Codes include `version_ref_malformed`
            (bad `targetVersion` grammar) and `rollback_no_op`
            (target is already the current version).
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/ProblemDetails' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '422':
          description: |
            Idempotency-Key reuse with a different request body.
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/ProblemDetails' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 3. versions — list, publish, detail, files, and diff are live.
  # ──────────────────────────────────────────────────────────────────────
  /api/roosts/{roostId}/versions/{versionRef}:
    parameters:
      - $ref: '#/components/parameters/RoostIdPath'
      - $ref: '#/components/parameters/VersionRefPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: getVersion
      tags: [Versions]
      summary: fetch full version body
      description: |
        Returns the full oci version body.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
      responses:
        '200':
          description: Version detail with full layer list.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/VersionDetail' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateVersionDescription
      tags: [Versions]
      summary: edit a published version's description
      description: |
        Edits the `description` field on an already-published version.
        The version's content (files, chunks) is immutable once
        published — any other body field is rejected with
        `version_content_immutable`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId, description]
              properties:
                siteId:
                  type: string
                description:
                  type: string
                  nullable: true
                  maxLength: 500
                  description: |
                    New description. ≤ 500 chars; longer payloads are
                    rejected with 400. Empty / whitespace-only strings
                    are stored as null.
      responses:
        '200':
          description: Updated version metadata.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/VersionSummary' }
        '400':
          description: |
            Validation failed. Returns 400 with code
            `version_content_immutable` if the body contains any field
            other than `siteId` / `description`, or `version_ref_malformed`
            if the path `versionRef` has an invalid grammar.
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/ProblemDetails' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/roosts/{roostId}/versions/{versionRef}/files:
    parameters:
      - $ref: '#/components/parameters/RoostIdPath'
      - $ref: '#/components/parameters/VersionRefPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: listVersionFiles
      tags: [Versions]
      summary: paginated flat file list for a version
      description: |
        Flat, cursor-paginated list of files in a version. Avoids
        downloading the full oci layer array for large roosts.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
        - name: prefix
          in: query
          required: false
          schema:
            type: string
          description: POSIX path prefix filter (e.g. `assets/`).
      responses:
        '200':
          description: Paginated file list.
          content:
            application/json:
              schema:
                type: object
                required: [items, next_page_token]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/VersionFile' }
                  next_page_token:
                    type: string
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/roosts/{roostId}/versions/{versionRef}/diff:
    parameters:
      - $ref: '#/components/parameters/RoostIdPath'
      - $ref: '#/components/parameters/VersionRefPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: diffVersions
      tags: [Versions]
      summary: file-level diff between two versions
      description: |
        Returns the `added`, `removed`, and `modified` file sets
        between the path's version (`to`) and the `against` query
        parameter (`from`).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
        - name: against
          in: query
          required: true
          schema:
            type: string
            pattern: '^([0-9a-f]{64}|vrs_[0-9a-f]{64}|[1-9][0-9]*|[#vV][1-9][0-9]*|current|previous|first)$'
          description: The "from" versionRef to diff against.
      responses:
        '200':
          description: Diff result.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DiffResult' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 4. chunks — check/upload-urls/download-urls are live above.
  # mount + referrers are live.
  # ──────────────────────────────────────────────────────────────────────
  /api/chunks/{digest}/mount:
    parameters:
      - $ref: '#/components/parameters/DigestPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    post:
      operationId: mountChunk
      tags: [Chunks]
      summary: cross-roost chunk mount (zero-byte dedup)
      description: |
        Copies the chunk's virtual reference from `from` (source roost)
        into `toRoostId` (target roost). No bytes moved — R2 key stays
        the same; only the referrer set is updated. This is the dedup
        moat: cross-roost reuse without re-upload.
        See [chunks.md](/docs/api/chunks#cross-roost-mount).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
          description: Source roost id (caller must hold `read` scope on it).
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/MountChunkRequest' }
      responses:
        '200':
          description: Mount succeeded.
          content:
            application/json:
              schema:
                type: object
                required: [digest, siteId, from, to, mounted, zeroByte]
                properties:
                  digest:
                    type: string
                    pattern: '^[0-9a-f]{64}$'
                  siteId: { type: string }
                  from: { type: string }
                  to:   { type: string }
                  mounted: { type: boolean }
                  zeroByte: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '402': { $ref: '#/components/responses/Problem402' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/chunks/{digest}/referrers:
    parameters:
      - $ref: '#/components/parameters/DigestPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: listChunkReferrers
      tags: [Chunks]
      summary: list versions referencing a chunk
      description: |
        Lists versions currently referencing a chunk. Used by ops
        tooling and the gc mark-and-sweep.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
      responses:
        '200':
          description: Paginated referrers.
          content:
            application/json:
              schema:
                type: object
                required: [items, next_page_token]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/ChunkReferrer' }
                  next_page_token:
                    type: string
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 5. deployments (rollouts) — rollback, deploy, history, and detail are live.
  # ──────────────────────────────────────────────────────────────────────
  /api/roosts/{roostId}/deploy:
    parameters:
      - $ref: '#/components/parameters/RoostIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    post:
      operationId: deployRoost
      tags: [Roost]
      summary: trigger targeted rollout
      description: |
        Triggers fan-out of a specific version to a subset of the
        roost's target machines. Supports scheduling, dry-run, and
        canary-then-fleet strategy. 202 Accepted — see the response
        `rolloutId` and poll `/api/roosts/{roostId}/deployments/{rolloutId}`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DeployRequest' }
      responses:
        '202':
          description: Rollout queued.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeploymentPlan' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '412': { $ref: '#/components/responses/Problem412' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/roosts/{roostId}/deployments:
    parameters:
      - $ref: '#/components/parameters/RoostIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: listRoostDeployments
      tags: [Roost]
      summary: list rollout history for a roost
      description: |
        Cursor-paginated rollout history (deploys + rollbacks),
        newest first.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
        - name: stage
          in: query
          required: false
          schema:
            type: string
            enum: [scheduled, canary, complete, aborted]
          description: Filter by rollout stage.
      responses:
        '200':
          description: Paginated deployments.
          content:
            application/json:
              schema:
                type: object
                required: [items, next_page_token]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/DeploymentStatus' }
                  next_page_token:
                    type: string
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/roosts/{roostId}/deployments/{rolloutId}:
    parameters:
      - $ref: '#/components/parameters/RoostIdPath'
      - $ref: '#/components/parameters/RolloutIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: getRoostDeployment
      tags: [Roost]
      summary: fetch rollout detail (per-machine state)
      description: |
        Per-machine state machine snapshot for one rollout (canary →
        fleet progression).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
      responses:
        '200':
          description: Rollout detail.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeploymentStatus' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 6. sites (public management surface)
  # ──────────────────────────────────────────────────────────────────────
  /api/sites:
    get:
      operationId: listAccessibleSites
      tags: [Sites]
      summary: list sites accessible to the caller
      description: |
        Lists sites visible to the caller. Session and Firebase ID-token
        callers see the account sites they can access. Scoped API keys
        must carry explicit `site` scopes; scoped keys with only
        roost/machine/chat/user/installer scopes return an empty list.
        Legacy unscoped keys retain the account view and receive the
        legacy-scope deprecation header.

        This endpoint is intentionally unpaginated for the MVP.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Accessible sites.
          content:
            application/json:
              schema:
                type: object
                required: [sites]
                properties:
                  sites:
                    type: array
                    items: { $ref: '#/components/schemas/SiteSummary' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createSite
      tags: [Sites]
      summary: create a site
      description: |
        Creates a top-level site owned by the caller. Any authenticated,
        active user may create a site — self-serve signup depends on it — so
        there is no role requirement; API-key callers must hold `site=*:admin`.
        Returns 409 `site_already_exists` for a live id, or `site_id_retired`
        for one belonging to a deleted site (ids are not reusable).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteId, name]
              properties:
                siteId: { type: string }
                name: { type: string, maxLength: 200 }
                timezone: { type: string, nullable: true }
                schedulesFollowSiteTime:
                  type: boolean
                  description: |
                    Evaluate process schedules in the site's timezone instead of
                    each machine's own clock. Omit the field to leave the site
                    undecided — omission is not the same as `false`, which
                    records an explicit decision to keep machine clocks.
      responses:
        '201':
          description: Site created.
          content:
            application/json:
              schema:
                type: object
                required: [siteId, name, timezone, owner, createdAt, schedulesFollowSiteTime]
                properties:
                  siteId: { type: string }
                  name: { type: string }
                  timezone: { type: string }
                  owner: { type: string }
                  createdAt: { type: integer, format: int64 }
                  schedulesFollowSiteTime:
                    type: boolean
                    description: |
                      Effective value. `false` when the request omitted the
                      field, in which case no decision was stored.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: getSite
      tags: [Sites]
      summary: fetch site detail
      description: |
        Returns site metadata. Requires `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Site detail.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SiteDetail' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateSite
      tags: [Sites]
      summary: update site settings
      description: |
        Updates mutable site settings. Requires `site=<siteId>:admin`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string, maxLength: 200 }
                timezone: { type: string }
                timeFormat: { type: string, enum: [12h, 24h] }
                schedulesFollowSiteTime:
                  type: boolean
                  description: |
                    Evaluate process schedules in the site's timezone instead of
                    each machine's own clock. `true` is rejected with 400 unless
                    the site ends the request with a non-empty `timezone` — send
                    both fields in one request to set the timezone and enable
                    site time together. `false` is always accepted.
      responses:
        '200':
          description: Site updated, or no mutable fields were supplied.
          content:
            application/json:
              schema:
                type: object
                required: [siteId, changed, updated]
                properties:
                  siteId: { type: string }
                  changed: { type: boolean }
                  updated:
                    type: object
                    description: |
                      The fields written, echoed back. Values are strings except
                      `schedulesFollowSiteTime`, which is a boolean.
                    additionalProperties:
                      oneOf:
                        - { type: string }
                        - { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteSite
      tags: [Sites]
      summary: delete a site
      description: |
        Deletes the top-level site document. Requires
        `site=<siteId>:admin`, AND the caller must be the site **owner** or a
        superadmin — a site admin who does not own the site is refused. The id is
        tombstoned on delete and cannot be reused. Subcollection cascade cleanup
        is deferred from the MVP surface.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      responses:
        '200':
          description: Site deleted.
          content:
            application/json:
              schema:
                type: object
                required: [siteId, deleted]
                properties:
                  siteId: { type: string }
                  deleted: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 7. machines
  # ──────────────────────────────────────────────────────────────────────
  /api/sites/{siteId}/machines:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: listSiteMachines
      tags: [Machines]
      summary: list machines in a site
      description: |
        Machine inventory with online state and current roost summaries.
        The MVP route is intentionally unpaginated and returns all machines
        visible in the site.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Unpaginated machine list.
          content:
            application/json:
              schema:
                type: object
                required: [machines]
                properties:
                  machines:
                    type: array
                    items: { $ref: '#/components/schemas/MachineSummary' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    get:
      operationId: getSiteMachine
      tags: [Machines]
      summary: fetch machine detail
      description: |
        Full detail for a single machine: inventory fields, heartbeat,
        telemetry metrics, and reported process snapshots.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Machine detail.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MachineDetail' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteSiteMachine
      tags: [Machines]
      summary: remove a machine from a site
      description: |
        Hard-deletes the machine document, its config document, and the
        fixed pending/completed command-map documents. Requires
        `MACHINE_REMOVE`; in the current role matrix this is superadmin-only.
        API-key callers also need `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      responses:
        '200':
          description: Machine removal completed.
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data: { $ref: '#/components/schemas/RemoveMachineResult' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/deployments:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    get:
      operationId: listMachineDeployments
      tags: [Machines]
      summary: per-machine deployment history
      description: |
        Lists the current per-roost deployment state for roosts that target
        the machine. The MVP route is intentionally unpaginated.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Current per-roost deployment state.
          content:
            application/json:
              schema:
                type: object
                required: [siteId, machineId, deployments]
                properties:
                  siteId: { type: string }
                  machineId: { type: string }
                  deployments:
                    type: array
                    items: { $ref: '#/components/schemas/MachineDeployment' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 7b. installer-deploys (api-sprint wave 1 — track 1A)
  #     Public, scoped, api-key-friendly deployment endpoints.
  # ──────────────────────────────────────────────────────────────────────
  /api/sites/{siteId}/deployments:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: listSiteDeployments
      tags: [Deployments]
      summary: list installer deployments for a site
      description: |
        Cursor-paginated list of installer deployments on a site,
        newest first. Requires `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
      responses:
        '200':
          description: Paginated deployments.
          content:
            application/json:
              schema:
                type: object
                required: [items, next_page_token]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/InstallerDeploymentSummary' }
                  next_page_token:
                    type: string
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createSiteDeployment
      tags: [Deployments]
      summary: create an installer deployment
      description: |
        Creates a deployment record + fans out `install_software`
        commands to each target machine. Requires
        `site=<siteId>:write` and an `Idempotency-Key` header for
        replay-safe retries.

        Per-site target quota is read from `sites/{siteId}.deployQuota`
        (default 100). Requests over the quota return 413 with
        `code: over_quota`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, installer_url, installer_name, silent_flags, machines]
              properties:
                name: { type: string }
                installer_url:
                  type: string
                  format: uri
                  description: Must be an `https://` url.
                installer_name: { type: string }
                silent_flags:
                  type: string
                  example: '/SILENT /NORESTART'
                verify_path: { type: string, nullable: true }
                sha256_checksum:
                  type: string
                  pattern: '^[a-f0-9]{64}$'
                  nullable: true
                  description: |
                    SHA-256 hex digest of the installer binary. Strongly
                    recommended — agents 2.11.1+ refuse `install_software`
                    commands that omit it, so deployments created without a
                    checksum will fail on every up-to-date machine.
                parallel_install: { type: boolean }
                close_processes:
                  type: array
                  items: { type: string }
                  description: Process executable names agents should close before install.
                suppress_projects:
                  type: array
                  items: { type: string }
                  description: Project names or paths agents should suppress before install.
                machines:
                  type: array
                  items: { type: string }
                  minItems: 1
      responses:
        '201':
          description: Deployment queued.
          content:
            application/json:
              schema:
                type: object
                required: [deploymentId, siteId, status, targets]
                properties:
                  deploymentId: { type: string }
                  siteId: { type: string }
                  status: { type: string }
                  targets:
                    type: array
                    items: { type: object }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '413': { $ref: '#/components/responses/Problem413' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/deployments/{deploymentId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/DeploymentIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: getSiteDeployment
      tags: [Deployments]
      summary: fetch deployment detail
      description: |
        Returns the deployment doc with per-target status array.
        Requires `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Deployment detail.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InstallerDeploymentDetail' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteSiteDeployment
      tags: [Deployments]
      summary: delete a terminal deployment record
      description: |
        Deletes the deployment history record only. It does not uninstall
        software from target machines. The deployment and every target must
        already be terminal. Requires `site=<siteId>:write` and an
        `Idempotency-Key` header.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: false
        content:
          application/json:
            schema: { type: object }
      responses:
        '200':
          description: Deployment record deleted.
          content:
            application/json:
              schema:
                type: object
                required: [deploymentId, siteId, deleted]
                properties:
                  deploymentId: { type: string }
                  siteId: { type: string }
                  deleted: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/deployments/{deploymentId}/retry:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/DeploymentIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    post:
      operationId: retrySiteDeployment
      tags: [Deployments]
      summary: re-queue install for failed targets only
      description: |
        Targets in `failed` state are reset to `pending` and the
        `install_software` command is re-queued for each. Targets
        in any other state are untouched. Deployment-level status
        flips back to `in_progress`. Requires `site=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: false
        content:
          application/json:
            schema: { type: object }
      responses:
        '200':
          description: Retry queued.
          content:
            application/json:
              schema:
                type: object
                required: [deploymentId, siteId, status, retried, machine_ids]
                properties:
                  deploymentId: { type: string }
                  siteId: { type: string }
                  status: { type: string }
                  retried: { type: integer }
                  machine_ids:
                    type: array
                    items: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/deployments/{deploymentId}/cancel:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/DeploymentIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    post:
      operationId: cancelSiteDeployment
      tags: [Deployments]
      summary: cancel pending targets + purge their queued commands
      description: |
        Marks every target in `pending` / `closing_processes` /
        `downloading` state as `cancelled`, and removes the
        `install_software` command from each affected machine's
        pending command queue. Targets that have started
        `installing` (or are already terminal) are left alone.
        Requires `site=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: false
        content:
          application/json:
            schema: { type: object }
      responses:
        '200':
          description: Cancellation processed.
          content:
            application/json:
              schema:
                type: object
                required: [deploymentId, siteId, status, cancelled, machine_ids]
                properties:
                  deploymentId: { type: string }
                  siteId: { type: string }
                  status: { type: string }
                  cancelled: { type: integer }
                  machine_ids:
                    type: array
                    items: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/deployments/{deploymentId}/uninstall:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/DeploymentIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    post:
      operationId: uninstallSiteDeployment
      tags: [Deployments]
      summary: queue uninstall_software per target
      description: |
        Queues `uninstall_software` on every target machine and flips
        the deployment-level status to `uninstalling`. Per-target
        reconciliation is asynchronous — poll `GET ../{deploymentId}`
        for progress. Requires `site=<siteId>:admin` (privileged
        relative to retry/cancel).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: false
        content:
          application/json:
            schema: { type: object }
      responses:
        '200':
          description: Uninstall queued.
          content:
            application/json:
              schema:
                type: object
                required: [deploymentId, siteId, status, queued, machine_ids]
                properties:
                  deploymentId: { type: string }
                  siteId: { type: string }
                  status: { type: string }
                  queued: { type: integer }
                  machine_ids:
                    type: array
                    items: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 8. quotas
  # ──────────────────────────────────────────────────────────────────────
  # ----------------------------------------------------------------------
  # Site preset management (security boundary migration wave 3.6)
  # ----------------------------------------------------------------------
  /api/sites/{siteId}/presets/schedule:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: listSchedulePresets
      tags: [Presets]
      summary: list schedule preset override docs
      description: |
        Lists schedule presets stored in Firestore at
        `config/{siteId}/schedule_presets`. Built-in defaults are merged
        by dashboard clients, not by this API. Requires the
        `PRESET_MANAGE` capability; API keys need `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Schedule preset docs.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/SchedulePreset' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createSchedulePreset
      tags: [Presets]
      summary: create a custom schedule preset
      description: |
        Creates a custom schedule preset under
        `config/{siteId}/schedule_presets/{presetId}`. The server
        generates `sched-{slug}-{epochMs}` and stamps `createdAt`.
        Requires `PRESET_MANAGE`; API keys need `site=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SchedulePresetInput' }
      responses:
        '201':
          description: Schedule preset created.
          content:
            application/json:
              schema:
                type: object
                required: [presetId, siteId]
                properties:
                  presetId: { type: string }
                  siteId: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/schedule/{presetId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/PresetIdPath'
    get:
      operationId: getSchedulePreset
      tags: [Presets]
      summary: fetch a schedule preset doc
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Schedule preset.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SchedulePreset' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateSchedulePreset
      tags: [Presets]
      summary: update a schedule preset or built-in override
      description: |
        For `builtin-*` ids, the route uses set-with-merge to create or
        update the override doc and forces `isBuiltIn: true`. Custom
        ids require an existing doc. Requires `PRESET_MANAGE`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SchedulePresetPatch' }
      responses:
        '200':
          description: Schedule preset updated.
          content:
            application/json:
              schema:
                type: object
                required: [presetId, siteId, isBuiltInOverride]
                properties:
                  presetId: { type: string }
                  siteId: { type: string }
                  isBuiltInOverride: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteSchedulePreset
      tags: [Presets]
      summary: delete a schedule preset or built-in override
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '204':
          description: Schedule preset deleted.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/reboot:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: listRebootPresets
      tags: [Presets]
      summary: list reboot preset override docs
      description: |
        Lists reboot presets stored at `config/{siteId}/reboot_presets`.
        Built-in defaults are merged by dashboard clients. Requires
        `PRESET_MANAGE`; API keys need `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Reboot preset docs.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/RebootPreset' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createRebootPreset
      tags: [Presets]
      summary: create a custom reboot preset
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RebootPresetInput' }
      responses:
        '201':
          description: Reboot preset created.
          content:
            application/json:
              schema:
                type: object
                required: [presetId, siteId]
                properties:
                  presetId: { type: string }
                  siteId: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/reboot/{presetId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/PresetIdPath'
    get:
      operationId: getRebootPreset
      tags: [Presets]
      summary: fetch a reboot preset doc
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Reboot preset.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RebootPreset' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateRebootPreset
      tags: [Presets]
      summary: update a reboot preset or built-in override
      description: |
        For `builtin-*` ids, the route uses set-with-merge to create or
        update the override doc and forces `isBuiltIn: true`. Custom
        ids require an existing doc. Requires `PRESET_MANAGE`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RebootPresetPatch' }
      responses:
        '200':
          description: Reboot preset updated.
          content:
            application/json:
              schema:
                type: object
                required: [presetId, siteId, isBuiltInOverride]
                properties:
                  presetId: { type: string }
                  siteId: { type: string }
                  isBuiltInOverride: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteRebootPreset
      tags: [Presets]
      summary: delete a reboot preset or built-in override
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '204':
          description: Reboot preset deleted.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/talon:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: listTalonPresets
      tags: [Presets]
      summary: list talon preset (template) docs
      description: |
        Lists reusable talon templates stored at
        `config/{siteId}/talon_presets`. Built-in defaults are merged by
        dashboard clients. Requires `PRESET_MANAGE`; API keys need
        `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Talon preset docs.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/TalonPreset' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createTalonPreset
      tags: [Presets]
      summary: create a custom talon preset
      description: |
        Saves a talon as a reusable template. The `template` body is run
        through the talon validator, so a preset can never store a talon the
        talons API would refuse; failures come back as `template.*` field
        errors. Requires `PRESET_MANAGE` — NOT a pro subscription: curating a
        template reaches no machine. Instantiating one still goes through
        `POST /api/sites/{siteId}/talons`, which is pro-gated.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TalonPresetInput' }
      responses:
        '201':
          description: Talon preset created.
          content:
            application/json:
              schema:
                type: object
                required: [presetId, siteId]
                properties:
                  presetId: { type: string }
                  siteId: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/talon/{presetId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/PresetIdPath'
    get:
      operationId: getTalonPreset
      tags: [Presets]
      summary: fetch a talon preset doc
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Talon preset.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TalonPreset' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateTalonPreset
      tags: [Presets]
      summary: update a talon preset or built-in override
      description: |
        For `builtin-*` ids, the route uses set-with-merge to create or
        update the override doc and forces `isBuiltIn: true`. Custom
        ids require an existing doc. A supplied `template` is re-validated
        in full. Requires `PRESET_MANAGE`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TalonPresetPatch' }
      responses:
        '200':
          description: Talon preset updated.
          content:
            application/json:
              schema:
                type: object
                required: [presetId, siteId, isBuiltInOverride]
                properties:
                  presetId: { type: string }
                  siteId: { type: string }
                  isBuiltInOverride: { type: boolean }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteTalonPreset
      tags: [Presets]
      summary: delete a talon preset or built-in override
      description: |
        Idempotent — a missing preset is still a `204`. Deleting a `builtin-*`
        id removes the site's override so the shipped default reappears.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '204':
          description: Talon preset deleted.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/deployment-template:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: listDeploymentTemplates
      tags: [Presets]
      summary: list deployment templates
      description: |
        Lists installer templates at
        `sites/{siteId}/installer_templates`. Requires
        `PRESET_MANAGE`; API keys need `site=<siteId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Deployment templates.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/DeploymentTemplate' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createDeploymentTemplate
      tags: [Presets]
      summary: create a deployment template
      description: |
        Creates an installer template at
        `sites/{siteId}/installer_templates/{templateId}`. The server
        generates `template-{epochMs}` and stamps `createdAt`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DeploymentTemplateInput' }
      responses:
        '201':
          description: Deployment template created.
          content:
            application/json:
              schema:
                type: object
                required: [templateId, siteId]
                properties:
                  templateId: { type: string }
                  siteId: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/deployment-template/{templateId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/TemplateIdPath'
    get:
      operationId: getDeploymentTemplate
      tags: [Presets]
      summary: fetch a deployment template
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Deployment template.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeploymentTemplate' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateDeploymentTemplate
      tags: [Presets]
      summary: update a deployment template
      description: |
        Merges provided fields into an existing installer template.
        Requires `PRESET_MANAGE`; API keys need `site=<siteId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DeploymentTemplatePatch' }
      responses:
        '200':
          description: Deployment template updated.
          content:
            application/json:
              schema:
                type: object
                required: [templateId, siteId]
                properties:
                  templateId: { type: string }
                  siteId: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteDeploymentTemplate
      tags: [Presets]
      summary: delete a deployment template
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '204':
          description: Deployment template deleted.
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/quota:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: getSiteQuota
      tags: [Quotas]
      summary: current usage vs plan limits
      description: |
        Returns current storage + bandwidth usage against plan limits
        and any fired alarm thresholds. See
        [rate-limits.md](/docs/api/rate-limits).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Quota snapshot.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Quota' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/quota/history:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: getSiteQuotaHistory
      tags: [Quotas]
      summary: daily usage trend for charting
      description: |
        Daily storage + bandwidth usage series. Default 30 days.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: period
          in: query
          required: false
          schema:
            type: string
            enum: ['7d', '14d', '30d', '60d', '90d']
            default: '30d'
          description: Range length.
      responses:
        '200':
          description: Daily usage series.
          content:
            application/json:
              schema:
                type: object
                required: [siteId, period, days, daily]
                properties:
                  siteId: { type: string }
                  period:
                    type: string
                    enum: ['7d', '14d', '30d', '60d', '90d']
                  days:
                    type: integer
                  daily:
                    type: array
                    items: { $ref: '#/components/schemas/QuotaHistoryEntry' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 9. audit log (tamper-evident hash chain)
  # ──────────────────────────────────────────────────────────────────────
  /api/sites/{siteId}/audit-log:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: listAuditRecords
      tags: [Audit Log]
      summary: list audit records for a site
      description: |
        Paginated hash-chained audit log. Requires `site:X:read`
        scope. Record kinds match the audit sink taxonomy:
        `signed_url_issued`, `distribution_started`,
        `version_pointer_changed`, `api_key_used`, `gc_run`,
        `api_key_mutated`, `chunk_mutated`, `deployment_mutated`,
        `distribution_mutated`, `process_mutated`, `roost_mutated`,
        `machine_command_dispatched`, `user_mutated`, `site_mutated`,
        `site_member_mutated`, `installer_mutated`, `webhook_mutated`,
        and `chat_mutated`.
        Use `page_size` and `page_token`; legacy `limit` and `cursor`
        are accepted for existing callers. `actor` matches the full actor
        string, for example `apiKey:<keyId>` or `user:<uid>`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
          description: Maximum records per page.
        - name: page_token
          in: query
          required: false
          schema: { type: string }
          description: Previous `next_page_token` value.
        - name: kind
          in: query
          required: false
          schema: { type: string }
          description: Filter by audit kind.
        - name: actor
          in: query
          required: false
          schema: { type: string }
          description: Exact actor string, e.g. `apiKey:<keyId>` or `user:<uid>`.
        - name: since
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: ISO 8601 or unix-ms lower bound on `recordedAt`.
      responses:
        '200':
          description: Paginated audit records.
          content:
            application/json:
              schema:
                type: object
                required: [siteId, records, next_page_token, nextPageToken]
                properties:
                  siteId: { type: string }
                  records:
                    type: array
                    items: { $ref: '#/components/schemas/AuditRecord' }
                  next_page_token:
                    type: string
                  nextPageToken:
                    type: string
                    deprecated: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/audit-log/{recordHash}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/RecordHashPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: getAuditRecord
      tags: [Audit Log]
      summary: fetch single audit record + verify chain
      description: |
        Returns a single audit record and the hash-chain verification
        result for the record hash plus its immediate predecessor link.
        Non-genesis records report `ok: false` if the predecessor is
        missing or malformed.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Audit record + chain proof.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AuditRecordDetail' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '412': { $ref: '#/components/responses/Problem412' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 10. webhooks (public surface)
  # ──────────────────────────────────────────────────────────────────────
  /api/webhooks:
    get:
      operationId: listWebhookSubscriptions
      tags: [Webhooks]
      summary: list webhook subscriptions
      description: |
        Lists webhook subscriptions on a site.
        Use `page_size` and `page_token`; legacy `limit` and `cursor`
        are accepted for existing clients.
        See [webhooks.md](/docs/api/webhooks).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostVersionHeader'
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
      responses:
        '200':
          description: Paginated webhook list.
          content:
            application/json:
              schema:
                type: object
                required: [webhooks, next_page_token]
                properties:
                  webhooks:
                    type: array
                    items: { $ref: '#/components/schemas/WebhookDetail' }
                  next_page_token:
                    type: string
                  nextPageToken:
                    type: string
                    deprecated: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createWebhookSubscription
      tags: [Webhooks]
      summary: create webhook subscription
      description: |
        Creates a webhook subscription. The `signingSecret` is returned
        **once** on creation.

        Requires `site=<siteId>:write` scope and admin of this site.
        Listing and reading subscriptions is open to any member of the
        site; creating, updating, deleting and rotating are not.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostVersionHeader'
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateWebhookRequest' }
      responses:
        '201':
          description: Webhook created; `signingSecret` present here only.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WebhookWithSecret' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/webhooks/{webhookId}:
    parameters:
      - $ref: '#/components/parameters/WebhookIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
      - $ref: '#/components/parameters/SiteIdQuery'
    get:
      operationId: getWebhookSubscription
      tags: [Webhooks]
      summary: fetch webhook detail
      description: |
        Returns a webhook subscription. The signing secret is never
        included — use `rotate-secret` to mint a new one.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Webhook detail.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WebhookDetail' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: patchWebhookSubscription
      tags: [Webhooks]
      summary: update webhook url / events / paused state
      description: |
        Partial update — any subset of `url`, `events`, `description`,
        `paused` may be specified. Requires `site=<siteId>:write` scope
        and admin of this site.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PatchWebhookRequest' }
      responses:
        '200':
          description: Webhook updated.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WebhookDetail' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '412': { $ref: '#/components/responses/Problem412' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteWebhookSubscription
      tags: [Webhooks]
      summary: delete webhook subscription
      description: |
        Soft-deletes a subscription and pauses future delivery. Delivery
        history remains available for the 30-day retention window.
        Requires `site=<siteId>:write` scope and admin of this site.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Subscription soft-deleted.
          content:
            application/json:
              schema:
                type: object
                required: [id, siteId, softDeleted, tombstoneExpiresAt]
                properties:
                  id: { type: string }
                  siteId: { type: string }
                  softDeleted: { type: boolean }
                  tombstoneExpiresAt:
                    type: string
                    format: date-time
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/webhooks/{webhookId}/rotate-secret:
    parameters:
      - $ref: '#/components/parameters/WebhookIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
      - $ref: '#/components/parameters/SiteIdQuery'
    post:
      operationId: rotateWebhookSecret
      tags: [Webhooks]
      summary: rotate webhook hmac signing secret
      description: |
        Mints a new hmac signing secret. The previous secret is retained
        for a fixed 24-hour verifier grace window. The new secret is
        returned once. Requires `site=<siteId>:write` scope and admin of
        this site.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: New secret issued.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WebhookWithSecret' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/webhooks/{webhookId}/deliveries:
    parameters:
      - $ref: '#/components/parameters/WebhookIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
    get:
      operationId: listWebhookDeliveries
      tags: [Webhooks]
      summary: list webhook delivery history
      description: |
        Paginated delivery history, newest first. Use `page_size` and
        `page_token`; legacy `limit` and `cursor` are accepted for
        existing clients.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/PageSizeQuery'
        - $ref: '#/components/parameters/PageTokenQuery'
      responses:
        '200':
          description: Paginated delivery list.
          content:
            application/json:
              schema:
                type: object
                required: [deliveries, next_page_token]
                properties:
                  deliveries:
                    type: array
                    items: { $ref: '#/components/schemas/WebhookDelivery' }
                  next_page_token:
                    type: string
                  nextPageToken:
                    type: string
                    deprecated: true
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/webhooks/{webhookId}/deliveries/{deliveryId}:
    parameters:
      - $ref: '#/components/parameters/WebhookIdPath'
      - $ref: '#/components/parameters/DeliveryIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
      - $ref: '#/components/parameters/SiteIdQuery'
    get:
      operationId: getWebhookDelivery
      tags: [Webhooks]
      summary: fetch single delivery detail
      description: |
        Full delivery detail — request headers, payload, response
        headers, response body (truncated to 4 KiB).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Delivery detail.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WebhookDelivery' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/webhooks/{webhookId}/deliveries/{deliveryId}/retry:
    parameters:
      - $ref: '#/components/parameters/WebhookIdPath'
      - $ref: '#/components/parameters/DeliveryIdPath'
      - $ref: '#/components/parameters/RoostVersionHeader'
      - $ref: '#/components/parameters/SiteIdQuery'
    post:
      operationId: retryWebhookDelivery
      tags: [Webhooks]
      summary: manually redeliver a past event
      description: |
        Redelivers the same payload with a fresh signature timestamp
        and a new `deliveryId`. Queued — returns 202 Accepted.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '202':
          description: Retry queued.
          content:
            application/json:
              schema:
                type: object
                required: [id, webhookId, siteId, retryOf, state, nextAttemptAt]
                properties:
                  id: { type: string }
                  webhookId: { type: string }
                  siteId: { type: string }
                  retryOf: { type: string }
                  state:
                    type: string
                    enum: [pending]
                  nextAttemptAt:
                    type: string
                    format: date-time
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '412': { $ref: '#/components/responses/Problem412' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/webhooks/probe:
    post:
      operationId: probeWebhookUrl
      tags: [Webhooks]
      summary: synthetic webhook probe
      description: |
        Fires a signed synthetic payload for a chosen webhook event at
        `url` to verify signature verification + reachability. No
        subscription is created.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostVersionHeader'
        - $ref: '#/components/parameters/SiteIdQuery'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/WebhookProbeRequest' }
      responses:
        '200':
          description: Probe completed; inspect `status` for target response status.
          content:
            application/json:
              schema:
                type: object
                required: [status, durationMs, deliveryId, event, requestBody, signature, signingSecret]
                properties:
                  status:
                    type: integer
                    nullable: true
                  durationMs: { type: integer, minimum: 0 }
                  deliveryId: { type: string }
                  event: { type: string }
                  requestBody: { type: string }
                  signature: { type: string }
                  signingSecret: { type: string }
                  responseBody:
                    type: string
                  networkError:
                    type: string
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '412': { $ref: '#/components/responses/Problem412' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # 11. utility — whoami, version, sse event stream
  # ──────────────────────────────────────────────────────────────────────
  /api/whoami:
    x-stub: true
    get:
      operationId: whoami
      tags: [Utility]
      summary: resolve caller identity + scopes + rate-limit + quota
      description: |
        Echoes back the resolved auth context. Useful for sdk/cli
        sanity checks. No body; no query params.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RoostVersionHeader'
      responses:
        '200':
          description: Caller identity snapshot.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WhoAmIResponse' }
        '401': { $ref: '#/components/responses/Problem401' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/version:
    x-stub: true
    get:
      operationId: getApiVersion
      tags: [Utility]
      summary: supported api versions + current default
      description: |
        Publicly readable — no auth required. Lists the current dated
        api version, supported versions, and any in-flight
        deprecations / retirements. Clients use this to pick a
        `Roost-Version` header value.
      security: []
      responses:
        '200':
          description: Version catalog.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/VersionResponse' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/events/stream:
    get:
      operationId: streamEvents
      tags: [Utility]
      summary: server-sent-events scoped event transport
      description: |
        Scoped SSE transport for local-dev tooling. The MVP stream emits
        `connected` and `keepalive` liveness events only; production event
        fanout and resume buffers are Wave 3 work. Auth may be supplied as
        bearer/API-key headers or `api_key` query param for EventSource-style
        clients. 30-min connection lifetime; 15-s keepalive.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/SiteIdQuery'
        - name: events
          in: query
          required: false
          schema: { type: string }
          description: Comma-separated event type filter.
        - name: api_key
          in: query
          required: false
          schema: { type: string }
          description: API key query-param auth for EventSource clients that cannot set headers.
      responses:
        '200':
          description: SSE stream.
          content:
            text/event-stream:
              schema:
                type: string
                description: Server-Sent Events stream (see [webhooks.md](/docs/api/webhooks) for event payloads).
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # api-sprint wave 2 — track 2A (machine-api MVP).
  # appended at end-of-paths to avoid line-conflict with parallel work.
  # ──────────────────────────────────────────────────────────────────────

  /api/sites/{siteId}/machines/{machineId}/commands:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    post:
      operationId: dispatchMachineCommand
      tags: [Machines, Screenshots]
      summary: queue a command on a machine
      description: |
        Canonical site-scoped command endpoint. Restricted to an
        operator-safe allowlist covering machine power, screenshots,
        live view, display topology, process commands, hoot MCP tool
        calls, and Owlette updates. Requires `machine=<machineId>:write`
        for api-key callers and an idempotency key for all callers;
        session/id-token callers fall through to site membership.

        Returns 409 `machine_offline` if `machines/{machineId}.online`
        is `false` — the command is NOT queued.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type]
              properties:
                type:
                  type: string
                  enum: [reboot_machine, shutdown_machine, cancel_reboot, dismiss_reboot_pending, capture_screenshot, start_live_view, stop_live_view, restart_process, start_process, kill_process, set_launch_mode, apply_display_topology, ack_display_topology, enumerate_display_modes, test_display_apply, mcp_tool_call, update_owlette]
                params:
                  type: object
                  description: |
                    Per-type parameters. `reboot_machine` /
                    `shutdown_machine` accept `delay_seconds` (>=0).
                    `capture_screenshot` accepts `monitor` ('all' |
                    'primary' | non-negative integer). Process commands
                    accept `process_name` / `process_id`; `set_launch_mode`
                    accepts `process_name`, `mode`, optional `schedules`, and
                    optional `schedulePresetId`; `apply_display_topology`
                    requires `layout` and `applyId`; `ack_display_topology`
                    requires `applyId`; `enumerate_display_modes` and
                    `test_display_apply` do not require params; `mcp_tool_call`
                    accepts `tool_name`, optional `tool_params`, and optional
                    `chat_id`. Other command types accept their
                    command-specific allowlisted fields.
                  properties:
                    delay_seconds: { type: integer, minimum: 0 }
                    monitor:
                      oneOf:
                        - type: string
                          enum: [all, primary]
                        - type: integer
                          minimum: 0
                    process_name: { type: string }
                    process_id: { type: string }
                    mode: { type: string }
                    schedules:
                      type: array
                      items: { type: object }
                    schedulePresetId: { type: string }
                    layout:
                      type: object
                      additionalProperties: true
                      description: Desired display topology for `apply_display_topology`.
                    applyId: { type: string }
                    tool_name: { type: string }
                    tool_params:
                      type: object
                      additionalProperties: true
                    chat_id: { type: string }
                timeout_seconds:
                  type: integer
                  minimum: 1
                  maximum: 600
                  default: 60
      responses:
        '202':
          description: Command queued.
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [commandId, status]
                    properties:
                      commandId: { type: string }
                      status:
                        type: string
                        enum: [pending]
        '400':
          description: |
            Validation failure. Stable code `unsupported_command_type`
            indicates the requested type is outside the public allowlist.
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/ProblemDetails' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409':
          description: |
            `machine_offline` — machine is not currently connected; queueing is refused.
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/ProblemDetails' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/commands/{commandId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
      - name: commandId
        in: path
        required: true
        schema:
          type: string
          pattern: '^cmd_[A-Za-z0-9_-]{1,80}$'
        description: Command id returned by the dispatch endpoint.
    get:
      operationId: getMachineCommandStatus
      tags: [Machines, Screenshots]
      summary: poll command status
      description: |
        Returns the current status of a queued command. For completed
        `capture_screenshot` commands, the response includes a freshly-
        minted 1-hour signed read URL under `result.screenshot_url` —
        the URL is per-request and is never persisted.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Command status envelope.
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [commandId, status]
                    properties:
                      commandId: { type: string }
                      status:
                        type: string
                        enum: [pending, in_progress, completed, failed]
                      result:
                        type: object
                        description: |
                          Present when status=completed. For
                          capture_screenshot commands, includes
                          `screenshot_url` (1-hour signed) +
                          `expires_at`.
                        properties:
                          screenshot_url: { type: string, format: uri }
                          expires_at: { type: string, format: date-time }
                      error:
                        type: string
                        description: Present when status=failed.
                      createdAt: { type: string, format: date-time, nullable: true }
                      updatedAt: { type: string, format: date-time, nullable: true }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/screenshots/upload-url:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    post:
      operationId: issueScreenshotUploadUrl
      tags: [Machines]
      summary: mint a 5-minute signed write url for screenshot upload
      description: |
        Agent-internal helper invoked during a `capture_screenshot`
        command. Returns a 5-minute v4-signed PUT URL plus the
        canonical storage path (`screenshots/{siteId}/{machineId}/...png`)
        the agent should write to. Decoupling upload from command
        queueing keeps multi-megabyte image payloads off the web tier.

        30-day retention is enforced via a separate Firebase Storage
        lifecycle rule (out of scope for this endpoint).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                contentType:
                  type: string
                  enum: [image/png, image/jpeg]
                  default: image/png
      responses:
        '200':
          description: Signed upload url issued.
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [uploadUrl, storagePath, contentType, expiresAt]
                    properties:
                      uploadUrl: { type: string, format: uri }
                      storagePath: { type: string }
                      contentType: { type: string }
                      expiresAt: { type: string, format: date-time }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # Public API Wave 1.1 coverage additions.

  /api/cli/device-code:
    post:
      operationId: createCliDeviceCode
      tags: [API Keys]
      summary: start CLI device-code login
      description: |
        Starts a short-lived CLI pairing flow. The CLI displays the
        pairing phrase and polls `/api/cli/device-code/poll` with the
        opaque device code until a signed-in user authorizes it.
      security: []
      responses:
        '200':
          description: Device-code login started.
          content:
            application/json:
              schema:
                type: object
                required:
                  - pairPhrase
                  - deviceCode
                  - verificationUri
                  - pairingUrl
                  - expiresIn
                  - interval
                properties:
                  pairPhrase: { type: string }
                  deviceCode: { type: string }
                  verificationUri: { type: string, format: uri }
                  pairingUrl: { type: string, format: uri }
                  expiresIn: { type: integer, example: 600 }
                  interval: { type: integer, example: 5 }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/cli/device-code/authorize:
    post:
      operationId: authorizeCliDeviceCode
      tags: [API Keys]
      summary: authorize CLI device-code login
      description: |
        Browser-side approval step for CLI pairing. Requires a signed-in
        user or Firebase ID token, validates site access for requested
        scopes, then mints an `owk_*` API key for the waiting CLI.
      security:
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, name, scopes]
              properties:
                code: { type: string }
                name: { type: string, minLength: 1, maxLength: 100 }
                ttlDays: { type: integer, minimum: 1, maximum: 365, default: 90 }
                environment:
                  type: string
                  enum: [live, test]
                  default: live
                  deprecated: true
                  description: |
                    Ignored. Every key is minted `live`. Still accepted (and
                    still validated against this enum) so shipped clients that
                    send it are not broken; the value has no effect.
                    Rotation is the one exception — a rotated key inherits the
                    original key's environment.
                scopes:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required: [resource, id, permissions]
                    properties:
                      resource: { type: string }
                      id: { type: string }
                      permissions:
                        type: array
                        minItems: 1
                        items: { type: string }
      responses:
        '200': { description: Device code authorized and key minted. }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403':
          description: Forbidden - superadmin required for `user` or `installer` scopes.
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/ProblemDetails' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '410': { description: Pairing phrase expired. }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/cli/device-code/poll:
    post:
      operationId: pollCliDeviceCode
      tags: [API Keys]
      summary: poll CLI device-code login
      description: |
        CLI polling endpoint for the device-code flow. The device code is
        the polling credential; once authorized, the raw API key is returned
        exactly once and the pairing record is deleted.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [deviceCode]
              properties:
                deviceCode: { type: string }
      responses:
        '200':
          description: Authorized API key returned once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  apiKey: { type: string }
                  keyId: { type: string, nullable: true }
                  name: { type: string, nullable: true }
                  scopes:
                    type: array
                    nullable: true
                    items: { type: object }
                  environment: { type: string, nullable: true }
                  expiresAt:
                    oneOf:
                      - { type: integer }
                      - { type: 'null' }
                  siteId: { type: string, nullable: true }
        '202': { description: Authorization still pending. }
        '400': { $ref: '#/components/responses/Problem400' }
        '404': { $ref: '#/components/responses/Problem404' }
        '410': { description: Device code expired. }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/hoot-enabled:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    patch:
      operationId: setMachineHootEnabled
      tags: [Machines]
      summary: toggle Hoot tool dispatch for a machine
      description: Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [enabled]
              properties:
                enabled: { type: boolean }
      responses:
        '200': { description: Hoot flag updated. }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
  /api/sites/{siteId}/machines/{machineId}/cortex-enabled:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    patch:
      deprecated: true
      operationId: setMachineCortexEnabled
      tags: [Machines]
      summary: "[deprecated] toggle Hoot tool dispatch for a machine — use /api/sites/{siteId}/machines/{machineId}/hoot-enabled"
      description: Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [enabled]
              properties:
                enabled: { type: boolean }
      responses:
        '200': { description: Hoot flag updated. }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
  /api/sites/{siteId}/machines/{machineId}/display-layout:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    put:
      operationId: setMachineDisplayLayout
      tags: [Machines]
      summary: capture or configure a machine display layout
      description: |
        Machine-level display configuration surface. This stores the
        assigned layout, toggles auto-restore, resets its circuit breaker,
        or toggles the remote-apply kill switch. Requires
        `machine=<machineId>:write` and an idempotency key.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required: [op, monitors, capturedBy]
                  properties:
                    op: { type: string, const: capture }
                    monitors:
                      type: array
                      minItems: 1
                      items: { $ref: '#/components/schemas/DisplayMonitor' }
                    capturedBy: { type: string, minLength: 1 }
                - type: object
                  required: [op, enabled]
                  properties:
                    op: { type: string, const: set_auto_restore }
                    enabled: { type: boolean }
                    enabledBy:
                      type: string
                      description: Required when `enabled` is true.
                - type: object
                  required: [op]
                  properties:
                    op: { type: string, const: reset_breaker }
                - type: object
                  required: [op, enabled]
                  properties:
                    op: { type: string, const: set_remote_apply }
                    enabled: { type: boolean }
      responses:
        '200':
          description: Display layout operation completed.
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data: { $ref: '#/components/schemas/DisplayLayoutOperationResult' }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: clearMachineDisplayLayout
      tags: [Machines]
      summary: clear the assigned display layout
      description: |
        Clears `displays.assigned` while preserving sibling display
        settings such as auto-restore and remote-apply. Requires
        `machine=<machineId>:write` and an idempotency key.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      responses:
        '200':
          description: Display layout cleared.
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [machineId]
                    properties:
                      machineId: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/processes/{processId}/launch-mode:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
      - $ref: '#/components/parameters/ProcessIdPath'
    patch:
      operationId: setProcessLaunchMode
      tags: [Processes]
      summary: update process launch mode
      description: Requires `machine=<machineId>:write` and an idempotency key.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mode]
              properties:
                mode: { type: string, enum: [off, always, scheduled] }
                schedules:
                  type: array
                  items: { $ref: '#/components/schemas/ProcessScheduleBlock' }
                schedulePresetId: { type: string, nullable: true }
      responses:
        '200':
          description: Process launch mode updated.
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [processId, mode]
                    properties:
                      processId: { type: string }
                      mode: { type: string, enum: [off, always, scheduled] }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/reboot-schedule:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    put:
      operationId: setMachineRebootSchedule
      tags: [Machines]
      summary: update a machine reboot schedule
      description: Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [schedule]
              properties:
                schedule:
                  type: object
                  required: [enabled]
                  properties:
                    enabled: { type: boolean }
                    entries:
                      type: array
                      items: { type: object }
      responses:
        '200': { description: Reboot schedule updated. }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/uninstall:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    post:
      operationId: triggerMachineUninstall
      tags: [Machines]
      summary: queue software uninstall on a machine
      description: Requires `UNINSTALL_TRIGGER` and site write scope.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [software_name]
              properties:
                software_name: { type: string }
      responses:
        '202': { description: Uninstall command queued. }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: cancelMachineUninstall
      tags: [Machines]
      summary: queue cancellation for an in-flight software uninstall
      description: Requires `UNINSTALL_TRIGGER` and site write scope.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
        - name: software_name
          in: query
          required: false
          schema: { type: string }
          description: Software name to cancel; may also be sent in the JSON body.
      responses:
        '202': { description: Cancel-uninstall command queued. }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/distribution:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
    get:
      operationId: listDistributionPresets
      tags: [Presets]
      summary: list distribution presets
      description: Requires site read scope.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200': { description: Distribution presets. }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createDistributionPreset
      tags: [Presets]
      summary: create a distribution preset
      description: Requires site write scope.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '201': { description: Distribution preset created. }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/presets/distribution/{presetId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/PresetIdPath'
    get:
      operationId: getDistributionPreset
      tags: [Presets]
      summary: get a distribution preset
      description: Requires site read scope.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200': { description: Distribution preset. }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateDistributionPreset
      tags: [Presets]
      summary: update a distribution preset
      description: Requires site write scope.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200': { description: Distribution preset updated. }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteDistributionPreset
      tags: [Presets]
      summary: delete a distribution preset
      description: Requires site write scope.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200': { description: Distribution preset deleted. }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/users/me:
    delete:
      operationId: deleteCurrentUser
      tags: [Users]
      summary: delete the authenticated user's own account
      description: |
        Self-delete cascade for the current user. Requires a session or
        Firebase ID token and rejects API-key authentication.
      security:
        - firebaseIdToken: []
      parameters:
        - name: dryRun
          in: query
          required: false
          schema:
            type: string
            enum: ['1', 'true', 'yes']
      responses:
        '200': { description: Account deletion result. }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

# ════════════════════════════════════════════════════════════════════════
# Components
# ════════════════════════════════════════════════════════════════════════
  /api/sites/{siteId}/machines/{machineId}/processes:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
    get:
      operationId: listMachineProcesses
      tags: [Processes]
      summary: list processes on a machine
      description: |
        List all processes configured on a machine, merged with their live
        runtime status (pid, responsiveness, last update). Requires
        `machine=<machineId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: List of processes
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [processes, nextPageToken]
                    properties:
                      processes:
                        type: array
                        items: { $ref: '#/components/schemas/Process' }
                      nextPageToken:
                        type: string
                        nullable: true
                        description: Always null in the current process-list contract.
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    post:
      operationId: createMachineProcess
      tags: [Processes]
      summary: create a process
      description: |
        Create a new process configuration. `processId` is server-generated.
        Duplicate `name` per machine is rejected with 409 `duplicate_process_name`.
        Requires `machine=<machineId>:write` and `Idempotency-Key`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, exe_path]
              properties:
                name: { type: string }
                exe_path: { type: string }
                file_path: { type: string }
                cwd: { type: string }
                priority: { type: string }
                visibility: { type: string }
                time_delay: { type: string }
                time_to_init: { type: string }
                relaunch_attempts: { type: string }
                launch_mode:
                  type: string
                  enum: [off, always, scheduled]
                schedulePresetId: { type: string, nullable: true }
                schedules:
                  type: array
                  items: { $ref: '#/components/schemas/ProcessScheduleBlock' }
      responses:
        '201':
          description: Process created
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [processId]
                    properties:
                      processId: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/processes/{processId}:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
      - $ref: '#/components/parameters/ProcessIdPath'
    get:
      operationId: getMachineProcess
      tags: [Processes]
      summary: get a single process detail
      description: Requires `machine=<machineId>:read`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Process detail
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data: { $ref: '#/components/schemas/Process' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    patch:
      operationId: updateMachineProcess
      tags: [Processes]
      summary: partial update a process
      description: |
        Update one or more process fields. `processId` and `id` cannot be
        mutated. Duplicate-name attempts return 409. Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                name: { type: string }
                exe_path: { type: string }
                file_path: { type: string }
                cwd: { type: string }
                priority: { type: string }
                visibility: { type: string }
                time_delay: { type: string }
                time_to_init: { type: string }
                relaunch_attempts: { type: string }
                autolaunch: { type: boolean }
                launch_mode:
                  type: string
                  enum: [off, always, scheduled]
                schedulePresetId: { type: string, nullable: true }
                schedules:
                  type: array
                  nullable: true
                  items: { $ref: '#/components/schemas/ProcessScheduleBlock' }
                schedule:
                  type: object
                  nullable: true
                  additionalProperties: true
      responses:
        '200':
          description: Process updated.
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [processId]
                    properties:
                      processId: { type: string }
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }
    delete:
      operationId: deleteMachineProcess
      tags: [Processes]
      summary: delete a process
      description: |
        True-idempotent: returns 200 with `alreadyDeleted: true` when the
        process is already missing. Does NOT kill a running pid — issue
        `/kill` first if needed. Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Removed (or already absent)
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [processId, alreadyDeleted]
                    properties:
                      processId: { type: string }
                      alreadyDeleted: { type: boolean }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/processes/{processId}/kill:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
      - $ref: '#/components/parameters/ProcessIdPath'
    post:
      operationId: killMachineProcess
      tags: [Processes]
      summary: kill a running process (queued command)
      description: |
        Queue a `kill_process` command on the machine. Requires `machine=<machineId>:write`.
        Returns 202 with the queued `commandId`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      responses:
        '202':
          description: Command queued
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [commandId, status]
                    properties:
                      commandId: { type: string }
                      status: { type: string, example: pending }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/processes/{processId}/restart:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
      - $ref: '#/components/parameters/ProcessIdPath'
    post:
      operationId: restartMachineProcess
      tags: [Processes]
      summary: restart a process (queued command)
      description: Queue a `restart_process` command. Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      responses:
        '202':
          description: Command queued
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [commandId, status]
                    properties:
                      commandId: { type: string }
                      status: { type: string, example: pending }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/processes/{processId}/start:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
      - $ref: '#/components/parameters/ProcessIdPath'
    post:
      operationId: startMachineProcess
      tags: [Processes]
      summary: start a process (queued command)
      description: Queue a `start_process` command. Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      responses:
        '202':
          description: Command queued
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [commandId, status]
                    properties:
                      commandId: { type: string }
                      status: { type: string, example: pending }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/processes/{processId}/stop:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
      - $ref: '#/components/parameters/ProcessIdPath'
    post:
      operationId: stopMachineProcess
      tags: [Processes]
      summary: stop a process gracefully (queued command)
      description: Queue a `stop_process` command. Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      responses:
        '202':
          description: Command queued
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [commandId, status]
                    properties:
                      commandId: { type: string }
                      status: { type: string, example: pending }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '409': { $ref: '#/components/responses/Problem409' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  /api/sites/{siteId}/machines/{machineId}/processes/{processId}/schedule:
    parameters:
      - $ref: '#/components/parameters/SiteIdPath'
      - $ref: '#/components/parameters/MachineIdSitePath'
      - $ref: '#/components/parameters/ProcessIdPath'
    post:
      operationId: scheduleMachineProcess
      tags: [Processes]
      summary: update a process schedule (no command queue)
      description: |
        Update the schedule on a process. Does NOT queue a command — the
        agent picks up the new schedule on its next monitoring cycle.
        Requires `machine=<machineId>:write`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - $ref: '#/components/parameters/RequiredIdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mode]
              properties:
                mode:
                  type: string
                  enum: [off, always, scheduled]
                blocks:
                  type: array
                  description: required when mode=scheduled
                  items: { $ref: '#/components/schemas/ProcessScheduleBlock' }
      responses:
        '200':
          description: Schedule updated
          content:
            application/json:
              schema:
                type: object
                required: [ok, data]
                properties:
                  ok: { type: boolean }
                  data:
                    type: object
                    required: [processId, mode]
                    properties:
                      processId: { type: string }
                      mode:
                        type: string
                        enum: [off, always, scheduled]
        '400': { $ref: '#/components/responses/Problem400' }
        '401': { $ref: '#/components/responses/Problem401' }
        '403': { $ref: '#/components/responses/Problem403' }
        '404': { $ref: '#/components/responses/Problem404' }
        '422': { $ref: '#/components/responses/Problem422' }
        '429': { $ref: '#/components/responses/Problem429' }
        '500': { $ref: '#/components/responses/Problem500' }

  # ──────────────────────────────────────────────────────────────────────
  # Users (users-api / api-sprint wave 3 — track 3B)
  # ──────────────────────────────────────────────────────────────────────

  /api/users:
    get:
      tags: [Users]
      summary: list platform users (superadmin only)
      description: |
        Cursor-paginated per AIP-158. Soft-deleted users are excluded by
        default. Requires `user=*:read` scope (superadmin-only at minting)
        or a session/id-token from a superadmin user. Use `page_size`
        and `page_token`; legacy `limit` and `cursor` are accepted for
        existing clients. Responses include
        canonical `next_page_token`; `nextPageToken` is retained as a
        deprecated compatibility alias.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: page_size
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - name: page_token
          in: query
          required: false
          schema: { type: string }
        - name: role
          in: query
          required: false
          schema: { type: string, enum: [member, admin, superadmin] }
        - name: site
          in: query
          required: false
          description: filter to users where this siteId is in their `sites[]`
          schema: { type: string }
        - name: includeDeleted
          in: query
          required: false
          schema: { type: boolean, default: false }
      responses:
        '200':
          description: Page of users
          content:
            application/json:
              schema:
                type: object
                required: [users, next_page_token]
                properties:
                  users:
                    type: array
                    items:
                      type: object
                      properties:
                        uid: { type: string }
                        email: { type: string, nullable: true }
                        role: { type: string, enum: [member, admin, superadmin] }
                        sites: { type: array, items: { type: string } }
                        deletedAt: { type: integer, nullable: true }
                  next_page_token: { type: string }
                  nextPageToken:
                    type: string
                    deprecated: true
        '400': { description: Invalid filter }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }

  /api/users/deletions:
    get:
      tags: [Users]
      summary: list user-deletion audit events (superadmin only)
      description: |
        Lists user-deletion events from the platform audit log, newest first.
        Covers both self-service deletions (`USER_SELF_DELETE`) and
        superadmin-initiated deletions (`USER_DELETE`); both land in
        `global/audit_log/entries`. Requires `user=*:read` scope
        (superadmin-only at minting) or a session/id-token from a superadmin
        user.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: limit
          in: query
          required: false
          description: max events to return (clamped 1..200)
          schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
      responses:
        '200':
          description: Deletion events (newest first)
          content:
            application/json:
              schema:
                type: object
                required: [deletions]
                properties:
                  deletions:
                    type: array
                    items:
                      type: object
                      required: [id]
                      properties:
                        id: { type: string }
                        uid: { type: string, nullable: true }
                        actorUid: { type: string, nullable: true }
                        capability: { type: string }
                        outcome: { type: string }
                        timestamp: { type: string, format: date-time, nullable: true }
                        denyReason: { type: string, nullable: true }
                        counts: { type: object, nullable: true, additionalProperties: true }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }

  /api/users/bootstrap:
    post:
      tags: [Users]
      summary: bootstrap the caller user document
      description: |
        Creates `users/{uid}` for the authenticated caller on first sign-in
        or sign-up. Both the uid AND the email are taken from the verified
        session / Firebase Auth record (`getUser(uid).email`), never from the
        request body — a body-supplied `email` is ignored. This route
        intentionally does not use the platform superadmin handler because a
        new caller has no user document yet.
      security:
        - firebaseIdToken: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                The email is not accepted here; it is resolved server-side
                from the verified auth record. Only optional profile fields
                are read from the body.
              properties:
                displayName:
                  type: string
                  maxLength: 200
                timezone:
                  type: string
                  maxLength: 100
      responses:
        '200':
          description: User document created, or already existed.
          content:
            application/json:
              schema:
                type: object
                required: [uid, alreadyExists]
                properties:
                  uid: { type: string }
                  alreadyExists: { type: boolean }
                  email: { type: string, nullable: true }
                  displayName: { type: string, nullable: true }
                  timezone: { type: string, nullable: true }
                  createdAt: { type: integer, nullable: true }
        '400': { description: Invalid bootstrap payload }
        '401': { description: Unauthorized }

  /api/users/{uid}:
    parameters:
      - name: uid
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [Users]
      summary: get user detail (superadmin only)
      description: |
        Returns the user's role, site assignments, and metadata. Requires
        `user=*:read` scope or session/id-token from a superadmin user.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200': { description: User detail }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }
        '404': { description: User not found }
    delete:
      tags: [Users]
      summary: soft-delete a user with cascade (superadmin only)
      description: |
        Soft-delete cascade. Sets `deletedAt`, revokes the user's api keys,
        cancels their pending commands (best-effort, async), and transfers
        owned sites to the supplied `successorUid`. Refuses if the user
        owns sites and `successorUid` is not provided (409 `orphan_sites`).

        Talons the user authored are reported as `authoredTalonCount` on every
        successful delete — an unattended talon run resolves its AUTHOR's site
        access, so deleting the author stops it. They are only moved when
        `reassignTalons=true` is passed as well; the flag is opt-in so a client
        that has always sent `successorUid` for site ownership does not
        silently start rewriting authorship. Per-site refusals (a successor who
        is not a member there) come back in `talonReassignFailures` rather than
        aborting — the account is already deleted by that point.
        `GET /api/users/{uid}/talons` is the preview.

        True-idempotent: re-issuing DELETE on an already-deleted user
        returns 200 with `alreadyDeleted: true`.

        Requires `user=*:admin` scope or session/id-token from a superadmin.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: successorUid
          in: query
          required: false
          description: |
            Required when the user owns one or more sites. The successor
            must exist, not be soft-deleted, and have role `admin` or
            `superadmin`.
          schema: { type: string }
        - name: reassignTalons
          in: query
          required: false
          description: |
            `true` to also hand `successorUid` the talons this user authored.
            Requires `successorUid`; without it the request is rejected 400.
          schema: { type: string, enum: ['true'] }
        - name: Idempotency-Key
          in: header
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Soft-deleted (or already deleted)
          content:
            application/json:
              schema:
                type: object
                properties:
                  uid: { type: string }
                  alreadyDeleted: { type: boolean }
                  deletedAt: { type: integer }
                  transferredSites:
                    type: array
                    items: { type: string }
                  revokedKeyIds:
                    type: array
                    items: { type: string }
                  authoredTalonCount:
                    type: integer
                    description: Talons this user authored fleet-wide, reported whether or not they moved.
                  reassignedTalonIds:
                    type: array
                    items: { type: string }
                  talonReassignFailures:
                    type: array
                    description: Sites whose talons could not be moved, and why.
                    items:
                      type: object
                      properties:
                        siteId: { type: string }
                        detail: { type: string }
        '400': { description: Invalid successor, or reassignTalons without successorUid }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }
        '404': { description: User not found }
        '409': { description: Orphan sites (provide successorUid) }

  /api/users/{uid}/talons:
    parameters:
      - name: uid
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [Users]
      summary: talons this user authored, fleet-wide (superadmin only)
      description: |
        The account-deletion preview. `DELETE /api/users/{uid}` refuses to
        orphan the SITES a user owns but says nothing about the automations
        they wrote, and a talon with a `cortex` output resolves its author's
        site access on every run — so soft-deleting the author stops it.

        One collection-group query rather than a walk of the user's site
        membership: a superadmin's `sites[]` can be empty while they author
        talons across the fleet.

        Requires `user=*:read` scope or session/id-token from a superadmin.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: What this user authored, with a per-site tally.
          content:
            application/json:
              schema:
                type: object
                properties:
                  uid: { type: string }
                  count: { type: integer }
                  sites:
                    type: array
                    items:
                      type: object
                      properties:
                        siteId: { type: string }
                        count: { type: integer }
                  talons:
                    type: array
                    items:
                      type: object
                      properties:
                        siteId: { type: string }
                        talonId: { type: string }
                        name: { type: string }
                        enabled: { type: boolean }
        '400': { description: Malformed uid }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }

  /api/users/{uid}/promote:
    parameters:
      - name: uid
        in: path
        required: true
        schema: { type: string }
      - name: Idempotency-Key
        in: header
        required: false
        schema: { type: string }
    post:
      tags: [Users]
      summary: promote a user to admin or superadmin
      description: |
        Atomic role update via Firestore transaction. Requires `user=*:admin`
        scope or session/id-token from a superadmin user.

        `user=*:admin`, not the `user=*:write` its demote sibling takes: this
        route can grant `superadmin`, which confers every capability on every
        site. A key narrowed to `write` must not be able to mint a principal
        stronger than itself.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                role:
                  type: string
                  enum: [admin, superadmin]
      responses:
        '200': { description: Promoted (or noop if already at requested role) }
        '400': { description: Invalid role / soft-deleted target }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }
        '404': { description: User not found }

  /api/users/{uid}/demote:
    parameters:
      - name: uid
        in: path
        required: true
        schema: { type: string }
      - name: Idempotency-Key
        in: header
        required: false
        schema: { type: string }
    post:
      tags: [Users]
      summary: demote a user to member
      description: |
        Atomic role update via Firestore transaction. Refuses to demote the
        last active superadmin (409 `last_superadmin`). Requires
        `user=*:write` scope or session/id-token from a superadmin user.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200': { description: Demoted (or noop if already member) }
        '400': { description: Soft-deleted target }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }
        '404': { description: User not found }
        '409': { description: Last superadmin (cannot demote) }

  /api/users/{uid}/assign-sites:
    parameters:
      - name: uid
        in: path
        required: true
        schema: { type: string }
      - name: Idempotency-Key
        in: header
        required: false
        schema: { type: string }
    post:
      tags: [Users]
      summary: assign sites to a user
      description: |
        Adds siteIds to `users/{uid}.sites[]` via `arrayUnion` (idempotent
        at the field level). Validates each siteId exists; rejects the
        whole batch if any are unknown. Requires `user=*:write` scope or
        session/id-token from a superadmin user.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteIds]
              properties:
                siteIds:
                  type: array
                  items: { type: string }
                  minItems: 1
                  maxItems: 100
      responses:
        '200': { description: Sites assigned }
        '400': { description: Validation failure / unknown_site }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }
        '404': { description: User not found }

  /api/users/{uid}/remove-sites:
    parameters:
      - name: uid
        in: path
        required: true
        schema: { type: string }
      - name: Idempotency-Key
        in: header
        required: false
        schema: { type: string }
    post:
      tags: [Users]
      summary: remove sites from a user
      description: |
        Removes siteIds from `users/{uid}.sites[]` via `arrayRemove`. Best-
        effort cancels pending commands the user issued on those sites
        (failure does not block the response). Requires `user=*:write`
        scope or session/id-token from a superadmin user.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [siteIds]
              properties:
                siteIds:
                  type: array
                  items: { type: string }
                  minItems: 1
                  maxItems: 100
      responses:
        '200': { description: Sites removed }
        '400': { description: Validation failure }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not superadmin) }
        '404': { description: User not found }
        '409': { description: User owns one or more of the named sites; transfer ownership first }

  # ──────────────────────────────────────────────────────────────────────
  # Site members (users-api / api-sprint wave 3 — track 3B)
  # ──────────────────────────────────────────────────────────────────────

  /api/sites/{siteId}/members:
    parameters:
      - name: siteId
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [Site Members]
      summary: list site members with derived per-site role
      description: |
        Membership lives only on `users/{uid}.sites[]` (per the api-sprint
        wave 0.2 decision). This endpoint resolves it by querying users
        where `sites array-contains <siteId>` and additionally surfaces
        the site `owner` if they aren't already in that set. Per-site
        role is derived: `owner` (always wins), then `superadmin`,
        `admin` (global admin assigned to the site), else `member`.

        A member's other site memberships are deliberately not returned —
        they would disclose the site ids of every other organization that
        member belongs to.

        Requires `site=<siteId>:admin` scope or session/id-token from an
        admin of this site.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      responses:
        '200':
          description: Members
          content:
            application/json:
              schema:
                type: object
                properties:
                  members:
                    type: array
                    items:
                      type: object
                      properties:
                        uid: { type: string }
                        email: { type: string, nullable: true }
                        role:
                          type: string
                          enum: [owner, superadmin, admin, member]
                        globalRole:
                          type: string
                          enum: [member, admin, superadmin]
                        displayName: { type: string, nullable: true }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not site admin) }
        '404': { description: Site not found or no access }
    post:
      tags: [Site Members]
      summary: add a member to a site
      description: |
        Adds siteId to the target user's `users/{uid}.sites[]` via
        `arrayUnion`. The per-site role is derived from global role at
        read time, so requesting `role: 'admin'` for a member-tier user
        only succeeds in extending membership — the global role is
        unchanged (use `/api/users/{uid}/promote` for that). The
        response includes `roleHonored: false` in that case.

        Identify the target by **exactly one** of `uid` or `email`.
        `email` is trimmed and lowercased, then resolved to a uid through
        Firebase Auth; supplying both, or neither, is a `400`, and an
        address with no auth user is the same `404` as an unknown uid.

        Requires `site=<siteId>:admin` scope or session/id-token from an
        admin of this site.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                uid: { type: string }
                email: { type: string, format: email, maxLength: 254 }
                role: { type: string, enum: [admin, member] }
              oneOf:
                - required: [uid]
                - required: [email]
      responses:
        '200': { description: Member added }
        '400': { description: Validation failure }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not site admin) }
        '404': { description: User or site not found }

  /api/sites/{siteId}/transfer-ownership:
    parameters:
      - name: siteId
        in: path
        required: true
        schema: { type: string }
      - name: Idempotency-Key
        in: header
        required: false
        schema: { type: string }
    post:
      tags: [Site Members]
      summary: transfer site ownership to another user
      description: |
        Moves ownership of a site to `successorUid` in a single Firestore
        transaction, writing the new `sites/{siteId}/members/{uid}` role and the
        legacy `sites/{siteId}.owner` field together.

        **Only the current owner or a superadmin may transfer.** A site admin is
        admitted by the route's capability gate and then refused by the
        transaction with `403 not_owner`: administering a site and owning it are
        different things, the same line `SITE_DELETE` draws. The ownership check
        is made from the site document read INSIDE the transaction, so a transfer
        racing another transfer is serialised rather than interleaved — the
        loser re-reads, finds the owner changed, and is refused.

        The successor need not already be a member; they are made one by the same
        transaction. The outgoing owner is demoted to `admin` and KEEPS their
        membership — handing a site over is not eviction. Transferring to the
        current owner is `409 successor_already_owner` rather than a silent no-op.

        Requires `site=<siteId>:admin` scope or session/id-token from the site's
        owner (or a superadmin).
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [successorUid]
              properties:
                successorUid:
                  type: string
                  description: uid receiving ownership
      responses:
        '200': { description: Ownership transferred }
        '400': { description: Validation failure, or successor is soft-deleted }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (caller is not the owner or a superadmin) }
        '404': { description: Site or successor not found }
        '409': { description: Successor already owns the site, or the site records no owner }

  /api/sites/{siteId}/members/{uid}:
    parameters:
      - name: siteId
        in: path
        required: true
        schema: { type: string }
      - name: uid
        in: path
        required: true
        schema: { type: string }
      - name: Idempotency-Key
        in: header
        required: false
        schema: { type: string }
    delete:
      tags: [Site Members]
      summary: remove a member from a site
      description: |
        Removes siteId from the target user's `users/{uid}.sites[]` via
        `arrayRemove`. Refuses to remove the site owner (409
        `cannot_remove_owner`) — use `DELETE /api/users/{uid}?successorUid=<uid>`
        to transfer ownership instead.

        Talons the departing member authored survive the removal — nothing
        under `sites/{siteId}` is touched — but a talon with a `cortex` output
        re-resolves its AUTHOR's site access on every run, so it starts failing
        the moment they lose access. `talonCount` is therefore always in the
        response, and `?talonSuccessorUid=<uid>` moves those talons to a
        successor *before* the membership write, so the automation never has a
        moment where its author has no access. A refused successor answers
        `400 successor_invalid` and the membership is left intact.
        `GET /api/sites/{siteId}/talons/authored?uid=<uid>` is the preview.

        True-idempotent: removing a non-member returns 200 with
        `wasMember: false`.

        Requires `site=<siteId>:admin` scope or session/id-token from an
        admin of this site. Reassignment additionally requires `TALON_MANAGE`.
      security:
        - apiKey: []
        - bearerApiKey: []
        - firebaseIdToken: []
      parameters:
        - name: talonSuccessorUid
          in: query
          required: false
          description: |
            Move the talons this member authored on this site to that user
            before removing them. Held to the same bar as any talon author.
          schema: { type: string }
      responses:
        '200':
          description: Member removed (or was not a member)
          content:
            application/json:
              schema:
                type: object
                properties:
                  siteId: { type: string }
                  uid: { type: string }
                  wasMember: { type: boolean }
                  talonCount:
                    type: integer
                    description: Talons this member authored here, reported whether or not they moved.
                  reassignedTalonIds:
                    type: array
                    items: { type: string }
        '400': { description: Malformed or refused talonSuccessorUid }
        '401': { description: Unauthorized }
        '403': { description: Forbidden (not site admin, or lacks TALON_MANAGE) }
        '404': { description: User or site not found }
        '409': { description: Cannot remove site owner }

# ════════════════════════════════════════════════════════════════════════
# Components
# ════════════════════════════════════════════════════════════════════════
components:
  securitySchemes:
    apiKey:
      type: apiKey
      name: x-api-key
      in: header
      description: |
        Legacy api key header — supported for backwards compatibility.
        New integrations should use `bearerApiKey` instead
        (`Authorization: Bearer owk_live_...`).
    bearerApiKey:
      type: http
      scheme: bearer
      bearerFormat: owk_live_* / owk_test_*
      description: |
        Preferred auth mechanism for the Owlette public API. Send the
        scoped api key as `Authorization: Bearer owk_live_<secret>`
        (production) or `Authorization: Bearer owk_test_<secret>`
        (sandbox). See [authentication.md](/docs/api/authentication).
    firebaseIdToken:
      type: http
      scheme: bearer
      bearerFormat: Firebase ID Token
      description: |
        Firebase ID token issued to the authenticated user or agent device.
        Used by dashboard and agent requests. First-party
        clients only — external integrations use `apiKey` /
        `bearerApiKey` instead.

  parameters:
    SiteIdQuery:
      name: siteId
      in: query
      required: true
      schema:
        type: string
      description: The site ID

    MachineIdQuery:
      name: machineId
      in: query
      required: true
      schema:
        type: string
      description: The machine ID

    ProcessIdPath:
      name: processId
      in: path
      required: true
      schema:
        type: string
      description: The process configuration ID

    DeploymentIdPath:
      name: deploymentId
      in: path
      required: true
      schema:
        type: string
      description: The deployment ID

    PresetIdPath:
      name: presetId
      in: path
      required: true
      schema:
        type: string
        pattern: '^[A-Za-z0-9_-]{1,128}$'
      description: The preset ID

    TemplateIdPath:
      name: templateId
      in: path
      required: true
      schema:
        type: string
        pattern: '^[A-Za-z0-9_-]{1,128}$'
      description: The deployment template ID

    TalonIdPath:
      name: talonId
      in: path
      required: true
      schema:
        type: string
        pattern: '^[A-Za-z0-9_-]{1,128}$'
      description: The talon ID

    RoostIdPath:
      name: roostId
      in: path
      required: true
      schema:
        type: string
      description: The roost ID (deploy unit).

    IdempotencyKeyHeader:
      name: Idempotency-Key
      in: header
      required: false
      schema:
        type: string
        maxLength: 255
      description: |
        Client-generated idempotency key. If the server has seen the same
        key on this method, path, query, and request body within the replay
        window, it returns the original response instead of re-executing.
        Replayed responses include `Idempotent-Replayed: true`.

    RequiredIdempotencyKeyHeader:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        maxLength: 255
      description: |
        Required client-generated idempotency key. If the server has seen
        the same key on this method, path, query, and request body within
        the replay window, it returns the original response instead of
        re-executing. Replayed responses include `Idempotent-Replayed: true`.

    RoostVersionHeader:
      name: Roost-Version
      in: header
      required: false
      schema:
        type: string
        pattern: '^\d{4}-\d{2}-\d{2}$'
        example: '2026-04-22'
      description: |
        Selects a dated api version. Optional in wave 1; required once
        a second version ships. See [`GET /api/version`](#tag/utility/operation/getApiVersion).

    IfMatchHeader:
      name: If-Match
      in: header
      required: false
      schema:
        type: string
      description: |
        Optimistic concurrency token. Pass the prior resource version
        (e.g. a `currentVersionId` or opaque etag) to have the server
        reject the mutation with 412 if the resource has changed.

    PageSizeQuery:
      name: page_size
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
      description: Maximum items per page.

    PageTokenQuery:
      name: page_token
      in: query
      required: false
      schema:
        type: string
      description: |
        Opaque cursor from the prior response's `next_page_token`. An
        empty string means no more pages.

    KeyIdPath:
      name: keyId
      in: path
      required: true
      schema:
        type: string
        minLength: 1
      description: The API key id. Current scoped keys use UUID values; legacy records may use older ids.

    WebhookIdPath:
      name: webhookId
      in: path
      required: true
      schema:
        type: string
        pattern: '^[A-Za-z0-9_-]{8,64}$'
      description: Webhook subscription id.

    DeliveryIdPath:
      name: deliveryId
      in: path
      required: true
      schema:
        type: string
        pattern: '^[A-Za-z0-9_.-]{1,256}$'
      description: Webhook delivery id.

    RolloutIdPath:
      name: rolloutId
      in: path
      required: true
      schema:
        type: string
        pattern: '^rollout_[A-Z0-9]+$'
      description: Rollout (deployment) id (ulid-encoded).

    SiteIdPath:
      name: siteId
      in: path
      required: true
      schema:
        type: string
      description: Site id (path param variant; distinct from the `siteId` query parameter).

    MachineIdSitePath:
      name: machineId
      in: path
      required: true
      schema:
        type: string
      description: Machine id (path param on `/api/sites/{siteId}/machines/{machineId}`).

    VersionRefPath:
      name: versionRef
      in: path
      required: true
      schema:
        type: string
      description: |
        Addresses a version on the roost. Accepts any of:
          - a stable content-addressed id: 64-char sha-256 hex
          - a legacy stable id alias: `vrs_<hex>`
          - a version number: `3`, `#3`, `v3`, or `V3`
          - a roost-relative alias: `current`, `previous`, `first`
        Malformed refs return 400 `version_ref_malformed`;
        unmatched refs return 404 `version_not_found`.

    DigestPath:
      name: digest
      in: path
      required: true
      schema:
        type: string
        pattern: '^[0-9a-f]{64}$'
      description: Chunk digest as lowercase sha-256 hex, without `sha256:`.

    RecordHashPath:
      name: recordHash
      in: path
      required: true
      schema:
        type: string
        pattern: '^[0-9a-f]{64}$'
      description: Audit record hash as raw lowercase sha-256 hex.

  headers:
    RateLimitLimit:
      description: Window limit for the active rate-limit bucket.
      schema:
        type: integer
    RateLimitRemaining:
      description: Calls remaining in the current window.
      schema:
        type: integer
    RateLimitReset:
      description: Seconds until the current window fully resets.
      schema:
        type: integer
    RetryAfter:
      description: Seconds the client should wait before retrying (429 only).
      schema:
        type: integer
    RoostRateLimitedReason:
      description: |
        Which rate-limit bucket tripped (`global-rate`, `endpoint-rate`,
        `key-rate`, `site-concurrency`). Present only on 429.
      schema:
        type: string
        enum: [global-rate, endpoint-rate, key-rate, site-concurrency]
    IdempotentReplayed:
      description: Present with value `true` when a response was replayed from the idempotency cache.
      schema:
        type: string
        enum: ['true']

  responses:
    Problem400:
      description: Validation failed.
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem401:
      description: Authentication required.
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem402:
      description: |
        Payment required — the site's roost storage quota would be
        exceeded (`code: quota_exceeded`).
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem403:
      description: |
        Forbidden — cross-site access, a missing capability, or an api key
        without the required scope (`code: scope_insufficient`).
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem404:
      description: Resource not found.
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem409:
      description: Conflict (e.g. no previous version to roll back to).
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem412:
      description: Precondition failed (optimistic concurrency miss on finalize).
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem413:
      description: |
        Payload too large or quota-bound size limit exceeded — e.g. a
        deployment with more target machines than the per-site quota
        allows. The body carries `code: over_quota` and a `quota` block
        describing the active limit.
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem422:
      description: |
        Request semantics violated a constraint — e.g. an `Idempotency-Key`
        was reused with a different request body (`code: idempotency_key_mismatch`).
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem429:
      description: Rate limited.
      headers:
        RateLimit-Limit:           { $ref: '#/components/headers/RateLimitLimit' }
        RateLimit-Remaining:       { $ref: '#/components/headers/RateLimitRemaining' }
        RateLimit-Reset:           { $ref: '#/components/headers/RateLimitReset' }
        Retry-After:               { $ref: '#/components/headers/RetryAfter' }
        Roost-Rate-Limited-Reason: { $ref: '#/components/headers/RoostRateLimitedReason' }
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem500:
      description: Internal server error.
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }
    Problem501:
      description: |
        Endpoint is scaffolded but the backing infrastructure is not yet
        provisioned (current state for roost stubs pending wave 0.5/0.6).
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetails' }

  schemas:
    SuccessResponse:
      description: Generic success envelope returned by simple mutating endpoints that have no resource body to send back.
      type: object
      properties:
        success:
          type: boolean
          example: true

    ErrorResponse:
      description: Legacy error envelope. Public endpoints return RFC 7807 `application/problem+json` (see `ProblemDetails`); this shape only remains on a few compatibility routes.
      type: object
      properties:
        error:
          type: string
      example:
        error: Description of what went wrong

    HootConversationSummary:
      description: Metadata for a single hoot conversation, without its message history.
      allOf:
        - $ref: '#/components/schemas/CortexConversationSummary'

    HootConversation:
      description: A hoot conversation including its full message history.
      allOf:
        - $ref: '#/components/schemas/CortexConversation'

    CortexConversationSummary:
      description: Metadata for a single Hoot conversation, without its message history.
      type: object
      properties:
        conversationId: { type: string }
        title: { type: string, nullable: true }
        siteId: { type: string }
        machineId: { type: string, nullable: true }
        ownerUid: { type: string }
        createdAt: { type: string, format: date-time, nullable: true }
        updatedAt: { type: string, format: date-time, nullable: true }
        deletedAt: { type: string, format: date-time, nullable: true }
        messageCount: { type: integer }

    CortexConversation:
      description: A Hoot conversation including its full message history.
      allOf:
        - $ref: '#/components/schemas/CortexConversationSummary'
        - type: object
          properties:
            messages:
              type: array
              items:
                type: object
                properties:
                  role: { type: string, enum: [user, assistant, system] }
                  content: { type: string }
                  timestamp: { type: string, format: date-time, nullable: true }

    Site:
      description: A site — a logical grouping of machines in your fleet.
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        machineCount:
          type: integer

    Machine:
      description: A machine (agent install) registered to a site, with its live online and heartbeat state.
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        online:
          type: boolean
        lastHeartbeat:
          type: string
          format: date-time
        agentVersion:
          type: string
        os:
          type: string

    Process:
      description: A monitored process configured on a machine — launch mode, schedule, and live runtime status.
      type: object
      required: [processId, name, exe_path, launch_mode, status]
      properties:
        processId:
          type: string
        id:
          type: string
          nullable: true
          deprecated: true
        name:
          type: string
        exe_path:
          type: string
        file_path:
          type: string
        cwd:
          type: string
        priority:
          type: string
        visibility:
          type: string
        time_delay:
          type: string
        time_to_init:
          type: string
        relaunch_attempts:
          type: string
        autolaunch:
          type: boolean
        launch_mode:
          type: string
          enum: [off, always, scheduled]
        schedules:
          type: array
          nullable: true
          items: { $ref: '#/components/schemas/ProcessScheduleBlock' }
        schedule:
          type: object
          nullable: true
          additionalProperties: true
        schedulePresetId:
          type: string
          nullable: true
        status:
          type: string
        pid:
          type: integer
          nullable: true
        responsive:
          type: boolean
        last_updated:
          nullable: true
          oneOf:
            - type: string
            - type: number

    ProcessScheduleBlock:
      description: One scheduled on/off block for a process — which days and time ranges it should be running.
      type: object
      required: [days, ranges]
      properties:
        name:
          type: string
        colorIndex:
          type: number
        days:
          type: array
          items: { type: string }
        ranges:
          type: array
          items:
            type: object
            required: [start, stop]
            properties:
              start: { type: string }
              stop: { type: string }

    Schedule:
      description: A recurring weekly schedule expressed as days plus a start and end time.
      type: object
      properties:
        days:
          type: array
          items:
            type: string
            enum: [mon, tue, wed, thu, fri, sat, sun]
        startTime:
          type: string
          description: 24-hour time (HH:MM)
          example: "08:00"
        endTime:
          type: string
          description: 24-hour time (HH:MM)
          example: "18:00"

    ScheduleTimeRange:
      description: A single start/end time range within a schedule.
      type: object
      required: [start, end]
      properties:
        start:
          type: string
          pattern: '^([01]\d|2[0-3]):[0-5]\d$'
          example: "09:00"
        end:
          type: string
          pattern: '^([01]\d|2[0-3]):[0-5]\d$'
          example: "17:00"

    ScheduleBlock:
      type: object
      required: [days, ranges]
      properties:
        name: { type: string }
        colorIndex: { type: number }
        days:
          type: array
          items:
            type: string
            enum: [mon, tue, wed, thu, fri, sat, sun]
        ranges:
          type: array
          items: { $ref: '#/components/schemas/ScheduleTimeRange' }

    SchedulePresetInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        blocks:
          type: array
          items: { $ref: '#/components/schemas/ScheduleBlock' }
          default: []
        isBuiltIn:
          type: boolean
          default: false
        order:
          type: number
          default: 0

    SchedulePresetPatch:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        blocks:
          type: array
          items: { $ref: '#/components/schemas/ScheduleBlock' }
        isBuiltIn: { type: boolean }
        order: { type: number }

    SchedulePreset:
      allOf:
        - $ref: '#/components/schemas/SchedulePresetInput'
        - type: object
          required: [id, blocks, isBuiltIn, order, createdBy, createdAt]
          properties:
            id: { type: string }
            createdBy: { type: string }
            createdAt:
              type: string
              format: date-time
              nullable: true
            updatedAt:
              type: string
              format: date-time
              nullable: true

    RebootScheduleEntry:
      type: object
      required: [id, days, time]
      properties:
        id: { type: string }
        days:
          type: array
          items:
            type: string
            enum: [mon, tue, wed, thu, fri, sat, sun]
        time:
          type: string
          pattern: '^([01]\d|2[0-3]):[0-5]\d$'
          example: "03:00"

    RebootPresetInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        entries:
          type: array
          items: { $ref: '#/components/schemas/RebootScheduleEntry' }
          default: []
        isBuiltIn:
          type: boolean
          default: false
        order:
          type: number
          default: 0

    RebootPresetPatch:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        entries:
          type: array
          items: { $ref: '#/components/schemas/RebootScheduleEntry' }
        isBuiltIn: { type: boolean }
        order: { type: number }

    RebootPreset:
      allOf:
        - $ref: '#/components/schemas/RebootPresetInput'
        - type: object
          required: [id, entries, isBuiltIn, order, createdBy, createdAt]
          properties:
            id: { type: string }
            createdBy: { type: string }
            createdAt:
              type: string
              format: date-time
              nullable: true
            updatedAt:
              type: string
              format: date-time
              nullable: true

    # talons (automation)
    TalonScheduleEntry:
      type: object
      required: [id, days, time]
      properties:
        id:
          type: string
          maxLength: 128
          description: Client-generated row id, stable across edits.
        days:
          type: array
          minItems: 1
          items:
            type: string
            enum: [mon, tue, wed, thu, fri, sat, sun]
          description: De-duplicated and re-sorted into canonical week order on save.
        time:
          type: string
          pattern: '^([01]\d|2[0-3]):[0-5]\d$'
          example: '09:30'
          description: |
            24-hour clock time, resolved in the **site's** timezone — one talon
            can span machines in several zones and its schedule must have one
            meaning.

    TalonScheduleEntriesTrigger:
      type: object
      required: [type, entries]
      properties:
        type: { type: string, enum: [schedule] }
        entries:
          type: array
          minItems: 1
          maxItems: 50
          items: { $ref: '#/components/schemas/TalonScheduleEntry' }

    TalonIntervalTrigger:
      type: object
      required: [type, intervalMinutes]
      properties:
        type: { type: string, enum: [schedule] }
        intervalMinutes:
          type: integer
          minimum: 5
          maximum: 1440
          description: |
            Floor is 5, or 15 when the condition is a `visual_check` (each run
            costs a screenshot round-trip plus a model call). Anything longer
            than a day belongs in `entries` as a fixed clock time.

    TalonThresholdTrigger:
      type: object
      required: [type, metric, operator, value]
      properties:
        type: { type: string, enum: [threshold] }
        metric:
          type: string
          enum:
            - cpu_percent
            - memory_percent
            - disk_percent
            - gpu_percent
            - cpu_temp
            - gpu_temp
            - network_latency
            - network_packet_loss
        operator:
          type: string
          enum: ['>', '<', '>=', '<=']
        value: { type: number }

    TalonEventTrigger:
      type: object
      required: [type, eventTypes]
      properties:
        type: { type: string, enum: [event] }
        eventTypes:
          type: array
          minItems: 1
          items:
            type: string
            enum:
              - process_crash
              - process_start_failed
              - machine_offline
              - display_monitor_removed
              - display_apply_failed
              - display_auto_revert_fired
              - display_sync_lost
              - display_drift
              - display_monitor_swapped
              - display_mosaic_disabled
              - display_apply_refused_mosaic
              - display_monitor_added
              - display_apply_succeeded
          description: De-duplicated on save.

    TalonTrigger:
      description: |
        What makes the talon fire. A `schedule` trigger takes either `entries`
        (fixed clock times) or `intervalMinutes` — never both.
      oneOf:
        - $ref: '#/components/schemas/TalonScheduleEntriesTrigger'
        - $ref: '#/components/schemas/TalonIntervalTrigger'
        - $ref: '#/components/schemas/TalonThresholdTrigger'
        - $ref: '#/components/schemas/TalonEventTrigger'

    TalonCondition:
      description: |
        Optional gate between the trigger and the outputs. Defaults to `none`
        when omitted. A `visual_check` captures a fresh screenshot and asks the
        vision model whether `expectation` holds, so it requires a site-level
        LLM key.
      oneOf:
        - type: object
          required: [type]
          properties:
            type: { type: string, enum: [none] }
        - type: object
          required: [type, expectation]
          properties:
            type: { type: string, enum: [visual_check] }
            expectation:
              type: string
              maxLength: 500
              description: Natural-language statement the screenshot must satisfy.
            monitor:
              type: integer
              minimum: 0
              maximum: 64
              description: '0 (default) = all monitors combined, 1 = primary, 2 = second, and so on.'

    TalonOutput:
      description: |
        What the talon does when it fires. `command` outputs may only be
        authored by a site admin; `cortex` outputs require the talon's author
        to have an LLM key saved in their own account settings; `webhook` urls
        must be https and must not resolve to a private, loopback, or
        link-local address.
      oneOf:
        - type: object
          required: [type]
          properties:
            type: { type: string, enum: [email] }
        - type: object
          required: [type, url]
          properties:
            type: { type: string, enum: [webhook] }
            url:
              type: string
              format: uri
              description: |
                Signed with a per-talon `whsec_*` secret held server-side. The
                secret is never returned by any endpoint.
        - type: object
          required: [type, directive]
          properties:
            type: { type: string, enum: [cortex] }
            directive: { type: string, maxLength: 1000 }
            allowActions:
              type: boolean
              default: false
              description: |
                Let the unattended turn use tier-2 tools (process control,
                service management, screenshots) instead of the read-only
                tier-1 set. Site admins only — the same privilege a `command`
                output takes. Tier-3 tools stay unreachable on this path.
        - type: object
          required: [type, commandType]
          description: |
            Exactly one of `processId` or `processName` is required — a command
            with no target fails on every run.
          properties:
            type: { type: string, enum: [command] }
            commandType:
              type: string
              enum: [restart_process, start_process, stop_process]
            processId: { type: string, maxLength: 256 }
            processName: { type: string, maxLength: 256 }

    TalonScope:
      type: object
      required: [machineIds]
      properties:
        machineIds:
          type: array
          nullable: true
          minItems: 1
          items: { type: string }
          description: '`null` — or an omitted `scope` — means every machine in the site.'

    TalonInput:
      type: object
      required: [name, trigger, outputs]
      description: |
        The caller-owned half of a talon. Server-owned fields
        (`schemaVersion`, `createdBy`, `createdVia`, `createdAt`, `nextRunAt`,
        run bookkeeping) are rejected as unknown fields. `PATCH` bodies are
        full replacements — omitted optional fields return to their defaults.
      properties:
        name: { type: string, maxLength: 80 }
        description: { type: string, maxLength: 500 }
        enabled: { type: boolean, default: true }
        trigger: { $ref: '#/components/schemas/TalonTrigger' }
        condition: { $ref: '#/components/schemas/TalonCondition' }
        outputs:
          type: array
          minItems: 1
          maxItems: 5
          items: { $ref: '#/components/schemas/TalonOutput' }
        scope: { $ref: '#/components/schemas/TalonScope' }
        cooldownMinutes:
          type: integer
          minimum: 0
          maximum: 1440
          default: 60
          description: Minimum gap between runs.

    TalonPresetTemplate:
      type: object
      required: [name, trigger, outputs]
      description: |
        The talon-shaped payload a preset carries — `TalonInput` MINUS `scope`
        and `enabled`, both of which are rejected. Machine ids mean nothing in
        another site, and a template does not decide whether the talon it
        creates is armed. Validated by the talon validator, so every talon
        bound applies here too.
      properties:
        name: { type: string, maxLength: 80 }
        description: { type: string, maxLength: 500 }
        trigger: { $ref: '#/components/schemas/TalonTrigger' }
        condition: { $ref: '#/components/schemas/TalonCondition' }
        outputs:
          type: array
          minItems: 1
          maxItems: 5
          items: { $ref: '#/components/schemas/TalonOutput' }
        cooldownMinutes:
          type: integer
          minimum: 0
          maximum: 1440
          default: 60

    TalonPresetInput:
      type: object
      required: [name, template]
      properties:
        name: { type: string, maxLength: 80 }
        description: { type: string, maxLength: 500 }
        template: { $ref: '#/components/schemas/TalonPresetTemplate' }
        isBuiltIn:
          type: boolean
          default: false
        order:
          type: number
          default: 0

    TalonPresetPatch:
      type: object
      properties:
        name: { type: string, maxLength: 80 }
        description: { type: string, maxLength: 500 }
        template: { $ref: '#/components/schemas/TalonPresetTemplate' }
        isBuiltIn: { type: boolean }
        order: { type: number }

    TalonPreset:
      allOf:
        - $ref: '#/components/schemas/TalonPresetInput'
        - type: object
          required: [id, template, isBuiltIn, order, createdBy, createdAt]
          properties:
            id: { type: string }
            createdBy: { type: string }
            createdAt:
              type: string
              format: date-time
              nullable: true
            updatedAt:
              type: string
              format: date-time
              nullable: true

    TalonEnabledPatch:
      type: object
      required: [enabled]
      additionalProperties: false
      description: |
        Toggle-only `PATCH` body. Exactly one key: any additional key makes the
        request a full `TalonInput` update instead.
      properties:
        enabled: { type: boolean }

    Talon:
      allOf:
        - $ref: '#/components/schemas/TalonInput'
        - type: object
          required:
            - id
            - schemaVersion
            - enabled
            - condition
            - scope
            - cooldownMinutes
            - createdBy
            - createdVia
            - consecutiveFailures
          properties:
            id: { type: string }
            schemaVersion: { type: integer, enum: [1] }
            createdBy:
              type: string
              description: uid of the authoring user.
            createdVia:
              type: string
              enum: [ui, cortex]
            chatId:
              type: string
              description: Present when `createdVia` is `cortex` — the chat the talon came from.
            createdAt:
              type: string
              format: date-time
              nullable: true
            updatedAt:
              type: string
              format: date-time
              nullable: true
            nextRunAt:
              type: string
              format: date-time
              nullable: true
              description: Schedule triggers only — when the scheduler next evaluates the talon.
            lastRunAt:
              type: string
              format: date-time
              nullable: true
            lastRunStatus:
              type: string
              nullable: true
              enum: [running, succeeded, failed, skipped, missed]
            lastRunId:
              type: string
              nullable: true
            consecutiveFailures:
              type: integer
              description: Reset to 0 on any successful run; drives auto-disable backoff.
            disabledReason:
              type: string
              nullable: true
              enum:
                - creator_not_a_user
                - creator_deleted
                - creator_access_revoked
                - creator_missing_llm_key
                - repeated_failures
              description: >-
                Why the SYSTEM switched this talon off. `null` while it is
                enabled, and cleared whenever a person toggles it either way.
                The four `creator_*` reasons are unrecoverable and disable the
                talon on the first occurrence; `repeated_failures` is the
                consecutive-failure backoff.

    TalonRun:
      type: object
      required: [id, talonId, talonName, status, outputs, manual]
      properties:
        id: { type: string }
        talonId: { type: string }
        talonName:
          type: string
          description: Denormalized so the run list renders without a talon lookup.
        triggerType:
          type: string
          nullable: true
          enum: [schedule, threshold, event]
        triggerSummary:
          type: string
          nullable: true
          description: 'Human-readable trigger summary, e.g. `cpu_percent > 90`.'
        machineId: { type: string, nullable: true }
        machineName: { type: string, nullable: true }
        status:
          type: string
          enum: [running, succeeded, failed, skipped, missed]
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        durationMs: { type: integer, nullable: true }
        condition:
          type: object
          nullable: true
          required: [type, verdict]
          description: Outcome of the condition gate, when the talon had one.
          properties:
            type: { type: string, enum: [none, visual_check] }
            verdict: { type: string, enum: [pass, fail] }
            confidence:
              type: number
              description: Vision-model confidence, 0–1, when the condition produced one.
            reason: { type: string }
            screenshotPath: { type: string }
            screenshotUrl:
              type: string
              format: uri
              description: Short-lived signed read url for `screenshotPath`.
        outputs:
          type: array
          description: Per-output delivery result.
          items:
            type: object
            required: [type, status]
            properties:
              type: { type: string, enum: [email, webhook, cortex, command] }
              status: { type: string, enum: [sent, failed, skipped] }
              detail: { type: string }
              httpStatus:
                type: integer
                description: Webhook outputs only.
              error: { type: string }
              disabledReason:
                type: string
                enum:
                  - creator_not_a_user
                  - creator_deleted
                  - creator_access_revoked
                  - creator_missing_llm_key
                description: >-
                  Set when THIS output's failure was unrecoverable and cost the
                  talon its enabled state.
        correlationId:
          type: string
          nullable: true
          description: Ties the run to its audit-log entries and downstream commands.
        chatId: { type: string, nullable: true }
        error: { type: string, nullable: true }
        disabledReason:
          type: string
          nullable: true
          enum:
            - creator_not_a_user
            - creator_deleted
            - creator_access_revoked
            - creator_missing_llm_key
            - repeated_failures
          description: >-
            Set when this run is the one that switched the talon off, so the run
            history explains itself. Mirrors `Talon.disabledReason`.
        manual:
          type: boolean
          description: True when an operator ran the talon on demand rather than the trigger firing.

    TalonRunSummary:
      type: object
      required: [runId, status, outputs]
      description: |
        What one execution did, returned by the manual-run endpoint. A
        condensed view of the run record — fetch the run history for the
        denormalized trigger, machine, and timing fields.
      properties:
        runId: { type: string }
        status:
          type: string
          enum: [running, succeeded, failed, skipped, missed]
        machineId:
          type: string
          description: Present when the run targeted a single machine.
        outputs:
          type: array
          description: Per-output delivery result.
          items:
            type: object
            required: [type, status]
            properties:
              type: { type: string, enum: [email, webhook, cortex, command] }
              status: { type: string, enum: [sent, failed, skipped] }
              detail: { type: string }
              httpStatus:
                type: integer
                description: Webhook outputs only.
              error: { type: string }
        condition:
          type: object
          required: [type, verdict]
          description: Outcome of the condition gate, when the talon had one.
          properties:
            type: { type: string, enum: [none, visual_check] }
            verdict: { type: string, enum: [pass, fail] }
            confidence:
              type: number
              description: Vision-model confidence, 0–1, when the condition produced one.
            reason: { type: string }
            screenshotPath: { type: string }
            screenshotUrl:
              type: string
              format: uri
              description: Short-lived signed read url for `screenshotPath`.
        error:
          type: string
          description: Skip reason or failure detail, mirroring the run record's `error`.

    DeploymentTemplateInput:
      type: object
      required: [name, installer_name, installer_url, silent_flags]
      properties:
        name: { type: string }
        installer_name: { type: string }
        installer_url:
          type: string
          format: uri
          description: Must use `https://`.
        silent_flags: { type: string }
        verify_path:
          type: string
          nullable: true
        close_processes:
          type: array
          items: { type: string }
        parallel_install: { type: boolean }

    DeploymentTemplatePatch:
      type: object
      properties:
        name: { type: string }
        installer_name: { type: string }
        installer_url:
          type: string
          format: uri
        silent_flags: { type: string }
        verify_path:
          type: string
          nullable: true
        close_processes:
          type: array
          items: { type: string }
        parallel_install: { type: boolean }

    DeploymentTemplate:
      allOf:
        - $ref: '#/components/schemas/DeploymentTemplateInput'
        - type: object
          required: [id, name, installer_name, installer_url, silent_flags, createdAt]
          properties:
            id: { type: string }
            close_processes:
              type: array
              items: { type: string }
              nullable: true
            createdAt:
              type: string
              format: date-time
              nullable: true
            updatedAt:
              type: string
              format: date-time
              nullable: true

    DeploymentSummary:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        installer_name:
          type: string
        status:
          type: string
          enum: [pending, in_progress, completed, failed, partial, partial_failed, cancelled, uninstalled]
        createdAt:
          type: integer
          description: Unix timestamp in milliseconds

    Deployment:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        installer_name:
          type: string
        installer_url:
          type: string
          format: uri
        silent_flags:
          type: string
        verify_path:
          type: string
        status:
          type: string
          enum: [pending, in_progress, completed, failed, partial, partial_failed, cancelled, uninstalled]
        createdAt:
          type: integer
          description: Unix timestamp in milliseconds
        targets:
          type: array
          items:
            type: object
            properties:
              machineId:
                type: string
              status:
                type: string
              progress:
                type: integer
                description: Download progress percentage (0-100)
              completedAt:
                type: integer
                description: Unix timestamp in milliseconds

    Webhook:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        url:
          type: string
          format: uri
        events:
          type: array
          items:
            type: string
            enum: [process_crash, machine_offline, connection_failure]
        createdAt:
          type: string
          format: date-time

    ApiKeyMetadata:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        keyPrefix:
          type: string
          description: First few characters of the key for identification
        createdAt:
          type: string
          format: date-time
        lastUsedAt:
          type: string
          format: date-time

    SystemPresetInput:
      type: object
      required:
        - name
        - software_name
        - category
        - installer_name
        - installer_url
        - silent_flags
        - is_owlette_agent
        - order
      properties:
        name: { type: string }
        software_name: { type: string }
        category: { type: string }
        description: { type: string, nullable: true }
        icon: { type: string, nullable: true }
        installer_name: { type: string }
        installer_url: { type: string }
        silent_flags: { type: string }
        verify_path: { type: string, nullable: true }
        close_processes:
          type: array
          items: { type: string }
        parallel_install: { type: boolean }
        is_owlette_agent: { type: boolean }
        timeout_seconds: { type: integer, nullable: true }
        order: { type: integer }

    SystemPreset:
      allOf:
        - $ref: '#/components/schemas/SystemPresetInput'
        - type: object
          properties:
            id: { type: string }
            createdBy: { type: string }
            createdAt: { type: string, format: date-time, nullable: true }
            updatedAt: { type: string, format: date-time, nullable: true }

    AlertRule:
      type: object
      required:
        - id
        - name
        - metric
        - operator
        - value
        - severity
        - channels
        - enabled
        - cooldownMinutes
      properties:
        id: { type: string }
        name: { type: string }
        metric: { type: string }
        operator: { type: string, enum: ['>', '<', '>=', '<='] }
        value: { type: number }
        severity: { type: string, enum: [info, warning, critical] }
        channels:
          type: array
          items: { type: string, enum: [email, webhook] }
        enabled: { type: boolean }
        cooldownMinutes: { type: number, minimum: 0 }

    InstallerVersion:
      type: object
      required: [version, download_url, checksum_sha256, file_size, uploaded_at, uploaded_by, release_date, deletedAt]
      properties:
        version:
          type: string
        download_url:
          type: string
          format: uri
          nullable: true
        checksum_sha256:
          type: string
          nullable: true
        release_notes:
          type: string
          nullable: true
        file_size:
          type: integer
          nullable: true
        uploaded_at:
          type: integer
          nullable: true
        uploaded_by:
          type: string
          nullable: true
        release_date:
          type: string
          format: date-time
          nullable: true
        deletedAt:
          type: integer
          nullable: true
        promoted_at:
          type: integer
          nullable: true
        promoted_by:
          type: string
          nullable: true

    McpTool:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        tier:
          type: integer
          enum: [1, 2, 3]
          description: "1 = read-only, 2 = actions, 3 = advanced"
        parameters:
          type: object
          description: JSON Schema for tool parameters

    # ── roost (project distribution v2) ─────────────────────────────
    DisplayMonitor:
      type: object
      description: |
        Monitor topology record reported by the agent and stored as a
        machine-level assigned layout. The agent remains the source of
        truth for vendor-specific fields; public callers should preserve
        unknown properties returned by display state tooling.
      additionalProperties: true
      properties:
        id: { type: string }
        deviceName: { type: string }
        primary: { type: boolean }
        position:
          type: object
          properties:
            x: { type: number }
            y: { type: number }
        width: { type: integer, minimum: 0 }
        height: { type: integer, minimum: 0 }
        scale: { type: number }
        orientation: { type: string }

    DisplayLayoutOperationResult:
      type: object
      required: [machineId, op]
      properties:
        machineId: { type: string }
        op:
          type: string
          enum: [capture, set_auto_restore, reset_breaker, set_remote_apply]

    ProblemDetails:
      type: object
      description: |
        RFC 7807 problem+json envelope returned by public API endpoints on
        error. This carries a stable machine-readable `type` URI, `code`,
        docs link, correlation `requestId`, and optional field-level
        `errors`.
      required: [type, title, status, code, docsUrl, requestId]
      properties:
        type:
          type: string
          format: uri
          description: Stable URI identifying the problem category.
          example: https://owlette.app/problems/validation-failed
        title:
          type: string
          description: Short, stable human-readable summary.
          example: validation failed
        status:
          type: integer
          description: HTTP status code, mirrors the response status.
          example: 400
        detail:
          type: string
          description: Occurrence-specific explanation; safe to show to users.
        instance:
          type: string
          description: URI reference for this occurrence (often the route path).
        requestId:
          type: string
          description: Correlation ID for trace lookup; quote in support tickets.
        code:
          type: string
          description: Stable machine-readable code for client branching.
          example: validation_failed
        docsUrl:
          type: string
          format: uri
          description: Documentation anchor for the error code.
          example: https://owlette.app/docs/api/errors#validation_failed
        param:
          type: string
          description: Optional offending header, query parameter, path parameter, or body field.
        errors:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
          description: |
            Field-level errors keyed by dotted JSON path (400/422 responses).
            Example: `{ "body.hashes": ["must be a non-empty array"] }`

    VersionSummary:
      type: object
      description: Entry in a roost's immutable version history.
      required: [versionId, versionNumber, createdAt, totalSize, totalFiles]
      properties:
        versionId:
          type: string
          description: Stable content-addressed id (`vrs_*`).
        versionNumber:
          type: integer
          description: Monotonic 1-indexed version number, per-roost.
        description:
          type: string
          nullable: true
          maxLength: 500
          description: |
            Optional plaintext "what changed?" note supplied at publish
            time or edited later via PATCH. Null when missing.
        versionUrl:
          type: string
          format: uri
          description: R2 URL of the version body (short-TTL signed).
        createdAt:
          type: string
          format: date-time
        createdBy:
          type: string
          description: UID of the user or agent that published this version.
        totalSize:
          type: integer
          format: int64
          description: Sum of all file sizes in bytes.
        totalFiles:
          type: integer
        parentVersionId:
          type: string
          nullable: true

    OciVersion:
      type: object
      description: |
        OCI Image v1.1 derivation — owlette surfaces this as "version". See
        [version-format.md](/docs/internal/version-format) for the
        full schema. This OpenAPI description captures the shape required
        by the finalize endpoint; the authoritative JSON schema lives in
        the internal docs.
      required: [schemaVersion, mediaType, config, files]
      properties:
        schemaVersion:
          type: integer
          enum: [2]
        mediaType:
          type: string
          enum: [application/vnd.owlette.version.v1+json]
        config:
          type: object
          required: [name, createdAt, createdBy, siteId]
          properties:
            name:
              type: string
            createdAt:
              type: string
              format: date-time
            createdBy:
              type: string
            siteId:
              type: string
        files:
          type: array
          items:
            type: object
            required: [path, size, chunks]
            properties:
              path:
                type: string
                description: |
                  POSIX-style relative path. Agents reject absolute paths,
                  `..`, `.`, empty segments, NUL bytes, drive letters, and
                  ADS colons.
              size:
                type: integer
                format: int64
                minimum: 0
              chunks:
                type: array
                items:
                  type: object
                  required: [hash, size]
                  properties:
                    hash:
                      type: string
                      pattern: '^[0-9a-f]{64}$'
                    size:
                      type: integer
                      minimum: 1

    # Public API schemas.

    # Common primitives.
    Permission:
      type: string
      enum: [read, write, deploy, rollback, admin]
      description: |
        Atomic permission within a scope entry. Permissions are exact;
        `write`, `deploy`, `rollback`, and `admin` do not imply `read`
        or any other permission.
    Scope:
      type: object
      description: |
        One entry on an api key's `scopes[]`. The set of scopes on a
        key is the union of access granted.
      required: [resource, id, permissions]
      properties:
        resource:
          type: string
          enum: [roost, site, machine, chat, deploy, process, user, installer]
          description: Resource type this entry grants against.
        id:
          type: string
          description: |
            Specific resource id or `*` to match any resource of the
            given type within the caller's own access. Platform resources
            `user` and `installer` must use `*`.
        permissions:
          type: array
          minItems: 1
          items: { $ref: '#/components/schemas/Permission' }

    Cursor:
      type: string
      description: |
        Opaque pagination cursor. Not a stable format — clients must
        round-trip the exact bytes without parsing.

    # api keys
    ApiKey:
      type: object
      required: [id, name, environment, keyPrefix, scopes, createdAt]
      properties:
        id:            { type: string }
        name:          { type: string, maxLength: 128 }
        environment:
          type: string
          enum: [live, test]
          nullable: true
          description: |
            `live` for every key minted now. `test` appears only on legacy keys
            issued before the option was withdrawn; they authenticate
            identically. Null on pre-scoping keys, which stored no environment.
        keyPrefix:
          type: string
          description: Display-safe prefix of the raw key; current keys store the first 15 characters.
        scopes:
          type: array
          items: { $ref: '#/components/schemas/Scope' }
        lastUsedAt:
          type: integer
          format: int64
          nullable: true
          description: Epoch milliseconds. Null until the key is first used.
        lastUsedIp:
          type: string
          nullable: true
        expiresAt:
          type: integer
          format: int64
          nullable: true
          description: Epoch milliseconds. Keys always expire (default 90d, max 365d).
        createdAt:
          type: integer
          format: int64
          nullable: true
          description: |
            Epoch milliseconds. Stored inconsistently — a Firestore Timestamp on
            keys created after the switch to `serverTimestamp()`, a number before
            it — and normalised by the route, so clients always receive a number.
        revokedAt:
          type: integer
          format: int64
          nullable: true
          description: Epoch milliseconds. Set on revocation.
        rotatedAt:
          type: integer
          format: int64
          nullable: true
          description: Epoch milliseconds. Set when this key was rotated.
        rotatedFromKeyId:
          type: string
          nullable: true
        retiresAt:
          type: integer
          format: int64
          nullable: true
          description: |
            Epoch milliseconds. When a rotated key stops working
            (`rotatedAt` + 24h grace).
        expiredMarkedAt:
          type: integer
          format: int64
          nullable: true
        expired:
          type: boolean
          description: |
            Derived server-side against the moment the list was built: true once
            `expiresAt` has passed. Matches the `<=` boundary the API enforces,
            so a key shown as expired is exactly a key the API will reject with
            `token_expired`.
        retired:
          type: boolean
          description: Derived. A rotated key whose grace window has closed.
        revoked:
          type: boolean
          description: |
            Derived from the presence of `revokedAt`, deliberately not from
            `revokedAt <= now`: the API rejects a key the instant the field is set,
            so a future-dated stamp is already dead. Terminal — it outranks
            `expired` and `retired`, matching the order the API checks them in.
    ApiKeyWithSecret:
      description: |
        Returned **only** on create or rotate — the one and only time
        the raw `key` value is exposed. Subsequent reads return
        `keyPrefix` only.
      allOf:
        - $ref: '#/components/schemas/ApiKey'
        - type: object
          required: [key]
          properties:
            key:
              type: string
              description: Raw api key (`owk_live_*` or `owk_test_*`). Store immediately.
            previousKeyExpiresAt:
              type: string
              format: date-time
              nullable: true
              description: Rotation grace window end (rotate only; null on create).
            rotatedAt:
              type: string
              format: date-time
              nullable: true
              description: Timestamp of the rotation (rotate only; null on create).
    ApiKeyCreateResponse:
      type: object
      required: [success, key, keyId, name, environment, scopes, expiresAt, keyPrefix]
      properties:
        success: { type: boolean }
        key:
          type: string
          description: Raw api key (`owk_live_*` or `owk_test_*`). Store immediately.
        keyId: { type: string }
        name: { type: string }
        environment:
          type: string
          enum: [live, test]
        scopes:
          type: array
          items: { $ref: '#/components/schemas/Scope' }
        expiresAt:
          type: integer
          format: int64
          description: Unix timestamp in milliseconds.
        keyPrefix: { type: string }
    ApiKeyRotateResponse:
      allOf:
        - $ref: '#/components/schemas/ApiKeyCreateResponse'
        - type: object
          required: [rotatedFromKeyId, previousKey]
          properties:
            rotatedFromKeyId: { type: string }
            previousKey:
              type: object
              required: [keyId, retiresAt]
              properties:
                keyId: { type: string }
                retiresAt:
                  type: integer
                  format: int64
                  description: Unix timestamp in milliseconds when the old key stops working.
    CreateApiKeyRequest:
      type: object
      required: [name, scopes]
      properties:
        name:          { type: string, maxLength: 128 }
        environment:
          type: string
          enum: [live, test]
          default: live
          deprecated: true
          description: |
            Ignored. Every key is minted `live`. Still accepted so shipped
            clients that send it are not broken.
        scopes:
          type: array
          minItems: 1
          items: { $ref: '#/components/schemas/Scope' }
        ttlDays:
          type: integer
          minimum: 1
          maximum: 365
          default: 90
    PatchApiKeyRequest:
      type: object
      minProperties: 1
      description: |
        At least one of `name` and `scopes` is required; sending neither
        returns 400. `scopes` replaces the key's scope list outright — it
        is not merged with what is already there.

        Scopes may be broadened as well as narrowed, subject to the same
        grant rules as key creation: `user` and `installer` scopes require
        superadmin, and platform resources must use `id: "*"`.
      properties:
        name:   { type: string, maxLength: 128 }
        scopes:
          type: array
          minItems: 1
          items: { $ref: '#/components/schemas/Scope' }
    RotateApiKeyRequest:
      type: object
      properties:
        ttlDays:
          type: integer
          minimum: 1
          maximum: 365
          default: 90

    # roosts
    RoostSummary:
      type: object
      required: [id, siteId, name, targets, totalSize, totalFiles, updatedAt]
      properties:
        id:       { type: string }
        siteId:   { type: string }
        name:     { type: string }
        targets:
          type: array
          items: { type: string }
        currentVersionId:
          type: string
          nullable: true
          pattern: '^[0-9a-f]{64}$'
        previousVersionId:
          type: string
          nullable: true
          pattern: '^[0-9a-f]{64}$'
        totalSize:
          type: integer
          format: int64
          minimum: 0
        totalFiles:
          type: integer
          minimum: 0
        updatedAt:
          type: string
          format: date-time
    Roost:
      description: Full roost detail. Extends `RoostSummary` with `createdAt`.
      allOf:
        - $ref: '#/components/schemas/RoostSummary'
        - type: object
          required: [createdAt]
          properties:
            createdAt:
              type: string
              format: date-time
    CreateRoostRequest:
      type: object
      required: [siteId, name]
      properties:
        siteId:  { type: string }
        name:
          type: string
          maxLength: 128
          description: Display name. Must be unique within the site.
        targets:
          type: array
          items: { type: string }
          description: Machine ids to target; may be empty at create time.
    PatchRoostRequest:
      type: object
      properties:
        name:
          type: string
          maxLength: 128
        targets:
          type: array
          items: { type: string }

    # versions
    VersionFile:
      type: object
      required: [path, digest, size]
      properties:
        path:   { type: string }
        digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        size:
          type: integer
          format: int64
          minimum: 0
    Version:
      description: |
        Full oci-style version body. Roost emits schemaVersion 2,
        mediaType `application/vnd.owlette.version.v1+json`. See
        [versions.md](/docs/api/versions).
      type: object
      required: [schemaVersion, mediaType, config, layers]
      properties:
        schemaVersion:
          type: integer
          enum: [2]
        mediaType:
          type: string
          enum: [application/vnd.owlette.version.v1+json]
        config:
          type: object
          required: [mediaType, digest, size]
          properties:
            mediaType: { type: string }
            digest:
              type: string
              pattern: '^sha256:[0-9a-f]{64}$'
            size:
              type: integer
              format: int64
              minimum: 0
        layers:
          type: array
          items:
            type: object
            required: [mediaType, path, digest, size]
            properties:
              mediaType: { type: string }
              path:      { type: string }
              digest:
                type: string
                pattern: '^sha256:[0-9a-f]{64}$'
              size:
                type: integer
                format: int64
                minimum: 0
    VersionDetail:
      description: Version metadata wrapper returned on GET.
      type: object
      required: [id, versionNumber, publishedAt, totalSize, totalFiles, isCurrent, version]
      properties:
        id:
          type: string
          description: Stable content-addressed id (64-char sha-256 hex).
        versionNumber:
          type: integer
          description: Monotonic 1-indexed version number, per-roost.
        description:
          type: string
          nullable: true
          maxLength: 500
          description: |
            Optional plaintext "what changed?" note. Editable via PATCH.
        publishedAt:
          type: string
          format: date-time
        publishedBy:
          type: string
          description: User or api key id that published this version.
        totalSize:
          type: integer
          format: int64
        totalFiles:
          type: integer
        isCurrent:
          type: boolean
        version: { $ref: '#/components/schemas/Version' }
    DiffResult:
      type: object
      required: [from, to, added, removed, modified]
      properties:
        from:
          type: string
          pattern: '^[0-9a-f]{64}$'
        to:
          type: string
          pattern: '^[0-9a-f]{64}$'
        added:
          type: array
          items: { $ref: '#/components/schemas/VersionFile' }
        removed:
          type: array
          items: { $ref: '#/components/schemas/VersionFile' }
        modified:
          type: array
          items:
            type: object
            required: [path, digestBefore, digestAfter, sizeBefore, sizeAfter]
            properties:
              path: { type: string }
              digestBefore:
                type: string
                pattern: '^sha256:[0-9a-f]{64}$'
              digestAfter:
                type: string
                pattern: '^sha256:[0-9a-f]{64}$'
              sizeBefore:
                type: integer
                format: int64
              sizeAfter:
                type: integer
                format: int64

    # chunks
    ChunkReferrer:
      type: object
      required: [entryId, source]
      properties:
        entryId: { type: string }
        source:
          type: string
          enum: [version, mount]
        roostId:
          type: string
          nullable: true
        fromRoostId:
          type: string
          nullable: true
        toRoostId:
          type: string
          nullable: true
        versionId:
          type: string
          nullable: true
          pattern: '^[0-9a-f]{64}$'
        versionNumber:
          type: integer
          nullable: true
        fileCount:
          type: integer
          nullable: true
          minimum: 1
          description: How many files in that version reference this chunk.
        referencedAt:
          type: string
          format: date-time
          nullable: true
    MountChunkRequest:
      type: object
      required: [siteId, toRoostId]
      properties:
        siteId:    { type: string }
        toRoostId: { type: string }

    # deployments
    MachineDeploymentState:
      type: object
      required: [machineId, state]
      properties:
        machineId: { type: string }
        role:
          type: string
          enum: [canary, fleet]
        state:
          type: string
          enum: [queued, in_progress, succeeded, failed, cancelled, simulated]
        syncStartedAt:
          type: string
          format: date-time
          nullable: true
        syncCompletedAt:
          type: string
          format: date-time
          nullable: true
        bytesTransferred:
          type: integer
          format: int64
          minimum: 0
        error:
          type: string
          nullable: true
    DeployRequest:
      type: object
      required: [siteId, versionId]
      properties:
        siteId: { type: string }
        versionId:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        machines:
          type: array
          items: { type: string }
          description: Subset of the roost's targets. Omit to target all.
        scheduleAt:
          type: string
          format: date-time
          nullable: true
          description: Future timestamp to queue the rollout for.
        dryRun:
          type: boolean
          default: false
        strategy:
          type: string
          enum: [all-at-once, canary-then-fleet, canary-only]
          default: all-at-once
    DeploymentPlan:
      description: |
        Initial rollout response (202 Accepted). For ongoing status
        polling, call `GET /api/roosts/{roostId}/deployments/{rolloutId}`.
      type: object
      required: [rolloutId, roostId, versionId, strategy, dryRun, createdAt, machines]
      properties:
        rolloutId: { type: string }
        roostId:   { type: string }
        versionId:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        strategy:
          type: string
          enum: [all-at-once, canary-then-fleet, canary-only]
        dryRun:    { type: boolean }
        scheduledAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        machines:
          type: array
          items: { $ref: '#/components/schemas/MachineDeploymentState' }
    DeploymentStatus:
      description: |
        Current state snapshot for a rollout — used by both the list
        endpoint and the detail endpoint (detail populates
        `machines[]`; list omits them to keep payloads small).
      type: object
      required: [rolloutId, roostId, versionId, strategy, state, startedAt]
      properties:
        rolloutId: { type: string }
        roostId:   { type: string }
        versionId:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        strategy:
          type: string
          enum: [all-at-once, canary-then-fleet, canary-only]
        state:
          type: string
          enum: [queued, in_progress, succeeded, failed, cancelled]
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true
        machineCount:
          type: integer
          minimum: 0
        successCount:
          type: integer
          minimum: 0
        failureCount:
          type: integer
          minimum: 0
        machines:
          type: array
          items: { $ref: '#/components/schemas/MachineDeploymentState' }
          description: Populated on detail responses only.
    DryRunResult:
      description: |
        Returned when `DeployRequest.dryRun: true`. Same shape as
        `DeploymentPlan` but every machine is in `state: simulated`
        and no agent is contacted. Included as a distinct schema for
        sdk code-gen clarity.
      allOf:
        - $ref: '#/components/schemas/DeploymentPlan'
    InstallerDeploymentTarget:
      type: object
      required: [machineId, status]
      properties:
        machineId: { type: string }
        status:
          type: string
          enum:
            - pending
            - closing_processes
            - downloading
            - installing
            - completed
            - failed
            - cancelled
            - uninstalled
        error:
          type: string
          nullable: true
        cancelledAt:
          type: string
          format: date-time
          nullable: true
        retriedAt:
          type: string
          format: date-time
          nullable: true
      additionalProperties: true
    InstallerDeploymentSummary:
      type: object
      required:
        - id
        - name
        - installer_name
        - installer_url
        - silent_flags
        - verify_path
        - sha256_checksum
        - parallel_install
        - targets
        - status
        - createdAt
        - completedAt
        - updatedAt
      properties:
        id: { type: string }
        name: { type: string }
        installer_name: { type: string }
        installer_url:
          type: string
          format: uri
        silent_flags: { type: string }
        verify_path:
          type: string
          nullable: true
        sha256_checksum:
          type: string
          nullable: true
        parallel_install: { type: boolean }
        targets:
          type: array
          items: { $ref: '#/components/schemas/InstallerDeploymentTarget' }
        status:
          type: string
          enum:
            - pending
            - in_progress
            - completed
            - failed
            - partial
            - partial_failed
            - cancelled
            - uninstalling
            - uninstalled
        createdAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
          nullable: true
    InstallerDeploymentDetail:
      allOf:
        - $ref: '#/components/schemas/InstallerDeploymentSummary'
        - type: object
          required: [siteId]
          properties:
            siteId: { type: string }

    # sites
    SiteSummary:
      type: object
      required: [id, name]
      properties:
        id:       { type: string }
        name:     { type: string }
        plan:
          type: string
          enum: [free, starter, pro, enterprise]
          nullable: true
        timezone:
          type: string
          nullable: true
          description: IANA tz name.
        owner:
          type: string
          nullable: true
          description: User id that owns the site.
        createdAt:
          type: string
          format: date-time
          nullable: true
        schedulesFollowSiteTime:
          type: boolean
          description: |
            True when process schedules are evaluated in the site's `timezone`
            rather than each machine's own clock. Effective value: a site that
            has never been asked reports `false`, the same as one that declined.
    SiteDetail:
      description: Site detail currently shares the live `SiteSummary` shape.
      allOf:
        - $ref: '#/components/schemas/SiteSummary'

    # machines
    MachineSummary:
      type: object
      required: [id, name, online]
      properties:
        id:     { type: string }
        name:   { type: string }
        online: { type: boolean }
        lastHeartbeat:
          type: string
          format: date-time
          nullable: true
        agentVersion:
          type: string
          nullable: true
        os:
          type: string
          nullable: true
        currentRoosts:
          type: array
          items:
            type: object
            required: [roostId, name, currentVersionId, versionCounter]
            properties:
              roostId: { type: string }
              name: { type: string }
              currentVersionId:
                type: string
                nullable: true
              versionCounter:
                type: integer
                minimum: 0
    MachineDetail:
      description: Full machine detail; extends `MachineSummary` with telemetry.
      allOf:
        - $ref: '#/components/schemas/MachineSummary'
        - type: object
          properties:
            siteId: { type: string }
            hostname:
              type: string
              nullable: true
            metrics:
              type: object
              nullable: true
              additionalProperties: true
            processes:
              type: array
              items:
                type: object
                additionalProperties: true
    MachineDeployment:
      description: Current state for one roost that targets a machine.
      type: object
      required: [roostId, name, currentVersionId, previousVersionId, versionCounter, extractPath, reportedVersionId, reportedStatus, reportedAt]
      properties:
        roostId: { type: string }
        name: { type: string }
        currentVersionId:
          type: string
          nullable: true
        previousVersionId:
          type: string
          nullable: true
        versionCounter:
          type: integer
          minimum: 0
        extractPath:
          type: string
          nullable: true
        reportedVersionId:
          type: string
          nullable: true
        reportedStatus:
          type: string
          nullable: true
        reportedAt:
          type: string
          format: date-time
          nullable: true
    RemoveMachineResult:
      type: object
      required: [siteId, machineId, deleted]
      properties:
        siteId: { type: string }
        machineId: { type: string }
        deleted:
          type: object
          required: [machine, config, pendingCommands, completedCommands]
          properties:
            machine: { type: string }
            config: { type: string }
            pendingCommands: { type: string }
            completedCommands: { type: string }

    # quotas
    Quota:
      type: object
      required: [siteId, usedBytes, pendingBytes, committedBytes, limitBytes, fractionUsed, lastAlarmLevel, alarms]
      properties:
        siteId: { type: string }
        limitBytes:
          type: integer
          format: int64
          minimum: 0
          nullable: true
          description: |
            Included storage per site, in bytes. A per-site grant recorded on
            the quota doc takes precedence over the standard allowance.
        usedBytes:
          type: integer
          format: int64
          minimum: 0
        pendingBytes:
          type: integer
          format: int64
          minimum: 0
          description: Bytes reserved by in-flight uploads but not yet persisted.
        committedBytes:
          type: integer
          format: int64
          minimum: 0
          description: "`usedBytes + pendingBytes`."
        fractionUsed:
          type: number
          nullable: true
          minimum: 0
          maximum: 1
          description: |
            `committedBytes / limitBytes`, capped at 1. `null` when the site
            has no storage allowance — there is no ratio to take.
        lastAlarmLevel:
          type: number
        lastAlarmAt:
          type: string
          format: date-time
          nullable: true
        lastReconciledAt:
          type: string
          format: date-time
          nullable: true
        alarms:
          type: array
          items:
            type: object
            required: [id, threshold, firedAt]
            properties:
              id: { type: string }
              threshold:
                type: number
                nullable: true
                minimum: 0
                maximum: 1
                description: Fraction of `limitBytes` at which this alarm fired.
              firedAt:
                type: string
                format: date-time
                nullable: true
    QuotaHistoryEntry:
      type: object
      required: [date, storageBytesAvg, classAOps, classBOps, egressBytes]
      properties:
        date:
          type: string
          format: date
          description: UTC calendar date (YYYY-MM-DD).
        storageBytesAvg:
          type: integer
          format: int64
          minimum: 0
          nullable: true
        classAOps:
          type: integer
          minimum: 0
        classBOps:
          type: integer
          minimum: 0
        egressBytes:
          type: integer
          format: int64
          minimum: 0

    # operational logs
    SiteLogEntry:
      type: object
      required: [id, siteId, timestamp, action, level, machineId, machineName, processName, details, userId, screenshotUrl]
      properties:
        id: { type: string }
        siteId: { type: string }
        timestamp:
          type: string
          format: date-time
          nullable: true
        action:
          type: string
          description: Event action, e.g. `process_crash` or `agent_started`.
        level:
          type: string
          enum: [debug, info, warning, error, critical]
        machineId:
          type: string
          nullable: true
        machineName:
          type: string
          nullable: true
        processName:
          type: string
          nullable: true
        details:
          oneOf:
            - type: string
            - type: object
              additionalProperties: true
          nullable: true
        userId:
          type: string
          nullable: true
        screenshotUrl:
          type: string
          format: uri
          nullable: true

    # audit log
    AuditRecord:
      type: object
      required: [hash, kind, actor, siteId, target, occurredAt, recordedAt, attributes]
      properties:
        hash:
          type: string
          pattern: '^[0-9a-f]{64}$'
        kind:
          type: string
          enum:
            - signed_url_issued
            - distribution_started
            - version_pointer_changed
            - api_key_used
            - gc_run
            - api_key_mutated
            - chunk_mutated
            - deployment_mutated
            - distribution_mutated
            - process_mutated
            - roost_mutated
            - machine_command_dispatched
            - user_mutated
            - site_mutated
            - site_member_mutated
            - installer_mutated
            - webhook_mutated
            - chat_mutated
            - billing_mutated
            - talon_mutated
        actor:
          type: string
          description: Actor identifier such as `user:<uid>` or `apiKey:<keyId>`.
        siteId: { type: string }
        target:
          type: string
          nullable: true
          description: Optional mutated resource id for mutation audit records.
        occurredAt:
          type: integer
          format: int64
          nullable: true
        recordedAt:
          type: integer
          format: int64
          nullable: true
        attributes:
          type: object
          description: |
            Kind-specific structured detail. Additional properties
            depend on `kind`; treat unknown keys as forward-compatible.
          additionalProperties: true
    AuditRecordDetail:
      type: object
      required: [siteId, hash, previousHash, recordedAt, event, verification]
      properties:
        siteId: { type: string }
        hash:
          type: string
          pattern: '^[0-9a-f]{64}$'
        previousHash:
          type: string
          pattern: '^[0-9a-f]{64}$'
        recordedAt:
          type: integer
          format: int64
        event:
          type: object
          required: [kind, siteId, actor, occurredAt, attributes]
          properties:
            kind:
              type: string
              enum:
                - signed_url_issued
                - distribution_started
                - version_pointer_changed
                - api_key_used
                - gc_run
                - api_key_mutated
                - chunk_mutated
                - deployment_mutated
                - distribution_mutated
                - process_mutated
                - roost_mutated
                - machine_command_dispatched
                - user_mutated
                - site_mutated
                - site_member_mutated
                - installer_mutated
                - webhook_mutated
                - chat_mutated
                - billing_mutated
                - talon_mutated
            siteId: { type: string }
            actor: { type: string }
            target:
              type: string
              description: Optional mutated resource id for mutation audit records.
            occurredAt:
              type: integer
              format: int64
            attributes:
              type: object
              additionalProperties: true
        verification:
          type: object
          required: [ok, hashValid, linkageValid, isGenesis, predecessorPresent, reason]
          properties:
            ok: { type: boolean }
            hashValid: { type: boolean }
            linkageValid:
              type: boolean
              nullable: true
            isGenesis: { type: boolean }
            predecessorPresent: { type: boolean }
            reason:
              type: string
              nullable: true

    # webhooks
    WebhookDeliveryStatus:
      type: string
      enum: [pending, succeeded, failed]
    WebhookSubscription:
      description: |
        Base webhook subscription fields. Most reads return the
        `Webhook` wrapper which extends this with activity pointers;
        `WebhookWithSecret` extends with the one-time raw secret.
      type: object
      required: [id, url, events, paused, createdAt]
      properties:
        id:          { type: string }
        siteId:      { type: string }
        url:
          type: string
          format: uri
          description: Must be https — no private / loopback ips.
        events:
          type: array
          minItems: 1
          items: { type: string }
        description:
          type: string
          nullable: true
        paused:
          type: boolean
          default: false
        createdAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
          nullable: true
    WebhookDetail:
      description: |
        Webhook subscription wrapper with activity pointers. Returned
        on `GET /api/webhooks` and `GET /api/webhooks/{webhookId}`.
      allOf:
        - $ref: '#/components/schemas/WebhookSubscription'
        - type: object
          properties:
            lastDeliveryAt:
              type: string
              format: date-time
              nullable: true
            lastDeliveryOk:
              type: string
              nullable: true
              deprecated: true
            lastDeliveryStatus:
              type: string
              nullable: true
            consecutiveFailures:
              type: integer
              minimum: 0
              deprecated: true
            failureCount:
              type: integer
              minimum: 0
    WebhookWithSecret:
      description: |
        Returned **only** on webhook create and `rotate-secret`. The
        `signingSecret` is the full hmac secret — store it immediately.
      allOf:
        - $ref: '#/components/schemas/WebhookSubscription'
        - type: object
          required: [signingSecret]
          properties:
            signingSecret:
              type: string
              description: HMAC signing secret (`whsec_*`). Shown once.
            previousSecretExpiresAt:
              type: string
              format: date-time
              nullable: true
              deprecated: true
            previousSecretValidUntil:
              type: string
              format: date-time
              nullable: true
            gracePeriodHours:
              type: integer
            rotatedAt:
              type: string
              format: date-time
              nullable: true
    CreateWebhookRequest:
      type: object
      required: [url, events]
      properties:
        url:
          type: string
          format: uri
          description: Must be https — no private ips; reachable during create-time smoke test.
        events:
          type: array
          minItems: 1
          items: { type: string }
        description:
          type: string
          maxLength: 500
    PatchWebhookRequest:
      type: object
      properties:
        url:
          type: string
          format: uri
        events:
          type: array
          items: { type: string }
        description:
          type: string
          maxLength: 500
          nullable: true
        paused:
          type: boolean
    WebhookDelivery:
      type: object
      required: [id, event, state, attempt, createdAt, nextAttemptAt]
      properties:
        id: { type: string }
        webhookId:  { type: string }
        siteId: { type: string }
        event:      { type: string }
        state:      { $ref: '#/components/schemas/WebhookDeliveryStatus' }
        lastStatus:
          type: integer
          nullable: true
          description: HTTP status the target returned. Null if the attempt never connected.
        attempt:
          type: integer
          minimum: 0
        createdAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        nextRetryAt:
          type: string
          format: date-time
          nullable: true
          deprecated: true
        nextAttemptAt:
          type: string
          format: date-time
          nullable: true
        lastError:
          type: string
          nullable: true
        request:
          type: object
          description: Populated on detail responses only.
          properties:
            url:     { type: string, format: uri }
            headers:
              type: object
              additionalProperties: { type: string }
            body:
              type: string
              description: Raw canonical event payload.
        response:
          type: object
          description: Populated on detail responses only; body truncated to 4 KiB.
          properties:
            status:  { type: integer }
            headers:
              type: object
              additionalProperties: { type: string }
            body:      { type: string }
            durationMs: { type: integer, minimum: 0 }
    WebhookProbeRequest:
      type: object
      required: [url, event]
      properties:
        url:
          type: string
          format: uri
        event:
          type: string
          description: Known webhook event name.
        payload:
          type: object
          additionalProperties: true
        signingSecret:
          type: string
          description: Optional secret to hmac-sign the synthetic probe payload with. If omitted, the server returns a generated test secret.

    # utility
    WhoAmIResponse:
      type: object
      required: [userId, email, role, key, rateLimit, quota, primarySiteId]
      properties:
        userId:
          type: string
          description: Authenticated Owlette user id.
        email:
          type: string
          format: email
          nullable: true
        role:
          type: string
          nullable: true
        key:
          type: object
          nullable: true
          description: Populated when the request authenticated with an API key.
          required: [keyId, name, keyPrefix, scopes, environment, expiresAt, lastUsedAt, isLegacy]
          properties:
            keyId:
              type: string
              nullable: true
            name:
              type: string
              nullable: true
            keyPrefix:
              type: string
              nullable: true
            scopes:
              type: array
              nullable: true
              items: { $ref: '#/components/schemas/Scope' }
            environment:
              type: string
              enum: [live, test]
              nullable: true
            expiresAt:
              type: integer
              nullable: true
              description: Unix timestamp in milliseconds.
            lastUsedAt:
              type: integer
              nullable: true
              description: Unix timestamp in milliseconds.
            isLegacy:
              type: boolean
        rateLimit:
          type: object
          required: [tier, limitPerMinute, note]
          properties:
            tier: { type: string }
            limitPerMinute: { type: integer }
            note: { type: string }
        quota:
          type: object
          nullable: true
          properties:
            siteId:
              type: string
            usedBytes:
              type: integer
              format: int64
            pendingBytes:
              type: integer
              format: int64
            limitBytes:
              type: integer
              format: int64
              nullable: true
        primarySiteId:
          type: string
          nullable: true
    VersionResponse:
      type: object
      required: [current, supported, deprecated, retired]
      properties:
        current:
          type: string
          pattern: '^\d{4}-\d{2}-\d{2}$'
        supported:
          type: array
          items:
            type: string
            pattern: '^\d{4}-\d{2}-\d{2}$'
        deprecated:
          type: array
          items:
            type: string
            pattern: '^\d{4}-\d{2}-\d{2}$'
        retired:
          type: array
          items:
            type: string
            pattern: '^\d{4}-\d{2}-\d{2}$'
