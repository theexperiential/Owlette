# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# Playwright E2E — outputs clustered under e2e/.output/ via playwright.config.ts
/e2e/.output/
/playwright/.cache/
/e2e/fixtures/
/e2e/.auth/
/e2e/debug/

# Live dev smoke suite (playwright.live.config.ts): signed-in role states, run output
/e2e-live/.auth/
/e2e-live/.output/

# next.js — glob covers .next, .next-e2e, and any ad-hoc .next-* probe
# dirs (e.g. .next-dev-probe used when debugging hydration in dev mode)
/.next*/
/out/

# fumadocs generated source
/.source

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
