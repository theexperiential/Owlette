# api keys consolidation — Plan
**Created**: 2026-08-17 | **Status**: COMPLETE 2026-09-05 — all five waves shipped (waves 4+5 in `8e5afdcc`). Ops residuals, none blocking: register `/api/cron/api-key-expiry` on cron-job.org (0 8 * * *, dev AND prod, per-env CRON_SECRET) — **ORDER MATTERS on prod: deploy firestore indexes to prod FIRST** (`npx firebase deploy --only firestore:indexes --project owlette-prod-90a12`; the scan needs the api_keys.expiresAt COLLECTION_GROUP field override added in 02371bef, or the route 500s — proven live on dev 2026-09-05, fixed same day); `firebase deploy --only functions` for the revived expiredMarkedAt sweep + decide the one-off backfill for pre-change keys; SDK `ApiKey` types lack the new `revoked` field (release-bearing, deferred deliberately).

## Summary

The api-key feature is currently split across two surfaces that are not two implementations of one thing — they are one feature with a degraded copy. `web/components/AccountSettingsDialog.tsx:1140-1401` can create with a preset and revoke; it cannot set a ttl, build custom scopes, rotate, or even display what a key is scoped to. `web/app/settings/api-keys/` (page.tsx + CreateKeyDialog.tsx + KeyCard.tsx) can do all of it. Neither has a shared data layer — both are raw `fetch()` against `/api/keys` with no hook in `web/hooks/`.

This plan collapses them into **one organised panel** — an extracted `ApiKeysManager` mounted in the account dialog's `api` section and re-used by a thin `/settings/api-keys` wrapper — and lands four behaviour changes on top: always mint `live` (drop the test option), a `PATCH /api/keys/{keyId}` so scopes are editable, revoke as a soft delete instead of a hard delete of two docs, and a daily expiry-notice email.

Everything here has an in-repo precedent to copy: `PasskeyManager` + `usePasskeys` for the extraction, `api/account/api-keys/route.ts:108` for live-only minting, `webhooks/[webhookId]/route.ts:120-271` for the PATCH shape, `lib/userDeleteCascade.server.ts:161-191` for soft revoke, and `api/cron/health-check/route.ts` for the expiry cron.

## Settled — do not relitigate

1. **Always mint live.** The environment selector is removed from every UI. `environment` is *not* cosmetic — it is the second component of the idempotency cache key (`web/lib/idempotency.ts:10,41,135,261-267`) — so the union `'live' | 'test'` **stays**, no data is migrated, and legacy `owk_test_*` keys keep working forever (auth never reads the field: `apiAuth.server.ts:232-238`).
2. **Server is the enforcement point**, not the UI — SDK and CLI callers get the same guarantee. Incoming `environment` is **tolerated and ignored**, never a 400: `@owlette/cli` v1.0.0-rc.0, `sdks/node/src/resources/keys.ts:46` and `sdks/python/roost/resources/keys.py:77` all still send it.
3. **Rotate keeps inheriting the old key's environment** (`api/keys/[keyId]/rotate/route.ts:128-129`). It is published contract (`openapi.yaml:2509`) and flipping it would re-namespace idempotency mid-flight. Deliberate, documented in the code comment.
4. **The account dialog's `api` section is the primary surface.** `/settings/api-keys` **survives** as a ~30-line wrapper: 9 e2e navigations across 5 spec files, 3 docs references, and `e2e/COVERAGE.md:44` point at the route, and nothing today deep-links a dialog section by URL (`initialSection` is a prop, `AccountSettingsDialog.tsx:79`), so a redirect would require building query-param→section plumbing first.
5. **No client Firestore reads for keys, ever.** `users/{uid}/api_keys` and `api_keys/{keyHash}` are Admin-SDK-only. All reads stay on `GET /api/keys`.
6. **No `firestore.rules` change in any workstream.** Verified per workstream: prefs are allowlisted wholesale (`firestore.rules:653-662`), `users/{uid}/api_keys/{keyId}` is read-own / write-denied and already names `revokedAt` (`:701-708`), `api_keys/{keyHash}` is deny-all (`:712-714`), and `environment` appears only in a descriptive comment (`:703`). Any proposal implying a rules edit is a red flag, not a task.
7. **`revoked` derives as `revokedAt !== null`**, never `<= now` — `apiAuth.server.ts:199` is a plain truthiness check, so a future-dated stamp already 401s.
8. **All copy lowercase**, `lucide-react` only, theme tokens only.

## Approach

### A. Consolidate the UI (Wave 2)

- New `web/hooks/useApiKeys.ts` exposing `{ keys, loading, refresh, createKey, rotateKey, revokeKey }` — modelled on `web/hooks/usePasskeys.ts:9` (fetch-based, not Firestore). It replaces six inline fetch sites: `settings/api-keys/page.tsx:29-48`, `AccountSettingsDialog.tsx:203-214/:1225-1248/:1334-1352`, `CreateKeyDialog.tsx:103-129`, `KeyCard.tsx:85-104/:106-124`.
- New `web/components/ApiKeysManager.tsx` with a `compact` prop — the exact `PasskeyManager.tsx:26` shape, mounted `compact` in the dialog (`AccountSettingsDialog.tsx:1133-1135` is the precedent one section above) and full-size on the page.
- Create moves in as **inline disclosure, not a nested modal**. Width is not a constraint: the dialog gives the scope builder ~656px (`sm:max-w-4xl` 896 − 192 sidebar − 48 padding) vs CreateKeyDialog's ~528px today.
- Panel order: header → reveal banner (fed by **both** create and rotate) → your keys → create key → usage snippet.
- Four hook-level divergences settled: revoke **refetches** (not local filter); the dialog's expired/retired-last **sort wins**; `CreateKeyDialog`'s client-side **name validation wins**; `now` comes from the existing `web/hooks/useMinuteTick.ts` singleton so "expiring soon" and "rotated (grace)" go live.

### B. Remove the test environment (Wave 1)

One exported constant `MINTED_API_KEY_ENVIRONMENT = 'live'` in `web/lib/apiKeyTypes.ts`, imported by the two mint routes that still branch (`api/keys/route.ts:189`, `api/cli/device-code/authorize/route.ts:246`). `api/account/api-keys/route.ts:108` already hardcodes live — it is the precedent. Three UI surfaces, not two: `CreateKeyDialog.tsx:192-208`, `app/cli/authorize/page.tsx:141-157`, and the account dialog (which never had a selector). OpenAPI request enums are marked `deprecated: true` but **keep `[live, test]`**; response enums keep both values permanently.

### C. Editable scopes — `PATCH /api/keys/{keyId}` (Wave 3)

Extract `validateScopes` + the grantability gates out of `api/keys/route.ts` into `web/app/api/keys/_shared.ts` so POST and PATCH cannot drift, then add PATCH beside DELETE in `api/keys/[keyId]/route.ts`. The write is a `db.batch()` touching **both** docs — the same shape as `rotate/route.ts:142-184`. Full-array replacement semantics, no merge, `[]` rejected.

### D. Soft-delete revoke (Wave 4)

Stamp `revokedAt` instead of deleting, on **both** revoke routes. The auth path already handles it (`apiAuth.server.ts:199-201` checks `revokedAt` *before* `retiresAt` and `expiresAt`, and the lookup is read live with no cache), so this needs **zero auth-path changes**, fails closed just as immediately as a delete, keeps the keyHash→userId mapping attributable for post-revocation replay, and preserves the documented rollback at `docs/runbooks/upgrade-2.12.0.md:556`.

### E. Expiry alerts (Wave 5)

A daily `GET /api/cron/api-key-expiry` on the `health-check` scan-decide-stamp shape (not the digest queue-drain shape — there is no queue here). T-14 / T-3 / T-0 ladder, one email per owner listing all their expiring keys, notice state stamped **before** send on the user-subcollection doc only.

## Waves

### Wave 1 — always mint live *(server + 2 selectors; no data migration)* — **SHIPPED** `de944fd`

1. **Add the mint constant.** `web/lib/apiKeyTypes.ts` — export `MINTED_API_KEY_ENVIRONMENT: ApiKeyEnvironment = 'live'` near the union at `:12`. **Do not narrow the union** — it would break `buildApiKeyListItem`'s `=== 'test'` branch (`:248-249`), `e2e/helpers/apiKey.ts:57`, `e2e/specs/api-sprint/installer-deployments.spec.ts:5`, `public-api-smoke.spec.ts:103`, and the unions in `lib/idempotency.ts:41` / `lib/auditLogClient.ts:25,71`.
2. **`POST /api/keys`.** Delete `VALID_ENVIRONMENTS` (`route.ts:45`) and the parse block (`:170-176`); keep `environment?: unknown` on the body with an accepted-and-ignored comment. Update the jsdoc at `:101-107`.
3. **`POST /api/cli/device-code/authorize`.** Identical edit — `:70`, `:190-201`, `:246`. No CLI change needed: `cli/src/config.ts:143` and `credentialStore.ts:183` accept either value and nothing branches on it.
4. **Remove both selectors.** `CreateKeyDialog.tsx:57,68,110,192-208` (collapse the `grid-cols-2` so ttl doesn't sit orphaned) and `app/cli/authorize/page.tsx:38,69,141-157`. Drop the now-unused `ApiKeyEnvironment` type imports (`:33`, `:24`) or eslint flags them.
5. **KeyCard badge → legacy marker.** `KeyCard.tsx:134` becomes `apiKey.environment === 'test' &&`, keeping only the amber branch (`:141`, already a token). Once live is the only mint, a "live" badge is noise — but the badge is the *only* place a legacy test key is visible.
6. **Rotate: document the inheritance.** Extend the comment at `rotate/route.ts:123-127` one sentence so nobody "fixes" it.
7. **`web/openapi.yaml`.** `deprecated: true` + "ignored" description on the request-side `environment` (`:5418-5421`, `:8983-8986`), enums untouched. Prose at `:13-14`, `:141`, `:7193-7198`, `:8930`, `:8948`. Fold in the pre-existing bug: `ApiKey.environment` is `required`/non-nullable at `:8850,:8854` but `buildApiKeyListItem` returns `null` for pre-scoping records — add `nullable: true`, copying `:5484`.
8. **Docs.** Delete the "choose live or test" bullet (`content/docs/api/authentication.mdx:44-49`); relabel the `owk_test_` prefix row (`:29-32`) "legacy — no longer issued; existing keys remain valid"; swap `owk_test_` examples to `owk_live_` in `quickstart.mdx:48`, `examples/sdk-workflows.mdx:20`, `sdk-python.mdx:537`; drop the `--environment` rows from `cli/reference/key.mdx:32,40,122`. **Flag separately, do not fix:** there is no `key` command in the CLI (`cli/src/index.ts:51-84`) — that whole page may be aspirational.
9. **Tests.** `__tests__/api/keys.test.ts:255-269` still POSTs `environment: 'test'` but now asserts `'live'` — that single flip *is* the negative control. Leave `e2e/helpers/apiKey.ts:57` and the owk_test_ specs alone: they write via the Admin SDK and are the standing back-compat proof.

**Verify:** `rg "owk_\$\{environment\}"` — every remaining hit resolves to the constant or the deliberate rotate inheritance; both `VALID_ENVIRONMENTS` arrays are gone.

### Wave 2 — one organised panel *(UI-only; zero route, payload or document change)* — **SHIPPED** `d12fe0c`

1. **`web/hooks/useApiKeys.ts`** — per §A. Sole owner of every `/api/keys` fetch.
2. **`web/components/ApiKeysManager.tsx`** — move `KeyCard.tsx:1-261` **verbatim**, add the `compact` prop, delete the dialog's parallel list (`AccountSettingsDialog.tsx:1259-1388`) and `formatKeyDate` (`:71-74`). This is what the dialog gains: scope display (`summarizeScopes` :24-31 / :163-166), rotate (:85-104, :192-214), the full status ladder (`keyStatusAt` :33-52), the expiry-warning + grace lines (:178-189), relative last-used (:54-65), and a loading spinner (the dialog sets `apiKeysLoading` at :207 but only uses it to suppress the empty state at :1386). Carry the Wave 1 legacy-`test` badge across — the dialog's grid `grid-cols-[minmax(0,1fr)_9rem_5.25rem_5.25rem_5.25rem]` (:1289) has no slot for it.
3. **Fold create in as inline disclosure** — move `CreateKeyDialog.tsx:56-158` + `:176-330`, delete the reduced form at `AccountSettingsDialog.tsx:1174-1256`. Adds ttl (`:209-223`), the `custom` preset option (`:242`) and the builder (`:250-330`) with its validation (`:74-83`). Stack the scope row `grid-cols-1 sm:grid-cols-[110px_1fr_auto]` (replacing `:268`) — at 390px the builder has ~302px against a fixed 110px select. Collapsed behind `+ create key`; pre-expanded when the list is empty.
4. **Mount + strip.** `AccountSettingsDialog.tsx` — section body becomes header + `<ApiKeysManager compact />` + usage snippet. Delete 8 state hooks (`:127-134`), the fetch block (`:203-214`) and the reset block (`:235-242`) out of the 82-line open-effect with its hand-maintained dep array and eslint-disable (`:245`). Remove the "need custom scopes? manage api keys" link (`:1209-1218`) and its imports (`:20-26`, `:30`, `:16`, the `ApiKeyEntry` alias at `:68`). **If any key state remains in this file, the extraction was done wrong.**
5. **Thin the page.** `settings/api-keys/page.tsx:78-168` → `PageHeader` + `<ApiKeysManager />`. Delete `CreateKeyDialog.tsx` and `KeyCard.tsx` **in the same commit that lands the replacement** — grep first to confirm `page.tsx:12-13` is the only importer, never delete ahead of verification.
6. **Copy pass.** Heading `api keys` lowercase (matches `page.tsx:86` and the e2e `exact: true` assertion). The page's lowercase toasts win over the dialog's sentence-case ones (`:1243`, `:1342`, `:1166`). **Drop the stale comments, do not port them:** `AccountSettingsDialog.tsx:203-206`, `:236-238` and `page.tsx:163-165` describe a pro gate / upgrade card removed by `adbfd06`.
7. **E2E.** New `web/e2e/specs/account/api-keys.spec.ts` modelled on `specs/account/passkeys.spec.ts` — open dialog, select `api`, create with a custom scope, rotate, revoke, plus `assertNoHorizontalOverflow` at 390px (today that only guards the standalone route). Update `e2e/COVERAGE.md:44` to name the dialog as primary. The `revoked` fixme at `api-keys-states.spec.ts:224` **stays fixme'd** — it belongs to Wave 4.

**Non-negotiable:** preserve KeyCard's DOM shape. Six passing tests bind to structural selectors — `div.rounded-md.border` + `p.font-medium` (`api-keys-states.spec.ts:157-158`), `[data-slot="badge"]` (`:182,:198,:218`), `svg.lucide-refresh-cw` / `svg.lucide-trash-2` (`api-keys-rotate-revoke.spec.ts:158,:217`), `code` holding `<prefix>•••` (`:126`). Restyle in a follow-up commit, or add testids and update the specs in the same commit.

### Wave 3 — editable scopes — **SHIPPED**

1. **`web/app/api/keys/_shared.ts`** — move `validateScopes` verbatim (`route.ts:57-94`) plus the constants (`:37-48`), and add `assertScopesGrantable(userId, activeUserData, scopes)` capturing the three gates POST does inline: SUPERADMIN_ONLY_RESOURCES must use id `*` (`:138-147`), superadmin role required (`:149-159`), and `assertUserHasSiteAccess` per site-scoped id (`:181-185`). Rewire POST through it.
2. **PATCH handler** in `api/keys/[keyId]/route.ts`. Auth pair copied from **POST** (`route.ts:114-115` — `rejectAgentTokens: true` + `assertActiveUser`), **not** DELETE (`:31`, which has neither). Accepts `scopes?` (full replacement) and `name?`; rejects `environment` and `ttlDays`. 400 no-updatable-fields, 404 missing, 409 on `revokedAt` **and on `rotatedAt`**, 500 on missing `keyHash`. `db.batch()` writing the user doc + `api_keys/{keyHash}`. Responds with `buildApiKeyListItem(...)` so the panel patches its row without a refetch.
3. **Rate limit** — `withRateLimit(..., { strategy: 'api', identifier: 'ip' })`, matching `keys/route.ts:111,286`. Leave rotate/DELETE unwrapped; that asymmetry is a separate cleanup.
4. **Audit** — `emitMutation` kind `api_key_mutated`, new `verb: 'update'`, `changedFields`, and `scopeFingerprintBefore/After` via `auditLogClient.ts:47` (never raw scopes). Update the taxonomy comment at `auditLogClient.ts:111`. `auditMutationCoverage.test.ts` needs no edit (file-level scan).
5. **`web/openapi.yaml`** — `patch:` under `/api/keys/{keyId}` (`:2469-2496`) + `UpdateApiKeyRequest`, using the rotate block (`:2498-2529`) as the literal template. Explicit `security: [firebaseIdToken]`; do **not** add `security: []` (flips it to public). Scope note and code samples are derived (`lib/openapiReference.ts:75,204-210`), so `__tests__/api/openapi.test.ts:34-54` stays green.
6. **Panel wiring** — an "edit scopes" affordance on the row reusing the Wave 2 builder. Surface the grace-window caveat (below) as a lowercase warning naming the predecessor's `retiresAt`.
7. **Docs** — a PATCH bullet at `content/docs/api/authentication.mdx:177-178`; extend `idempotency.mdx:10` to name PATCH among the key routes that ignore the header. SDK/CLI parity is a follow-on, not this wave.
8. **Tests** — `__tests__/api/keys.test.ts`: `makePatch` copied from `makeRotate` (`:139-148`). Must assert (a) the **lookup** row carries the new scopes, (b) 403 for a non-superadmin adding user/installer scopes (reuse the `it.each` at `:166-185`), (c) 409 revoked and 409 rotated, (d) 400 no-updatable-fields, (e) one `emitMutation` with verb `update`. Implementation constraint: use `db.batch()` — the mocked docRef (`:105-118`) has no `.update()`.

**Delivered beyond the plan:** the scope builder was extracted to `components/ApiKeyScopeFields.tsx` and shared by create and edit — a builder that could express less on edit than on create would strand keys in states the UI could produce but not correct. SDK parity was pulled forward rather than deferred (`keys.update` in both the node and python SDKs, with tests), since both already wrapped the other three lifecycle routes and a missing `update` would have read as "not supported". The audit event carries `scopeFingerprint{Before,After}` **and** the counts: the fingerprint is opaque, so the counts are what make a key quietly going from one scope to nine visible on a scan.

**The finding that decides the design:** authorization reads scopes *exclusively* from `api_keys/{keyHash}` (`apiAuth.server.ts:185,230`); the user subcollection is never consulted on the auth path (only a fire-and-forget `lastUsedAt` write at `:241-248`). A PATCH that writes one doc leaves the credential on its old permissions indefinitely while the UI claims otherwise. There is no cache — the batch commit takes effect on the very next request.

### Wave 3.5 — key-list layout *(follow-up; reported against Wave 3 the day it shipped)* — **SHIPPED**

Wave 3's editor put the scope checkboxes behind a dropdown whose `custom` option was the only route to them, and the product owner asked "why no checkboxes?".

**An always-visible resource x permission grid was built for this, shipped as `0eb356a`, and REVERTED.** Recording why, so it is not rebuilt: it made the account-settings dialog worse. That panel's content box is ~320px (a `w-48` sidebar plus nested padding), so the grid never reached its matrix breakpoint and rendered as a six-resource stacked column — taller and harder to scan than the compact rows it replaced. The instruction was "all you had to do was add the edit scope button", and that was right; the builder itself was fine once reachable. The scope builder is now byte-identical to `7b356fb`.

What survived from that round, because it was asked for and is genuinely better:

1. **The editor is joined to its key row** — square inner corners, one continuous accent outline across card and editor, `editing <name>` heading. A detached panel gave no indication which key was being edited.
2. **The key list is a collapsible row, not a stacked card.** Wave 2 had replaced the dialog's hand-tuned column layout (`grid-cols-[minmax(0,1fr)_9rem_5.25rem_5.25rem_5.25rem]`, iterated four times against screenshots) with `KeyCard`'s stacked form. That was a regression and this restores the column idea: name + status, then `created` / `last used` / `expires` as fixed tracks with the label stacked over the value.
3. **The prefix and scope summary collapse behind a per-row toggle**, and the three action buttons became one overflow menu. Both were width, not information: the prefix cost ~7rem for something nobody reads at a glance, and the buttons cost ~7.5rem. Together they left the name under 100px in the dialog; it now gets ~260px. The button count also caused a real alignment bug — three buttons on an active row versus one on an expired row is an 80px swing that came out of the only flexible column, so no two rows agreed on where their columns began.
4. **Terminal keys keep a bare delete button** rather than a menu holding one item.

**Do not relitigate:** the account dialog and `/settings/api-keys` mount the same component at very different widths (~320px vs ~800px), so any layout here must be verified in the dialog first — a viewport breakpoint cannot tell those two apart, and the dialog is the narrower and more discoverable of the two.

**Still open, deliberately not fixed here:** the original builder's `custom` option still discards the selected preset's scopes (pick `operator`, switch to `custom`, and 14 of 16 grants are silently replaced by one `site` row). It predates all of this work and is a separate decision.

### Wave 4 — soft-delete revoke *(touches the hot auth path — sequence carefully)*

1. **`DELETE /api/keys/{keyId}`** (`[keyId]/route.ts:50-56`) → `batch.update(keyRef, { revokedAt: now })` + `batch.set(lookupRef, { revokedAt: now }, { merge: true })`. **Merge-set, not `update`, on the lookup** — a batched `update()` against a missing doc fails the *entire* commit with NOT_FOUND, and the lookup can legitimately be absent (see the `.catch()` + comment at `userDeleteCascade.server.ts:180-182`). `Date.now()`, not `serverTimestamp()` (`ApiKeyRecord.revokedAt` is `number`, `apiKeyTypes.ts:65`). Idempotency guard: already-set `revokedAt` returns 200 without re-stamping. Rewrite the docblock at `:22-28`.
2. **The second, forgotten copy** — `api/account/api-keys/[keyId]/route.ts:48-54` hard-deletes both docs identically. Same edit, **same commit**. Guard: `rg "batch.delete\(db.collection\('api_keys'\)" web/app` must return zero hits afterward (today: exactly these two).
3. **Derive `revoked`** — add to `ApiKeyListItem` (`apiKeyTypes.ts:174-192`) beside `expired`/`retired`, emitted as `revokedAt !== null` in `buildApiKeyListItem` (`:236-265`) with a comment saying why not `<= now`. `scripts/migrations/audit-legacy-api-keys.mjs:150` diverges — leave it, it's an ops report, not the contract.
4. **Panel: terminal `revoked` state** — insert `if (k.revoked) return { label: 'revoked', tone: 'muted' }` as the **first** branch of `keyStatusAt` (ahead of `expired`), mirroring the auth path's own precedence. Gate the rotate button (`&& !apiKey.revoked` — rotate already 409s) and hide the revoke button on revoked rows.
5. **Display decision for a monotonically-growing list** — revoked keys now stay in `GET /api/keys` forever, interleaved by `createdAt desc` (`route.ts:296`). The Wave 2 sort already pushes terminal states last; extend it to group revoked rows behind a "show revoked" disclosure. **Do not filter server-side** — that defeats the auditability this change buys.
6. **Tests** — `__tests__/api/keys.test.ts:299-300` inverts (both docs exist, numeric `revokedAt`), plus a lookup-absent case (route still 200s — the NOT_FOUND guard) and a double-revoke case. `__tests__/lib/apiKeyListItem.test.ts:75-147` gains `revoked` cases including a **future** `revokedAt` → `true`. `__tests__/lib/apiAuth.test.ts:722` already proves a marked lookup 401s — cite it, don't change it. No test exists for `api/account/api-keys/[keyId]` — add a sibling describe or state explicitly that it ships e2e-only.
7. **E2E** — promote the `test.fixme` at `api-keys-states.spec.ts:224-241` (same pattern as the expired promotion at `:205-222`), and add the tone assertion. **Run it against unmodified `dev` first and confirm it fails** — the seeded key's `expiresAt` is 60 days out so it renders `active` today; a first-try green means the derivation isn't wired. Invert all three post-conditions in `api-keys-rotate-revoke.spec.ts:232,241,244` — **keep and flip** the lookup-doc assertion; it is the only end-to-end guard on the mirror write `apiAuth.server.ts:199` depends on.
8. **`openapi.yaml`** — add `revoked` to the `ApiKey` schema beside `expired`/`retired` (`:8908-8917`), and fix `:2476-2478` / `:246`: "Effective within 60s across all edge caches. Cannot be undone" is wrong on all three counts (no cache, immediate, and reversible per the runbook). SDKs need no change — both just DELETE and return void.
9. **Retention — recommend deferring.** The obvious home is `functions/src/apiKeyExpire.ts`, **but that sweep is dead code**: `.where('expiredMarkedAt','==',null)` (`:50`) matches only docs where the field exists and is null, and no writer ever initialises it (repo-wide it is assigned only at `:67`). Fixing that (initialise `expiredMarkedAt: null` on create + rotate, index-compatible) may be the entire retention answer — once live, the existing sweep reaps every revoked key's lookup at its `expiresAt` (≤365 days). Adding a `revokedAt` range pass instead requires **a new COLLECTION_GROUP field override in `firestore.indexes.json`** (`"fieldOverrides": []` at `:247`; automatic single-field indexes are COLLECTION scope only) deployed and Enabled *before* the function, per `docs/runbooks/upgrade-2.12.0.md:294-304`. **Flagged as the only guarded-file touch in this plan.** Default position: fix the sweep, skip the index.

### Wave 5 — expiry notice emails *(largest new surface)*

1. **`web/app/api/cron/api-key-expiry/route.ts`** on the `health-check` shape — inline 3-line `x-cron-secret` check (there is no shared wrapper; verbatim in all six crons), `apiError(error, 'cron/api-key-expiry')`, returns `{ ok, keysScanned, usersNotified, emailsSent }`, and exports the per-key threshold decision as a pure function (mirroring `classifyMachineHealth`, `health-check/route.ts:141`).
2. **Enumeration with no index change** — `collectionGroup('api_keys').where('expiresAt','>=', now - DAY_MS).where('expiresAt','<=', now + 14*DAY_MS)`: two range filters on one field, served by the automatic index. Owner uid from `doc.ref.parent.parent.id`. Threshold bucket and already-notified checks in memory. **Do not add `.where(<field>,'==',null)`** — that idiom is exactly what makes `apiKeyExpire.ts` inert.
3. **Notice state** — `expiryNoticedStages?: number[]` on `ApiKeyRecord` (`apiKeyTypes.ts:48-66`), merge-written **only** to `users/{uid}/api_keys/{keyId}`. **Never** onto `ApiKeyLookup` (`:72-81`) — that doc is the hottest read in the system. Stamp **before** send, per `health-check/route.ts:505-519`.
4. **Skip rules** — any doc with `rotatedAt` or `revokedAt`. Rotation mints a new doc and leaves the old `expiresAt` untouched (`rotate/route.ts:144-179`), so a superseded key otherwise nags about a credential already replaced.
5. **Promote the threshold constant** — `EXPIRATION_WARNING_MS` moves from `KeyCard.tsx:13` into `apiKeyTypes.ts` beside `DEFAULT_TTL_DAYS`/`MAX_TTL_DAYS` (`:134-136`) so badge and email cannot drift.
6. **`getUserAlertRecipient(userId, 'apiKeyAlerts')`** in `lib/adminUtils.server.ts` — the one genuinely missing piece; every existing resolver is site-scoped and keys have no site. Copy `getSiteAlertRecipients`' `typeof deletedAt === 'number'` guard (`:207/:219`), opt-out semantics (`:211`) and `alertCcEmails` (`:212`). **Do not port** the `ADMIN_EMAIL` fallback (`:240-272` — it exists for orphan sites and would mail the admin about a stranger's key) or `mutedMachines`.
7. **Preference plumbing, 6 files, following `talonAlerts` line for line** — `adminUtils.server.ts:178`, `contexts/AuthContext.tsx:184-190` + **both** default literals (`:266`, `:296`) + normalisation `!== false` (`:426-430`) + the equality memo (`:455-462`), and **mandatorily** `app/api/unsubscribe/route.ts:13-20` (that map is written wholesale at `:72-75` — an omitted key is never unsubscribed, which is compliance-facing). No rules change: preferences are allowlisted by key name.
8. **Both preference UIs** — one entry in `settings/alerts/page.tsx` `ALERT_TOGGLES` (`:31-62`) + the union (`:23-29`) + `pickAlertPrefs` (`:64-73`), and a hand-rolled card in `AccountSettingsDialog.tsx` copying `talonAlerts` at `:703-714`. Label `api key expiry`, description `emails before an api key expires`. Do **not** add it to the hydration dep array at `:246` — three siblings are already absent there.
9. **Email builder** in `lib/emailTemplates.server.ts` next to `buildDisplayDigestEmail:357`, reusing `EMAIL_COLORS`/`escapeHtml`/`safeEmailSubject`/`emailDataTable`/`emailTimestamp`. **Pass `unsubscribeUrl` even when undefined** — `wrapEmailLayout:151` decides an email is an alert via `'unsubscribeUrl' in options`, so omitting the key produces an alert with no opt-out link. Register `api_key_expiring` in **both** hand-duplicated preview lists (`api/test-email/route.ts:29` and `admin/email/page.tsx:40`) plus a `case` in `buildTemplateEmail`.
10. **Group by owner, send per recipient** — `Map<ownerUid, ExpiringKey[]>` exactly like `display-alerts/route.ts:71-77`; one email per owner. Guard `if (!resendClient) continue` (`:94-97`), check `result.error` rather than trusting the promise (`:127-139`), per-owner try/catch (`:149-151`). Body carries only `name`, `keyPrefix`, `expiresAt` — never keyHash, never scopes.
11. **Schedule + docs** — register `0 8 * * *` on cron-job.org for dev **and** prod with each environment's `CRON_SECRET` (schedules are not in the repo). No new env vars. Update `content/docs/setup/web-deployment.mdx:152-181` and `dashboard/admin/email-alerts.mdx:10-24,50-60,77-88,96-110`.
12. **Tests** — new `web/__tests__/api/cron/api-key-expiry.test.ts` beside the three existing cron tests: bucket boundaries, already-stamped skip, rotated/revoked skip, long-expired below the lower bound, 401 on a bad secret. Extend `__tests__/lib/adminUtils.test.ts` for `getUserAlertRecipient` rather than starting a new file.

## Cross-wave contracts

- Wave 2 must carry the Wave 1 legacy-`test` marker into `ApiKeysManager`, or legacy keys become invisible.
- Wave 3's edit affordance and Wave 4's `revoked` badge both land inside `ApiKeysManager` — Wave 2 must ship first or both fork the UI again.
- Wave 4 depends on Wave 2 for the revoked-row display decision, and must land the `AccountSettingsDialog.tsx:1337-1342` optimistic `.filter()` fix — under soft delete that row vanishes and then reappears on the next refetch. Wave 2 already replaces that call site with the hook's refetch; if Wave 4 somehow lands first, fix it there.
- Wave 5's promoted `EXPIRATION_WARNING_MS` is imported by both the manager and the cron.
- Waves 1–3 change zero stored data. Wave 4 changes revoke's post-condition. Wave 5 adds one field to the user-subcollection doc. **No wave migrates an existing record**, and existing keys authenticate unchanged throughout.

## Risks

- **Silent e2e breakage from restyling during the Wave 2 move.** The settings specs bind to structural selectors, not testids; the route surviving does not protect them. Move markup verbatim, defer visual redesign.
- **Stale lookup doc (Wave 3).** A PATCH writing only the user doc produces a UI claiming reduced scopes while the credential retains full access forever. One batch, both docs, with the unit assertion on `api_keys/<hash>.scopes`.
- **Rotation grace defeats a scope reduction (Wave 3).** The predecessor's lookup stays live with original scopes until `retiresAt` (`rotate/route.ts:176-182`, honoured at `apiAuth.server.ts:203-207`). 409 any PATCH on a key with `rotatedAt`; for a successor with `rotatedFromKeyId`, either apply the new scopes to the predecessor's lookup in the same batch or warn naming its `retiresAt`. Never silently ignore.
- **Privilege escalation (Wave 3).** `validateScopes` checks shape only; the superadmin and site-access gates are separate inline code. A PATCH wired to validation alone lets any member escalate a live key to `user:*:admin`. Shared helper + duplicated `it.each` rejection test.
- **Empty `scopes: []` (Wave 3)** puts a live key on the legacy-bypass branch (`apiAuth.server.ts:221-228`). Keep the non-empty guard as the only path to the field.
- **NOT_FOUND on the lookup (Wave 4)** would leave a key neither revoked nor deleted — still fully authenticating — while the UI toasts "key revoked". Merge-set, one atomic commit, pinned by a lookup-absent unit test. Do **not** split it into a fire-and-forget `.catch()`.
- **Cross-user leakage (Wave 5).** `collectionGroup` under the Admin SDK returns every user's keys flat; a grouping bug puts one customer's key names in another's inbox. Derive the owner from `doc.ref.parent.parent.id` and assert it matches the uid the recipient email was read from before rendering.
- **Duplicate emails (Wave 5)** from double registration or scheduler retry. Stamp before send and accept health-check's stated trade: a lost email beats an alert loop.
- **Unregistered cron fails silently** (`web-deployment.mdx:169-175`) — the feature simply never fires, with no error anywhere.
- **`sweepExpiredApiKeysDaily` is inert** (`apiKeyExpire.ts:50`). Pre-existing defect, surfaced by two workstreams. File it; do not build on it without fixing it first.
- **Someone "improves" the panel into a client `onSnapshot`** — that would require loosening `firestore.rules` and would expose key metadata to the client SDK. Reject on sight.

## Success Criteria

1. The account dialog's `api` section is a complete surface: create with a custom scope builder and a ttl, see each key's scopes and status, rotate, edit scopes, and revoke — with no link out. `/settings/api-keys` still resolves and renders the same component.
2. `AccountSettingsDialog.tsx` holds **zero** api-key state hooks and **zero** `/api/keys` fetches; every key read in the app goes through `web/hooks/useApiKeys.ts`.
3. Every mint path produces `owk_live_*`; `POST /api/keys` with `environment: 'test'` returns a live key with a 200, and `__tests__/api/keys.test.ts` proves it. Existing `owk_test_*` keys still authenticate — proven on every CI run by `installer-deployments.spec.ts` and `public-api-smoke.spec.ts`.
4. `PATCH /api/keys/{keyId}` updates scopes on **both** documents in one commit, is rejected for ungrantable scopes and for revoked/rotated keys, and emits `api_key_mutated` / `verb: 'update'` with before/after scope fingerprints.
5. Revoking retains both documents with a numeric `revokedAt`; the key 401s on the very next request; the row renders a muted `revoked` badge with rotate and revoke suppressed; the promoted e2e test was **shown to fail** against pre-change `dev`.
6. `rg "batch.delete\(db.collection\('api_keys'\)" web/app` returns zero hits.
7. A user with a key expiring in 14/3/0 days receives exactly one email per threshold listing all their affected keys, honours the `apiKeyAlerts` preference, and is turned off by the one-click unsubscribe link.
8. `npm run lint`, `npm test`, and one orchestrator-run e2e pass are green at the end of every wave; `openapi.yaml` validates via `__tests__/api/openapi.test.ts`.
9. **`firestore.rules` is untouched.** `firestore.indexes.json` is untouched unless Wave 4 step 9 explicitly chooses the `revokedAt` range pass — in which case the index deploys and reaches Enabled before the function.